---
phase: 10-automated-test-coverage
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - tests/Feature/NotificationDeliveryTest.php
autonomous: true
requirements:
  - TEST-05

must_haves:
  truths:
    - "php artisan test --filter=NotificationDeliveryTest exits 0 with 12 passed"
    - "php artisan test runs the full suite with zero failures"
    - "Full suite count is at least 50 tests across all feature + unit files"
    - "No test is skipped, incomplete, or asserting trivially-true conditions"
    - "TESTING.md documents environment setup: createdb briefy_test + php artisan migrate --env=testing"
  artifacts:
    - path: "tests/Feature/NotificationDeliveryTest.php"
      provides: "Notification system tests (12 test cases covering RT-03 to RT-07)"
      contains: "test_notification_created_on_assignment"
    - path: "TESTING.md"
      provides: "Environment setup documentation"
      contains: "createdb briefy_test"
  key_links:
    - from: "tests/Feature/NotificationDeliveryTest.php"
      to: "BriefyNotification model + demands.inline.update route"
      via: "actingAs($actor)->put(route('demands.inline.update', $demand), [...])->assertDatabaseHas('briefy_notifications')"
      pattern: "briefy_notifications"
    - from: "TESTING.md"
      to: "phpunit.xml"
      via: "Documents DB_CONNECTION=pgsql + DB_DATABASE=briefy_test environment"
      pattern: "briefy_test"
---

<objective>
Verify that the existing notification delivery tests pass (TEST-05), run the full test suite to confirm zero failures, and document the local environment setup required to run the suite.

Purpose: This is the final integration check. Plans 10-01 and 10-02 handled individual suites; this plan runs everything together, catches any cross-suite interference, and produces the TESTING.md per D-10.

Output: Full `php artisan test` run exits 0. TESTING.md created with environment setup. All 5 TEST-0X requirements confirmed green.
</objective>

<execution_context>
@D:/projetos/briefy/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
@D:/projetos/briefy/.planning/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/projetos/briefy/.planning/PROJECT.md
@D:/projetos/briefy/.planning/ROADMAP.md
@D:/projetos/briefy/.planning/phases/10-automated-test-coverage/10-CONTEXT.md
@D:/projetos/briefy/phpunit.xml

<interfaces>
<!-- Confirmed from reading NotificationDeliveryTest.php and phpunit.xml. -->

NotificationDeliveryTest test names (12 tests — do not modify):
- test_notification_created_on_assignment (RT-03)
- test_no_notification_when_no_assignee (RT-03)
- test_no_self_notification_on_assignment (RT-03)
- test_notification_created_for_creator_on_status_change (RT-04)
- test_notification_created_for_assignee_on_status_change (RT-04)
- test_no_self_notification_on_status_change (RT-04)
- test_dedup_when_creator_equals_assignee_on_status_change (RT-04)
- test_unread_count_shared_in_inertia_props (RT-05)
- test_get_notifications_returns_user_notifications (RT-06)
- test_mark_single_notification_as_read (RT-07)
- test_mark_all_notifications_as_read (RT-07)
- test_cannot_read_other_users_notification (RT-07)

phpunit.xml environment (reference):
DB_CONNECTION=pgsql
DB_DATABASE=briefy_test
BROADCAST_CONNECTION=null (prevents actual Reverb connection in tests)
QUEUE_CONNECTION=sync
SESSION_DRIVER=array
CACHE_STORE=array

Note: NotificationDeliveryTest does NOT attach pivot rows for its test users. This is correct behavior — the notification tests exercise DemandController actions that do NOT require isAdminOrOwner() checks (the actor is just a user in the same org, not required to be admin). Do not modify NotificationDeliveryTest to add pivot attaches — it would change the test intent.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run NotificationDeliveryTest in isolation and fix any genuine failures</name>
  <files>tests/Feature/NotificationDeliveryTest.php</files>
  <read_first>
    - tests/Feature/NotificationDeliveryTest.php — full file read; confirm all 12 test methods present; note that users are created WITHOUT pivot attach (deliberate — notification tests don't need isAdminOrOwner())
    - app/Models/BriefyNotification.php — read to confirm factory exists and model has: organization_id, user_id, type, read_at columns
    - database/factories/BriefyNotificationFactory.php — confirm available factory attributes
    - app/Events/DemandAssigned.php — read constructor; test uses Event::fake([DemandAssigned::class]) which intercepts the event dispatch
    - app/Events/DemandStatusChanged.php — read constructor
    - routes/web.php — search for 'notifications' routes to confirm: notifications.index (GET), notifications.read (POST /{notification}), notifications.read-all (POST)
  </read_first>
  <action>
Run the notification test suite in isolation:

```bash
php artisan test --filter=NotificationDeliveryTest --stop-on-failure
```

**Common failure patterns to check:**

1. `test_unread_count_shared_in_inertia_props` asserts `$page->where('unread_notifications', 3)`. If the shared prop key is different (e.g., `notifications_count` or `unread_count`), check `app/Http/Middleware/HandleInertiaRequests.php` for the actual key name and update the assertion to match.

2. `test_get_notifications_returns_user_notifications` uses `assertJsonCount(3)`. If the JSON response wraps data in a `{'data': [...]}` envelope, change to `assertJsonCount(3, 'data')`.

3. `test_notification_created_on_assignment` uses `demands.inline.update` route. If this route now requires the user to be admin/owner (EnsureRole middleware was added), the test may get 403. Confirm by running `php artisan route:list | grep inline` — if EnsureRole is on this route, add pivot attach for $actor in that specific test.

4. `BriefyNotification::factory()` — if factory does not exist, the test_unread_count_shared_in_inertia_props test will fail with "Target class not found". Check `database/factories/BriefyNotificationFactory.php` exists.

For each genuine failure: fix production code or the specific broken assertion (the test intent must remain — do not weaken assertions). Per D-06, do NOT modify tests unless fixing a bug.
  </action>
  <verify>
    <automated>php artisan test --filter=NotificationDeliveryTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=NotificationDeliveryTest` exits 0 with output "12 passed"
    - All 12 test method names still present in file (grep: `grep -c 'public function test_' tests/Feature/NotificationDeliveryTest.php` returns 12)
    - No test uses markTestSkipped() or markTestIncomplete()
    - assertDatabaseHas('briefy_notifications', [...]) present in at least 4 test methods
    - assertDatabaseMissing('briefy_notifications', [...]) present in at least 2 test methods
  </acceptance_criteria>
  <done>NotificationDeliveryTest runs 12/12 passing. TEST-05 verified green.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite smoke run and create TESTING.md</name>
  <files>TESTING.md</files>
  <read_first>
    - phpunit.xml — read full file to extract all env vars for TESTING.md documentation
    - .planning/phases/10-automated-test-coverage/10-01-SUMMARY.md — read to get final count of auth+org tests passing (created by plan 10-01 executor)
    - .planning/phases/10-automated-test-coverage/10-02-SUMMARY.md — read to get final count of demand+AI tests passing (created by plan 10-02 executor)
    - tests/Feature/DemandLifecycleTest.php — confirm file exists with 9 tests (created by plan 10-02)
  </read_first>
  <action>
**Step 1: Run the full test suite.**

```bash
php artisan test
```

Capture the output. The expected result is zero failures across all suites (Feature + Unit).

**If failures occur:**

Triage each failure:
- **Environment failure** (DB not found, migration not run): document in TESTING.md under "Known Setup Requirements". Do NOT mark the test as skipped.
- **Cross-suite interference** (one test leaving state that breaks another): investigate if RefreshDatabase is missing from any class. If a test class is missing `use RefreshDatabase`, add it.
- **Genuine bug** introduced between phases: fix the bug in production code per D-07.

After fixing, re-run `php artisan test` until it exits 0.

**Step 2: Create TESTING.md** (per D-10) in the project root with this content:

```markdown
# Testing Guide

## Prerequisites

1. **PostgreSQL running locally**
2. **Test database created:**
   ```bash
   createdb briefy_test
   ```
3. **Migrations run against test DB:**
   ```bash
   php artisan migrate --env=testing
   ```

## Running Tests

### Full suite
```bash
php artisan test
```

### Single class
```bash
php artisan test --filter=DemandLifecycleTest
php artisan test --filter=AuthenticationTest
php artisan test --filter=NotificationDeliveryTest
```

### Auth suite only
```bash
php artisan test tests/Feature/Auth/
```

## Environment Configuration

Tests use a separate PostgreSQL database configured in `phpunit.xml`:

| Variable | Value |
|----------|-------|
| DB_CONNECTION | pgsql |
| DB_DATABASE | briefy_test |
| BROADCAST_CONNECTION | null |
| QUEUE_CONNECTION | sync |
| SESSION_DRIVER | array |
| CACHE_STORE | array |

`RefreshDatabase` wraps each test in a transaction and rolls back after — no manual cleanup needed between runs.

## Test Coverage Map

| Requirement | Test Class | Cases |
|-------------|-----------|-------|
| TEST-01: Auth flow | AuthenticationTest, RegistrationTest | 11 |
| TEST-02: Org management | OrganizationCreationTest, InvitationControllerTest, InvitationAcceptTest, TeamRoleTest, TeamRosterTest, EnsureRoleTest | 22 |
| TEST-03: Demand lifecycle | DemandLifecycleTest | 9 |
| TEST-04: AI chat | AiChatControllerTest | 6 |
| TEST-05: Notifications | NotificationDeliveryTest | 12 |

## CI/CD

GitHub Actions with PostgreSQL service is **deferred** until the branch is published. See `.planning/phases/10-automated-test-coverage/10-CONTEXT.md` D-11 for context.
```

**Step 3: Verify TESTING.md was created** by checking it exists and contains `createdb briefy_test`.
  </action>
  <verify>
    <automated>php artisan test && grep 'createdb briefy_test' TESTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test` exits 0 with zero failures
    - Full suite output shows at least 50 tests (Feature + Unit combined)
    - `TESTING.md` exists in project root
    - `grep 'createdb briefy_test' TESTING.md` finds a match
    - `grep 'DB_DATABASE' TESTING.md` finds a match with value `briefy_test`
    - `grep 'DemandLifecycleTest' TESTING.md` finds a match in the coverage table
    - No test class in the suite is missing `use RefreshDatabase` (if any was missing, it was fixed)
    - `php artisan test` output line showing total tests count: at least "60 tests" or the actual number passing
  </acceptance_criteria>
  <done>Full test suite passes with zero failures. TESTING.md exists in project root with environment setup and coverage map. Phase 10 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test user → notification ownership | notifications.read endpoint must only allow the notification owner to mark it read |
| test suite → production DB | php artisan test must always target briefy_test (pgsql), never briefy (production) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-12 | Elevation of Privilege | NotificationDeliveryTest — ownership | HIGH | mitigate | test_cannot_read_other_users_notification confirms the notifications.read endpoint returns 403 when another user's notification is targeted — gate is verified by test |
| T-10-13 | Information Disclosure | test_get_notifications_returns_user_notifications | HIGH | mitigate | Test creates notifications for two users and confirms GET /notifications only returns the authenticated user's 3 records (not other user's 2) — scoping gate is tested |
| T-10-14 | Tampering | Full suite cross-contamination | MEDIUM | mitigate | All test classes must use RefreshDatabase; any class missing it could leave rows in briefy_test that cause false positives in later classes — verified during smoke run |
| T-10-15 | Denial of Service | Full suite runtime | LOW | accept | Running php artisan test against PostgreSQL locally may take 30–60 seconds; acceptable for pre-commit check; CI is deferred (D-11) |
| T-10-16 | Repudiation | TESTING.md accuracy | LOW | mitigate | TESTING.md is generated from phpunit.xml values and confirmed test counts — not aspirational documentation; counts come from actual test run output |
</threat_model>

<verification>
Full phase completion verification:

```bash
# TEST-05: notification suite
php artisan test --filter=NotificationDeliveryTest
# Expected: 12 passed, exit 0

# Full suite smoke run
php artisan test
# Expected: zero failures, exit 0

# TESTING.md presence
grep 'createdb briefy_test' TESTING.md
# Expected: match found

# Structural check on DemandLifecycleTest (created in 10-02)
grep -c 'public function test_' tests/Feature/DemandLifecycleTest.php
# Expected: 9

# Phase success criteria aggregate
php artisan test --filter=AuthenticationTest     # >= 5 passed (TEST-01)
php artisan test --filter=RegistrationTest       # >= 6 passed (TEST-01)
php artisan test --filter=OrganizationCreationTest  # >= 4 passed (TEST-02)
php artisan test --filter=DemandLifecycleTest    # 9 passed (TEST-03)
php artisan test --filter=AiChatControllerTest   # 6 passed (TEST-04)
php artisan test --filter=NotificationDeliveryTest  # 12 passed (TEST-05)
```
</verification>

<success_criteria>
1. `php artisan test` runs the full suite with zero failures in a properly configured environment (TEST-05 + phase goal).
2. Auth flow has at least 4 test cases passing (AuthenticationTest alone has 5 — criterion met).
3. Demand lifecycle (create, status update, assign, archive, trash/restore) has passing tests — DemandLifecycleTest 9/9.
4. Organization creation and team management flows have passing tests — 22 tests across 6 classes.
5. AI chat endpoint has at least one integration test verifying message persistence — test_stream_persists_user_and_assistant_messages passes.
6. TESTING.md exists with createdb briefy_test + php artisan migrate --env=testing setup instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-automated-test-coverage/10-03-SUMMARY.md` with:
- NotificationDeliveryTest pass/fail details
- Full suite total test count (Feature + Unit)
- Any fixes applied during smoke run
- TESTING.md creation confirmed
- Phase 10 overall status: COMPLETE
- All 5 TEST-0X requirements with their final status
</output>
