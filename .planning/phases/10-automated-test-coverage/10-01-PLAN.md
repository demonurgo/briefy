---
phase: 10-automated-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Auth/AuthenticationTest.php
  - tests/Feature/Auth/RegistrationTest.php
  - tests/Feature/OrganizationCreationTest.php
  - tests/Feature/InvitationControllerTest.php
  - tests/Feature/InvitationAcceptTest.php
  - tests/Feature/TeamRoleTest.php
  - tests/Feature/TeamRosterTest.php
  - tests/Feature/EnsureRoleTest.php
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "php artisan test --filter=AuthenticationTest exits 0 with 5 tests passing"
    - "php artisan test --filter=RegistrationTest exits 0 with 6 tests passing"
    - "php artisan test --filter=OrganizationCreationTest exits 0 with 4 tests passing"
    - "php artisan test --filter=InvitationControllerTest exits 0 with 5 tests passing"
    - "php artisan test --filter=InvitationAcceptTest exits 0 with 4 tests passing"
    - "php artisan test --filter=TeamRoleTest exits 0 with 3 tests passing"
    - "php artisan test --filter=TeamRosterTest exits 0 with 3 tests passing"
    - "php artisan test --filter=EnsureRoleTest exits 0 with 3 tests passing"
    - "All auth tests use RefreshDatabase and actingAs patterns"
    - "All org/team tests attach pivot rows via organizations()->attach()"
  artifacts:
    - path: "tests/Feature/Auth/AuthenticationTest.php"
      provides: "Auth flow tests (login, logout, invalid credentials)"
      contains: "test_users_can_authenticate_and_last_login_is_updated"
    - path: "tests/Feature/Auth/RegistrationTest.php"
      provides: "Registration flow tests (new user, validation errors)"
      contains: "test_new_users_can_register_and_organization_is_created"
    - path: "tests/Feature/OrganizationCreationTest.php"
      provides: "Org creation tests (store, slug duplicate, validation)"
      contains: "test_store_creates_org_and_attaches_owner"
    - path: "tests/Feature/InvitationControllerTest.php"
      provides: "Invitation send/cancel tests"
      contains: "test_admin_can_invite_by_email"
    - path: "tests/Feature/InvitationAcceptTest.php"
      provides: "Invitation accept flow tests"
      contains: "test_new_user_can_accept_invite_and_joins_org"
    - path: "tests/Feature/TeamRoleTest.php"
      provides: "Role change tests"
      contains: "test_admin_can_change_collaborator_to_admin"
    - path: "tests/Feature/TeamRosterTest.php"
      provides: "Member remove tests"
      contains: "test_admin_can_remove_collaborator"
    - path: "tests/Feature/EnsureRoleTest.php"
      provides: "Middleware RBAC guard tests"
      contains: "test_collaborator_gets_403_on_invite_route"
  key_links:
    - from: "tests/Feature/Auth/AuthenticationTest.php"
      to: "/login POST handler"
      via: "$this->post('/login', [...])->assertRedirect()"
      pattern: "post.*login.*assertRedirect"
    - from: "tests/Feature/OrganizationCreationTest.php"
      to: "organizations.store route"
      via: "$this->post(route('organizations.store'), [...])"
      pattern: "post.*organizations.store"
    - from: "tests/Feature/InvitationControllerTest.php"
      to: "team.invite route with EnsureRole middleware"
      via: "actingAs($collab)->post(route('team.invite'))->assertForbidden()"
      pattern: "assertForbidden"
---

<objective>
Verify that the existing auth flow and organization management test suites pass with zero failures against the PostgreSQL briefy_test database.

Purpose: TEST-01 and TEST-02 are already implemented across eight test classes. This plan runs each suite to confirm they pass, diagnoses any failures caused by bugs (not environment issues), and fixes genuine regressions. No new tests are written — the goal is a clean green run.

Output: All 33 auth + org tests passing. `php artisan test --filter=Auth` and `php artisan test --filter='InvitationControllerTest|InvitationAcceptTest|TeamRoleTest|TeamRosterTest|EnsureRoleTest|OrganizationCreationTest'` both exit 0.
</objective>

<execution_context>
@D:/projetos/briefy/.planning/../../../.claude/get-shit-done/workflows/execute-plan.md
@D:/projetos/briefy/.planning/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/projetos/briefy/.planning/PROJECT.md
@D:/projetos/briefy/.planning/ROADMAP.md
@D:/projetos/briefy/.planning/phases/10-automated-test-coverage/10-CONTEXT.md
@D:/projetos/briefy/.planning/phases/10-automated-test-coverage/10-PATTERNS.md

<interfaces>
<!-- Key patterns confirmed from reading the existing test files. -->

phpunit.xml environment:
```xml
<env name="DB_CONNECTION" value="pgsql"/>
<env name="DB_DATABASE" value="briefy_test"/>
<env name="BROADCAST_CONNECTION" value="null"/>
<env name="QUEUE_CONNECTION" value="sync"/>
```

AuthenticationTest test names (5 tests):
- test_login_screen_renders
- test_users_can_authenticate_and_last_login_is_updated
- test_users_cannot_authenticate_with_invalid_password
- test_users_cannot_authenticate_with_nonexistent_email
- test_users_can_logout

RegistrationTest test names (6 tests):
- test_registration_screen_renders
- test_new_users_can_register_and_organization_is_created
- test_registration_fails_with_duplicate_email
- test_registration_fails_with_mismatched_passwords
- test_registration_fails_with_missing_name
- test_registration_fails_with_invalid_email

OrganizationCreationTest test names (4 tests):
- test_store_creates_org_and_attaches_owner
- test_store_rejects_duplicate_slug
- test_store_requires_authentication
- test_store_requires_name

InvitationControllerTest test names (5 tests):
- test_admin_can_invite_by_email
- test_invitation_has_uuid_token_and_7_day_expiry
- test_collaborator_cannot_invite
- test_admin_can_cancel_pending_invite
- test_cannot_invite_email_already_member

InvitationAcceptTest test names (4 tests):
- test_valid_token_shows_accept_page
- test_expired_token_shows_error
- test_new_user_can_accept_invite_and_joins_org
- test_existing_user_accepts_invite_and_joins_second_org

TeamRoleTest test names (3 tests):
- test_admin_can_change_collaborator_to_admin
- test_owner_cannot_be_demoted
- test_collaborator_cannot_change_roles

TeamRosterTest test names (3 tests):
- test_settings_page_returns_members_with_pivot_data
- test_admin_can_remove_collaborator
- test_owner_cannot_be_removed

EnsureRoleTest test names (3 tests):
- test_collaborator_gets_403_on_invite_route
- test_collaborator_gets_403_on_member_remove_route
- test_admin_can_access_invite_route

RBAC pivot pattern (MANDATORY — every test that checks roles needs this):
```php
$user->organizations()->attach($org->id, ['role' => 'owner', 'joined_at' => now()]);
```
Without this, getCurrentRoleAttribute() returns null and all role assertions fail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run auth + registration test suites and fix any genuine failures</name>
  <files>
    tests/Feature/Auth/AuthenticationTest.php
    tests/Feature/Auth/RegistrationTest.php
    tests/Feature/Auth/PasswordResetTest.php
    tests/Feature/Auth/PasswordUpdateTest.php
    tests/Feature/Auth/EmailVerificationTest.php
    tests/Feature/Auth/PasswordConfirmationTest.php
  </files>
  <read_first>
    - tests/Feature/Auth/AuthenticationTest.php — read the full file to understand current state before any changes
    - tests/Feature/Auth/RegistrationTest.php — read full file; note it uses 'organization_id' in User::factory() (line 21) which may differ from other tests using 'current_organization_id'
    - phpunit.xml — confirm DB_CONNECTION=pgsql and DB_DATABASE=briefy_test are present
    - app/Http/Controllers/Auth/AuthenticatedSessionController.php — read store() method to confirm it sets last_login_at (referenced in test_users_can_authenticate_and_last_login_is_updated)
    - app/Http/Controllers/Auth/RegisteredUserController.php — read store() method to confirm it creates org and attaches pivot (referenced in test_new_users_can_register_and_organization_is_created)
  </read_first>
  <action>
Run the auth test suites in isolation:

```bash
php artisan test --filter=AuthenticationTest --stop-on-failure
php artisan test --filter=RegistrationTest --stop-on-failure
php artisan test tests/Feature/Auth/ --stop-on-failure
```

For each failure, determine if it is:
A) An environment problem (DB not configured, migration not run) — document but do NOT modify tests; note "environment issue, run: createdb briefy_test && php artisan migrate --env=testing"
B) A genuine code bug — fix the bug in the production code (not the test), per D-07.

**Known risk in RegistrationTest:** Line 21 uses `'organization_id' => $org->id` but User factory may expect `'current_organization_id'`. If this field mismatch causes failures, fix the factory call in the test to use `'current_organization_id'` (matching the pattern used in AuthenticationTest).

**Known risk in AuthenticationTest:** Line 21 uses `'organization_id'` instead of `'current_organization_id'`. If User model column is `current_organization_id`, update to `'current_organization_id' => $org->id`.

Do NOT add new tests. Do NOT skip failing tests. Fix root causes only.
  </action>
  <verify>
    <automated>php artisan test --filter=AuthenticationTest && php artisan test --filter=RegistrationTest && php artisan test tests/Feature/Auth/</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=AuthenticationTest` exits 0 with output showing "5 passed"
    - `php artisan test --filter=RegistrationTest` exits 0 with output showing "6 passed"
    - `php artisan test tests/Feature/Auth/` exits 0
    - No test is skipped, marked as incomplete, or asserting against wrong HTTP status
    - If any file was modified, it must still contain all original test method names (verified by grep)
  </acceptance_criteria>
  <done>All auth + registration tests pass with zero failures. php artisan test tests/Feature/Auth/ exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Run organization + team management test suites and fix any genuine failures</name>
  <files>
    tests/Feature/OrganizationCreationTest.php
    tests/Feature/InvitationControllerTest.php
    tests/Feature/InvitationAcceptTest.php
    tests/Feature/TeamRoleTest.php
    tests/Feature/TeamRosterTest.php
    tests/Feature/EnsureRoleTest.php
  </files>
  <read_first>
    - tests/Feature/OrganizationCreationTest.php — full file read; note line 63 asserts assertStatus(422) + assertJsonValidationErrors on POST route — verify this is consistent with how OrganizationController returns validation errors
    - tests/Feature/InvitationControllerTest.php — full file read; note makeAdminUser() helper creates User with 'role' => 'admin' column AND pivot attach — both must exist
    - tests/Feature/InvitationAcceptTest.php — full file read; note test_existing_user_accepts_invite_and_joins_second_org does NOT pass password — confirm InvitationController handles this path for existing users
    - tests/Feature/TeamRoleTest.php — full file read
    - tests/Feature/TeamRosterTest.php — full file read
    - tests/Feature/EnsureRoleTest.php — full file read
    - app/Http/Controllers/OrganizationController.php — read store() method to confirm it returns redirect (not JSON) or 422 JSON for validation
    - app/Http/Controllers/InvitationController.php — read store() method to confirm existing-user path (no password form) redirects to dashboard
  </read_first>
  <action>
Run each org/team test class in isolation:

```bash
php artisan test --filter=OrganizationCreationTest --stop-on-failure
php artisan test --filter=InvitationControllerTest --stop-on-failure
php artisan test --filter=InvitationAcceptTest --stop-on-failure
php artisan test --filter=TeamRoleTest --stop-on-failure
php artisan test --filter=TeamRosterTest --stop-on-failure
php artisan test --filter=EnsureRoleTest --stop-on-failure
```

**Common failure patterns to check:**

1. `OrganizationCreationTest::test_store_rejects_duplicate_slug` asserts `assertStatus(422)` and `assertJsonValidationErrors(['slug'])`. If OrganizationController returns a redirect with session errors (not JSON 422), this test will fail. Fix the test assertion to match controller behavior: if controller returns redirect, use `->assertSessionHasErrors(['slug'])` instead. If controller correctly returns 422 JSON, leave as-is.

2. `OrganizationCreationTest::test_store_requires_name` — same check as above for name validation response format.

3. `InvitationControllerTest::makeAdminUser()` sets `'role' => 'admin'` on the User factory. If the User model no longer has a `role` column directly (only via pivot), remove `'role' => 'admin'` from the factory call — the pivot attach handles it.

4. Any test that uses pivot role checks: confirm the pivot attach call is present with `['role' => '...', 'joined_at' => now()]` format.

For each fix: only change test assertions to match actual controller behavior, or fix production bugs. Never silence a failure by changing the assertion to something trivially true.
  </action>
  <verify>
    <automated>php artisan test --filter=OrganizationCreationTest && php artisan test --filter=InvitationControllerTest && php artisan test --filter=InvitationAcceptTest && php artisan test --filter=TeamRoleTest && php artisan test --filter=TeamRosterTest && php artisan test --filter=EnsureRoleTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=OrganizationCreationTest` exits 0, output shows "4 passed"
    - `php artisan test --filter=InvitationControllerTest` exits 0, output shows "5 passed"
    - `php artisan test --filter=InvitationAcceptTest` exits 0, output shows "4 passed"
    - `php artisan test --filter=TeamRoleTest` exits 0, output shows "3 passed"
    - `php artisan test --filter=TeamRosterTest` exits 0, output shows "3 passed"
    - `php artisan test --filter=EnsureRoleTest` exits 0, output shows "3 passed"
    - Each test file still contains all original test method names (grep verifiable)
    - No test uses assertSkip() or markTestIncomplete()
  </acceptance_criteria>
  <done>All 22 organization and team management tests pass. Every class exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test DB → production DB | RefreshDatabase uses transactions; must never touch briefy (production) DB |
| test actor → controller gate | EnsureRole middleware and authorizeDemand() must block cross-role/cross-org requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | AuthenticationTest | mitigate | Test uses RefreshDatabase — each test isolated in transaction; no cross-test state leakage |
| T-10-02 | Elevation of Privilege | EnsureRoleTest | mitigate | Tests assert 403 on collaborator role — confirms middleware blocks unauthorized escalation |
| T-10-03 | Information Disclosure | OrganizationCreationTest | mitigate | test_store_requires_authentication confirms unauthenticated POST returns /login redirect (not data) |
| T-10-04 | Spoofing | InvitationAcceptTest | mitigate | test_expired_token_shows_error confirms expired invitations are rejected before account creation |
| T-10-05 | Denial of Service | Test environment | accept | Parallel test execution could cause DB lock contention on briefy_test; acceptable — run sequentially with php artisan test |
| T-10-06 | Repudiation | False-positive tests | HIGH — mitigate | Tests that assert assertForbidden() must use actingAs() with properly pivot-attached users; without pivot attach, isAdminOrOwner() returns null and authorization may silently pass or fail wrong |
</threat_model>

<verification>
Full plan verification:

```bash
php artisan test tests/Feature/Auth/ && \
php artisan test --filter=OrganizationCreationTest && \
php artisan test --filter=InvitationControllerTest && \
php artisan test --filter=InvitationAcceptTest && \
php artisan test --filter=TeamRoleTest && \
php artisan test --filter=TeamRosterTest && \
php artisan test --filter=EnsureRoleTest
```

All commands must exit 0.
</verification>

<success_criteria>
1. All auth flow tests (AuthenticationTest + RegistrationTest + Auth/ subdirectory) pass — at least 11 test cases green.
2. All org/team tests (OrganizationCreationTest, InvitationControllerTest, InvitationAcceptTest, TeamRoleTest, TeamRosterTest, EnsureRoleTest) pass — 22 test cases green.
3. Zero skipped or incomplete tests.
4. No test silences a failure with a trivially-true assertion.
</success_criteria>

<output>
After completion, create `.planning/phases/10-automated-test-coverage/10-01-SUMMARY.md` with:
- Which tests ran and passed
- Any fixes applied (file, change, reason)
- Any environment issues that must be resolved before CI (createdb briefy_test, migrate --env=testing)
- Total test count confirmed passing
</output>
