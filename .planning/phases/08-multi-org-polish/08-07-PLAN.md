---
phase: 08-multi-org-polish
plan: 07
type: execute
wave: 4
depends_on:
  - "08-04"
  - "08-05"
  - "08-06"
files_modified: []
autonomous: false
requirements:
  - MORG-01
  - POLISH-01
  - POLISH-02
  - POLISH-03

must_haves:
  truths:
    - "All 4 OrganizationCreationTest tests pass (GREEN)"
    - "`npx tsc --noEmit` exits 0"
    - "No direct `new EventSource(` usage in any component file"
    - "OrgSwitcher button is clickable and opens CreateOrgModal"
    - "ChatTab conversation picker is functional and read-only for old convs"
  artifacts:
    - path: "tests/Feature/OrganizationCreationTest.php"
      provides: "GREEN — all 4 tests pass"
      contains: "test_store_creates_org_and_attaches_owner"
    - path: "resources/js/hooks/useAiStream.ts"
      provides: "Unified SSE hook with GET+EventSource branch"
      contains: "onEvent"
  key_links:
    - from: "Phase 8 success criteria"
      to: "ROADMAP.md Phase 8 success criteria items 1-4"
      via: "All 4 requirements delivered and verified"
      pattern: "MORG-01|POLISH-01|POLISH-02|POLISH-03"
---

<objective>
Wave 4 verification: run all automated checks, then hand off to human for functional UAT of the three UI features (org creation, SSE research timeline, conversation picker).

Purpose: Confirm all Phase 8 deliverables are complete before marking the phase done.

Output: Signed-off verification report in SUMMARY.md.
</objective>

<execution_context>
@D:/projetos/briefy/.planning/phases/08-multi-org-polish/08-VALIDATION.md
</execution_context>

<context>
@D:/projetos/briefy/.planning/PROJECT.md
@D:/projetos/briefy/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification suite</name>
  <files></files>
  <read_first>
    - .planning/phases/08-multi-org-polish/08-04-SUMMARY.md (MORG-01 frontend status)
    - .planning/phases/08-multi-org-polish/08-05-SUMMARY.md (POLISH-01 SSE status)
    - .planning/phases/08-multi-org-polish/08-06-SUMMARY.md (POLISH-03 picker status)
  </read_first>
  <action>
Run each check and record the result:

**Check 1: MORG-01 backend tests**
```bash
php artisan test --filter OrganizationCreationTest
```
Expected: 4/4 passing

**Check 2: Full test suite (no regressions)**
```bash
php artisan test
```
Expected: all tests pass (no regressions from Phase 8 changes)

**Check 3: TypeScript zero errors (POLISH-02)**
```bash
npx tsc --noEmit
```
Expected: exit 0, no output (or explicit "Found 0 errors")

**Check 4: No direct EventSource in components (POLISH-01)**
```bash
grep -rn "new EventSource" resources/js/Components/ resources/js/pages/ resources/js/hooks/useAiStream.ts
```
Expected: only one match — in useAiStream.ts GET branch. Zero matches in Components/ or pages/.

**Check 5: Route registered**
```bash
php artisan route:list --name=organizations.store
```
Expected: shows POST /organizations

**Check 6: OrgSwitcher button no longer disabled**
```bash
grep -n "cursor-not-allowed\|title=\"Em breve\"\|disabled" resources/js/Layouts/AppLayout.tsx | grep -i "criar"
```
Expected: no match (disabled stub replaced)

**Check 7: ChatTab uses selectedConvId (POLISH-03)**
```bash
grep -n "selectedConvId\|isLatest" resources/js/Components/ChatTab.tsx | head -10
```
Expected: multiple matches

**Check 8: index.d.ts has expanded types (POLISH-02 root fix)**
```bash
grep -n "role: string | null\|archive_count\|organizations: Array" resources/js/types/index.d.ts
```
Expected: all three found

If any check fails, fix the issue before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd D:/projetos/briefy && php artisan test --filter OrganizationCreationTest && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `php artisan test --filter OrganizationCreationTest` shows 4/4 PASS
    - `npx tsc --noEmit` exits 0
    - `grep -rn "new EventSource" resources/js/Components/` returns no matches
    - `grep -n "organizations.store" routes/web.php` returns a match
    - `grep -n "selectedConvId" resources/js/Components/ChatTab.tsx` returns multiple matches
    - `grep -n "role: string | null" resources/js/types/index.d.ts` returns a match
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 8 delivers four independent improvements:

1. **MORG-01 — Multi-org creation:** A '+ Criar nova organização' button now appears at the bottom of the OrgSwitcher dropdown (previously disabled). Clicking it opens a modal with Nome (required) and Slug (auto-generated) fields. On success, the user is switched to the new org and redirected to /dashboard.

2. **POLISH-01 — SSE consolidation:** The `useAiStream` hook now handles both POST delta-frame streams (AI chat/brief — unchanged) and GET custom-event streams (research timeline). `ClientResearchTimelineModal` no longer has a direct EventSource — it uses `useAiStream({ method: 'GET', ... })`.

3. **POLISH-02 — TypeScript zero errors:** All 15 pre-existing TypeScript errors are fixed. Global types in `index.d.ts` now match the real server payload. `npx tsc --noEmit` exits 0.

4. **POLISH-03 — Conversation picker:** The ChatTab header now has a dropdown to select previous AI conversations. Older conversations are displayed read-only (textarea + send button disabled). The + Nova conversa button still works. Inertia partial reloads do not reset a manually-selected older conversation.
  </what-built>
  <how-to-verify>
**Test 1 — MORG-01 (Multi-org creation):**
1. Log in to the app
2. Click your avatar/org name in the top-left to open the OrgSwitcher
3. Verify '+ Criar nova organização' button is visible and NOT grayed out/disabled
4. Click the button — verify the dropdown closes and a modal opens
5. Type a name (e.g., "Agência Teste 2") — verify the Slug field auto-fills as "agencia-teste-2"
6. Edit the slug manually to "agencia-teste-3" — verify it stays as typed
7. Click 'Criar' — verify: modal closes, you are redirected to /dashboard, the new org appears in the OrgSwitcher when you open it again
8. Bonus: try creating an org with a slug that already exists (e.g., the slug of your current org) — verify a red error appears under the Slug field

**Test 2 — POLISH-01 (SSE research timeline — if you have client research running):**
1. Open any client that has a research session in progress or can start one
2. Start a client research session
3. Open the research timeline modal
4. Verify events appear streaming in real-time without any JavaScript console errors mentioning EventSource
5. Open browser DevTools → Console — confirm no errors like "EventSource used directly" or "ARCH NOTE" references

**Test 3 — POLISH-03 (Conversation picker):**
1. Open a demand that has 2 or more AI conversations in the Chat tab
   - If no demand has multiple convs, open one, chat briefly, create a new conv via '+ Nova conversa', and chat again
2. Open the Chat tab — verify a dropdown button appears in the header showing the current conversation label (date + message count, or title if set)
3. Click the dropdown — verify a list of conversations appears with dates and message counts
4. Select an older conversation — verify:
   - Chat history shows the messages from that older conversation
   - The text input and send button are disabled/grayed out
   - A visual indication that this is read-only is shown
5. Click '+ Nova conversa' — verify picker resets to the latest conversation and input is enabled again
6. Send a message in the latest conversation — verify the picker does NOT jump to an older conv after the page updates

**Test 4 — TypeScript (for developers):**
```bash
npx tsc --noEmit
```
Verify: exits 0 (no errors)
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 8 complete, or describe any issues found during testing</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human UAT | Manual verification of browser UI behaviors that automated checks cannot cover |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Information Disclosure | Phase 8 combined delivery | accept | All features are scoped to authenticated, verified users in their organization context; no cross-org data exposure introduced |
| T-08-07-02 | Tampering | Phase regression via test suite | mitigate | `php artisan test` full suite run in Task 1 catches any regressions before human UAT |
</threat_model>

<verification>
All automated checks from Task 1 must pass before the human checkpoint proceeds.
Human checkpoint sign-off ("approved") completes the phase.
</verification>

<success_criteria>
**Automated (Task 1 must be green before proceeding to checkpoint):**
- `php artisan test --filter OrganizationCreationTest` → 4/4 PASS
- `php artisan test` → full suite passes (no regressions)
- `npx tsc --noEmit` → exit 0
- `grep -rn "new EventSource" resources/js/Components/` → 0 matches
- Route `organizations.store` registered and listed

**Human (UAT checkpoint):**
- MORG-01: User can create new org from OrgSwitcher, new org appears in switcher
- POLISH-01: Research timeline modal streams events without console errors
- POLISH-03: Conversation picker renders, switches conv, enforces read-only on old convs
- POLISH-02: Developer confirms `npx tsc --noEmit` exits 0

**Phase 8 is complete when human types "approved".**
</success_criteria>

<output>
After human approval, create `.planning/phases/08-multi-org-polish/08-07-SUMMARY.md` documenting:
- Results of all 8 automated checks from Task 1
- Human UAT sign-off confirmation
- Any issues found during UAT and how they were resolved
- Final state of all 4 requirements: MORG-01, POLISH-01, POLISH-02, POLISH-03
</output>
