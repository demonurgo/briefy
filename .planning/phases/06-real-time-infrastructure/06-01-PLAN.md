---
phase: 06-real-time-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Events/DemandCommentCreated.php
  - app/Http/Controllers/DemandController.php
  - tests/Feature/Broadcasting/DemandCommentCreatedTest.php
  - tests/Unit/Events/DemandBoardUpdatedTest.php
autonomous: true
requirements:
  - RT-02

must_haves:
  truths:
    - "Quando User A posta um comentário, o evento DemandCommentCreated é disparado com payload correto"
    - "O evento transmite no canal privado organization.{orgId}, não num canal público"
    - "O payload contém organizationId, demandId, e comment com id/body/user/created_at como string ISO"
    - "O método addComment() carrega a relação user antes de montar o payload"
    - "Os arquivos de teste existem e passam antes da implementação frontend (Wave 2)"
  artifacts:
    - path: "app/Events/DemandCommentCreated.php"
      provides: "Evento broadcast síncrono para novo comentário"
      exports: ["DemandCommentCreated"]
    - path: "app/Http/Controllers/DemandController.php"
      provides: "Dispatch do evento após persistir comentário"
      contains: "DemandCommentCreated::dispatch"
    - path: "tests/Feature/Broadcasting/DemandCommentCreatedTest.php"
      provides: "Testes de integração do dispatch e payload"
      contains: "Event::assertDispatched"
    - path: "tests/Unit/Events/DemandBoardUpdatedTest.php"
      provides: "Testes unitários do evento RT-01"
      contains: "broadcastAs"
  key_links:
    - from: "app/Http/Controllers/DemandController.php"
      to: "app/Events/DemandCommentCreated.php"
      via: "DemandCommentCreated::dispatch()"
      pattern: "DemandCommentCreated::dispatch"
    - from: "app/Events/DemandCommentCreated.php"
      to: "private-organization.{orgId}"
      via: "broadcastOn() -> PrivateChannel"
      pattern: "PrivateChannel.*organization"
---

<objective>
Criar o backend completo de RT-02: evento `DemandCommentCreated` + dispatch em `DemandController::addComment()`, junto com os stubs de teste (Wave 0) que a Wave 2 depende para validação.

Purpose: Sem o evento backend, a subscription React em `DemandDetailModal.tsx` (Wave 2) não recebe nada. Os testes criados aqui bloqueiam regressões e provam que o payload está correto antes da integração frontend.

Output: `DemandCommentCreated.php` (evento), `DemandController.php` modificado com dispatch, dois arquivos de teste passando.
</objective>

<execution_context>
@D:/projetos/briefy/.planning/phases/06-real-time-infrastructure/06-CONTEXT.md
@D:/projetos/briefy/.planning/phases/06-real-time-infrastructure/06-PATTERNS.md
</execution_context>

<context>
@D:/projetos/briefy/.planning/ROADMAP.md
@D:/projetos/briefy/.planning/phases/06-real-time-infrastructure/06-RESEARCH.md

<interfaces>
<!-- Tipos e contratos extraídos do codebase. Executor NÃO precisa explorar os arquivos. -->

De app/Events/DemandBoardUpdated.php (analog exato — copiar estrutura):
```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Events;

use Illuminate\Broadcasting\Channel;
use Illuminate\Broadcasting\InteractsWithSockets;
use Illuminate\Broadcasting\PrivateChannel;
use Illuminate\Contracts\Broadcasting\ShouldBroadcastNow;
use Illuminate\Foundation\Events\Dispatchable;

class DemandBoardUpdated implements ShouldBroadcastNow
{
    use Dispatchable, InteractsWithSockets;

    public function __construct(
        public readonly int $organizationId,
        public readonly string $action,
    ) {}

    public function broadcastOn(): Channel
    {
        return new PrivateChannel("organization.{$this->organizationId}");
    }

    public function broadcastAs(): string
    {
        return 'demand.board.updated';
    }
}
```

De app/Http/Controllers/DemandController.php — método addComment() ATUAL (linhas 193-206):
```php
public function addComment(Request $request, Demand $demand): RedirectResponse
{
    $this->authorizeDemand($demand);
    $request->validate(['body' => 'required|string|max:5000']);

    DemandComment::create([
        'demand_id' => $demand->id,
        'user_id'   => auth()->id(),
        'body'      => $request->body,
        'source'    => 'user',
    ]);

    return back()->with('success', __('app.comment_added'));
}
```

De tests/Feature/ActivityLogTest.php — padrão setUp (linhas 19-25):
```php
protected function setUp(): void
{
    parent::setUp();
    $this->org = Organization::factory()->create();
    $this->admin = User::factory()->create(['current_organization_id' => $this->org->id]);
    $this->org->users()->attach($this->admin->id, ['role' => 'admin']);
}
```

De routes/channels.php — autorização existente do canal (já cobre RT-01 e RT-02):
```php
Broadcast::channel('organization.{orgId}', function ($user, $orgId) {
    return (int) $user->current_organization_id === (int) $orgId;
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar DemandCommentCreated.php e stubs de teste (Wave 0)</name>
  <files>
    app/Events/DemandCommentCreated.php,
    tests/Feature/Broadcasting/DemandCommentCreatedTest.php,
    tests/Unit/Events/DemandBoardUpdatedTest.php
  </files>

  <read_first>
    - app/Events/DemandBoardUpdated.php — analog exato; copiar estrutura completa
    - tests/Feature/ActivityLogTest.php — padrão setUp, namespace, imports
    - tests/Unit/Models/DemandModelTest.php — padrão de teste unitário
    - phpunit.xml — confirmar BROADCAST_CONNECTION=null para testes
  </read_first>

  <behavior>
    DemandCommentCreated:
    - Instanciar com (int $organizationId, int $demandId, array $comment) retorna evento válido
    - broadcastOn() retorna PrivateChannel com nome "organization.{organizationId}"
    - broadcastAs() retorna string 'demand.comment.created' (sem namespace, sem ponto)
    - Implementa ShouldBroadcastNow (não ShouldBroadcast — sync, sem fila)

    DemandBoardUpdatedTest:
    - broadcastOn() com orgId=5 retorna canal com nome "organization.5"
    - broadcastAs() retorna 'demand.board.updated'
    - instanceof ShouldBroadcastNow é verdadeiro

    DemandCommentCreatedTest (feature):
    - POST /demands/{demand}/comments com body válido → evento DemandCommentCreated é dispatched
    - Payload do evento: demandId === demand->id, organizationId === org->id, comment.body === 'Comentário teste'
    - Payload do evento: comment.id existe, comment.user.id existe, comment.user.name existe, comment.created_at é string
    - broadcastOn() do evento retorna PrivateChannel com nome "organization.{org->id}"
    - broadcastAs() do evento retorna 'demand.comment.created'
  </behavior>

  <action>
**1. Criar app/Events/DemandCommentCreated.php**

Copiar EXATAMENTE a estrutura de `DemandBoardUpdated.php`, trocando:
- Nome da classe: `DemandCommentCreated`
- Propriedades do construtor:
  ```php
  public readonly int   $organizationId,
  public readonly int   $demandId,
  public readonly array $comment,
  ```
- `broadcastAs()` retorna: `'demand.comment.created'`
- `broadcastOn()` permanece idêntico: `new PrivateChannel("organization.{$this->organizationId}")`
- NÃO definir `broadcastWith()` — Laravel serializa propriedades public automaticamente (mesma convenção do analog)
- Manter cabeçalho `// (c) 2026 Briefy contributors — AGPL-3.0`

**2. Criar tests/Unit/Events/DemandBoardUpdatedTest.php**

Criar o diretório `tests/Unit/Events/` se não existir.

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace Tests\Unit\Events;

use App\Events\DemandBoardUpdated;
use Illuminate\Broadcasting\PrivateChannel;
use Illuminate\Contracts\Broadcasting\ShouldBroadcastNow;
use Tests\TestCase;

class DemandBoardUpdatedTest extends TestCase
{
    public function test_broadcast_on_returns_private_channel(): void
    {
        $event = new DemandBoardUpdated(organizationId: 5, action: 'updated');
        $channel = $event->broadcastOn();

        $this->assertInstanceOf(PrivateChannel::class, $channel);
        $this->assertSame('private-organization.5', $channel->name);
    }

    public function test_broadcast_as_returns_correct_name(): void
    {
        $event = new DemandBoardUpdated(organizationId: 1, action: 'created');

        $this->assertSame('demand.board.updated', $event->broadcastAs());
    }

    public function test_implements_should_broadcast_now(): void
    {
        $event = new DemandBoardUpdated(organizationId: 1, action: 'deleted');

        $this->assertInstanceOf(ShouldBroadcastNow::class, $event);
    }
}
```

**3. Criar tests/Feature/Broadcasting/DemandCommentCreatedTest.php**

Criar o diretório `tests/Feature/Broadcasting/` se não existir.

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace Tests\Feature\Broadcasting;

use App\Events\DemandCommentCreated;
use App\Models\Client;
use App\Models\Demand;
use App\Models\Organization;
use App\Models\User;
use Illuminate\Broadcasting\PrivateChannel;
use Illuminate\Contracts\Broadcasting\ShouldBroadcastNow;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Event;
use Tests\TestCase;

class DemandCommentCreatedTest extends TestCase
{
    use RefreshDatabase;

    private Organization $org;
    private User $admin;
    private Client $client;
    private Demand $demand;

    protected function setUp(): void
    {
        parent::setUp();
        $this->org = Organization::factory()->create();
        $this->admin = User::factory()->create(['current_organization_id' => $this->org->id]);
        $this->org->users()->attach($this->admin->id, ['role' => 'admin']);
        $this->client = Client::factory()->create(['organization_id' => $this->org->id]);
        $this->demand = Demand::factory()->create([
            'organization_id' => $this->org->id,
            'client_id'       => $this->client->id,
            'created_by'      => $this->admin->id,
        ]);
    }

    public function test_dispatch_after_add_comment(): void
    {
        Event::fake([DemandCommentCreated::class]);

        $this->actingAs($this->admin)
            ->post(route('demands.comments.store', $this->demand), ['body' => 'Comentário teste'])
            ->assertRedirect();

        Event::assertDispatched(DemandCommentCreated::class, function ($event) {
            return $event->demandId === $this->demand->id
                && $event->organizationId === $this->org->id
                && $event->comment['body'] === 'Comentário teste'
                && isset($event->comment['id'])
                && isset($event->comment['user']['id'])
                && isset($event->comment['user']['name'])
                && is_string($event->comment['created_at']);
        });
    }

    public function test_broadcast_channel_is_private_organization(): void
    {
        $event = new DemandCommentCreated(
            organizationId: 42,
            demandId: 99,
            comment: ['id' => 1, 'body' => 'test', 'user' => ['id' => 1, 'name' => 'Test'], 'created_at' => now()->toJSON()]
        );

        $channel = $event->broadcastOn();

        $this->assertInstanceOf(PrivateChannel::class, $channel);
        $this->assertSame('private-organization.42', $channel->name);
    }

    public function test_broadcast_as_returns_correct_event_name(): void
    {
        $event = new DemandCommentCreated(
            organizationId: 1,
            demandId: 1,
            comment: ['id' => 1, 'body' => 'test', 'user' => ['id' => 1, 'name' => 'Test'], 'created_at' => now()->toJSON()]
        );

        $this->assertSame('demand.comment.created', $event->broadcastAs());
    }

    public function test_implements_should_broadcast_now(): void
    {
        $event = new DemandCommentCreated(1, 1, ['id' => 1, 'body' => 'test', 'user' => ['id' => 1, 'name' => 'Test'], 'created_at' => now()->toJSON()]);

        $this->assertInstanceOf(ShouldBroadcastNow::class, $event);
    }
}
```

**Nota sobre o nome do canal:** Laravel Echo preficia automaticamente canais privados com `private-` internamente. A propriedade `$channel->name` em `PrivateChannel` **não** contém o prefixo `private-` — o nome armazenado é `"organization.{orgId}"`. Ajuste o assert para `$this->assertSame('organization.42', $channel->name)` se o teste falhar por causa do prefixo. Verifique lendo a classe `PrivateChannel` ou executando o teste e observando o valor real.
  </action>

  <verify>
    <automated>php artisan test --filter "DemandBoardUpdatedTest|DemandCommentCreatedTest" 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - app/Events/DemandCommentCreated.php existe e contém `implements ShouldBroadcastNow`
    - app/Events/DemandCommentCreated.php contém `broadcastAs(): string` retornando `'demand.comment.created'`
    - app/Events/DemandCommentCreated.php contém `public readonly int $demandId`
    - app/Events/DemandCommentCreated.php contém `public readonly array $comment`
    - tests/Unit/Events/DemandBoardUpdatedTest.php existe
    - tests/Feature/Broadcasting/DemandCommentCreatedTest.php existe
    - `php artisan test --filter "DemandBoardUpdatedTest|DemandCommentCreatedTest"` sai com código 0 (todos os testes passam)
    - Saída dos testes mostra ao menos 5 testes passando (3 feature + 2 unit no mínimo)
  </acceptance_criteria>

  <done>Evento DemandCommentCreated criado, dois arquivos de teste existem e passam. O teste de dispatch vai falhar até que a Task 2 modifique DemandController — isso é esperado (TDD RED → GREEN).</done>
</task>

<task type="auto">
  <name>Task 2: Dispatch DemandCommentCreated em DemandController::addComment()</name>
  <files>app/Http/Controllers/DemandController.php</files>

  <read_first>
    - app/Http/Controllers/DemandController.php — ler método addComment() completo (linhas ~193-206); ler imports existentes no topo do arquivo
    - app/Events/DemandCommentCreated.php — confirmar assinatura do construtor antes de chamar dispatch()
  </read_first>

  <action>
**1. Adicionar import no topo de DemandController.php**

Após os imports existentes, adicionar:
```php
use App\Events\DemandCommentCreated;
```

**2. Modificar o método addComment()**

Substituir o método atual por:
```php
public function addComment(Request $request, Demand $demand): RedirectResponse
{
    $this->authorizeDemand($demand);
    $request->validate(['body' => 'required|string|max:5000']);

    $comment = DemandComment::create([
        'demand_id' => $demand->id,
        'user_id'   => auth()->id(),
        'body'      => $request->body,
        'source'    => 'user',
    ]);

    // Carregar relação user ANTES do dispatch — create() não carrega relações
    $comment->load('user');

    DemandCommentCreated::dispatch(
        $demand->organization_id,
        $demand->id,
        [
            'id'         => $comment->id,
            'body'       => $comment->body,
            'user'       => [
                'id'   => $comment->user->id,
                'name' => $comment->user->name,
            ],
            'created_at' => $comment->created_at->toJSON(), // Carbon->toJSON() = string ISO 8601
        ]
    );

    return back()->with('success', __('app.comment_added'));
}
```

**Pitfalls críticos a evitar:**
- NÃO usar `$comment->created_at` diretamente (objeto Carbon) — sempre `->toJSON()`
- NÃO omitir `$comment->load('user')` — create() retorna model sem relações carregadas
- NÃO chamar `dispatch()` antes do `load('user')` — `$comment->user` seria null
  </action>

  <verify>
    <automated>php artisan test --filter "DemandCommentCreatedTest" 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - app/Http/Controllers/DemandController.php contém `use App\Events\DemandCommentCreated;` no bloco de imports
    - app/Http/Controllers/DemandController.php contém `$comment->load('user');`
    - app/Http/Controllers/DemandController.php contém `DemandCommentCreated::dispatch(`
    - app/Http/Controllers/DemandController.php contém `$comment->created_at->toJSON()`
    - `php artisan test --filter "DemandCommentCreatedTest"` sai com código 0
    - Saída dos testes inclui "test_dispatch_after_add_comment" como PASSED
    - `php artisan test` sai com código 0 (suite completa — sem regressões)
  </acceptance_criteria>

  <done>DemandController::addComment() dispara DemandCommentCreated com payload correto. Todos os testes passam. Backend RT-02 completo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| client→API | Dados do comentário chegam via POST /demands/{demand}/comments — input não confiável |
| API→Reverb | Payload do broadcast construído no servidor — confiável após validação |
| Reverb→browser | Evento entregue a todos os assinantes autorizados do canal `private-organization.{orgId}` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | DemandController::addComment() | mitigate | `$this->authorizeDemand($demand)` já valida que o usuário pertence à organização da demanda antes de criar o comentário |
| T-06-02 | Information Disclosure | private-organization.{orgId} channel | mitigate | `routes/channels.php` já autentica o canal via `$user->current_organization_id === (int) $orgId` — não-membros da org não recebem o broadcast |
| T-06-03 | Tampering | Payload do evento (comment array) | accept | Payload construído 100% no servidor após validação Eloquent; client não influencia o conteúdo do broadcast, apenas o `body` do comentário (já validado com `max:5000`) |
| T-06-04 | Elevation of Privilege | Canal privado via WebSocket | mitigate | Laravel Echo usa token de sessão + `/broadcasting/auth` endpoint para autorizar assinaturas — sem sessão válida o canal não é acessível |
</threat_model>

<verification>
Verificação geral do plano:

```bash
php artisan test --filter "DemandBoardUpdatedTest|DemandCommentCreatedTest"
```

Deve retornar 0 (todos os testes passando), com ao menos:
- DemandBoardUpdatedTest: 3 testes (broadcastOn, broadcastAs, ShouldBroadcastNow)
- DemandCommentCreatedTest: 4 testes (dispatch, channel, broadcastAs, ShouldBroadcastNow)

```bash
php artisan test
```

Suite completa deve continuar verde (sem regressões).
</verification>

<success_criteria>
- `app/Events/DemandCommentCreated.php` existe com `ShouldBroadcastNow`, `broadcastAs()` retornando `'demand.comment.created'`, canal `PrivateChannel("organization.{$this->organizationId}")`
- `app/Http/Controllers/DemandController.php::addComment()` contém `$comment->load('user')` + `DemandCommentCreated::dispatch(...)` com payload `{id, body, user: {id, name}, created_at: ->toJSON()}`
- `php artisan test --filter "DemandCommentCreatedTest"` → exit 0, test_dispatch_after_add_comment PASSED
- `php artisan test` → exit 0 (sem regressões na suite)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-real-time-infrastructure/06-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Estrutura do evento DemandCommentCreated
- Resultado dos testes
- Qualquer divergência encontrada (ex: nome do canal com/sem prefixo 'private-')
</output>
