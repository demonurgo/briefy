---
phase: 06-real-time-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/pages/Demands/Index.tsx
autonomous: true
requirements:
  - RT-01

must_haves:
  truths:
    - "Quando User B muda status de uma demanda, o Kanban de User A reflete a mudança sem refresh"
    - "O broadcast chega via canal private-organization.{orgId} — apenas membros da org"
    - "Múltiplos broadcasts rápidos (rafada de drag) disparam um único router.reload graças ao debounce de 300ms"
    - "A subscription RT-01 já existe e funciona em Index.tsx — o trabalho é apenas reforçar com debounce"
  artifacts:
    - path: "resources/js/pages/Demands/Index.tsx"
      provides: "Subscription Echo com debounce para demand.board.updated"
      contains: "reloadTimeout"
  key_links:
    - from: "resources/js/pages/Demands/Index.tsx"
      to: "router.reload({ only: ['demands'] })"
      via: "channel.listen('.demand.board.updated', callback)"
      pattern: "router\\.reload.*only.*demands"
---

<objective>
Reforçar a subscription RT-01 já existente em `Index.tsx` com um debounce de 300ms, prevenindo que uma rafada de broadcasts (ex: drag rápido) dispare múltiplos `router.reload` simultâneos.

Purpose: A subscription já está funcional (linhas 62–77 verificadas). O único risco operacional identificado na pesquisa é o "jank visual" ao fazer drag enquanto um broadcast chega — o reload reseta o estado otimista do Kanban no meio do gesto. O debounce de 300ms agrupa a rafada sem precisar de prop-drilling do estado de drag.

Output: `Index.tsx` com debounce aplicado ao callback do `.demand.board.updated`.
</objective>

<execution_context>
@D:/projetos/briefy/.planning/phases/06-real-time-infrastructure/06-CONTEXT.md
@D:/projetos/briefy/.planning/phases/06-real-time-infrastructure/06-PATTERNS.md
</execution_context>

<context>
@D:/projetos/briefy/.planning/ROADMAP.md

<interfaces>
<!-- Código atual verificado no codebase — RT-01 já funcional -->

De resources/js/pages/Demands/Index.tsx linhas 62-77 (ATUAL — não reescrever, apenas modificar o callback):
```typescript
const { auth } = usePage<PageProps>().props;
const orgId = (auth?.user as { current_organization_id?: number } | undefined)?.current_organization_id;

useEffect(() => {
  if (!orgId || !window.Echo) return;

  const channel = window.Echo.private(`organization.${orgId}`);
  channel.listen('.demand.board.updated', () => {
    router.reload({ only: ['demands'] });
  });

  return () => {
    window.Echo.leave(`organization.${orgId}`);
  };
}, [orgId]);
```

Nota: Este plano NÃO move a subscription para KanbanBoard.tsx (divergência com D-03 do CONTEXT.md).
D-03 diz que a subscription deveria estar em KanbanBoard.tsx, mas o código já está em Index.tsx e
é o lugar correto — router.reload opera no nível de página. Registrar esta divergência no SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar debounce de 300ms ao callback RT-01 em Index.tsx</name>
  <files>resources/js/pages/Demands/Index.tsx</files>

  <read_first>
    - resources/js/pages/Demands/Index.tsx — ler o arquivo completo para ver imports existentes, estrutura do componente, posição do useEffect RT-01, e verificar se useRef já está importado
  </read_first>

  <action>
**1. Verificar imports existentes**

Ler o topo de `Index.tsx` para ver o import de React hooks (`useEffect`, `useRef`, `useState`, etc.). Se `useRef` já está importado, não adicionar novamente.

Se `useRef` NÃO está importado, adicionar ao import existente:
```typescript
import { useEffect, useRef } from 'react';
// ou na linha de import já existente, adicionar useRef
```

**2. Adicionar o ref do timeout DENTRO do componente (antes do useEffect RT-01)**

Localizar a linha que declara `orgId`:
```typescript
const orgId = (auth?.user as { current_organization_id?: number } | undefined)?.current_organization_id;
```

Adicionar imediatamente após:
```typescript
const reloadTimeout = useRef<ReturnType<typeof setTimeout> | null>(null);
```

**3. Modificar o useEffect RT-01 (linhas ~62-77)**

Substituir APENAS o callback do `channel.listen()`, mantendo tudo o mais intocado:

ANTES:
```typescript
channel.listen('.demand.board.updated', () => {
  router.reload({ only: ['demands'] });
});
```

DEPOIS:
```typescript
channel.listen('.demand.board.updated', () => {
  if (reloadTimeout.current) clearTimeout(reloadTimeout.current);
  reloadTimeout.current = setTimeout(() => {
    router.reload({ only: ['demands'] });
  }, 300);
});
```

**4. Manter o cleanup existente inalterado**

O `return () => { window.Echo.leave(...) }` NÃO deve ser alterado. Index.tsx é o único subscriber do canal neste contexto (DemandDetailModal usa stopListening, não leave).

**Divergência D-03 registrada:** CONTEXT.md D-03 especifica que a subscription deve viver em `KanbanBoard.tsx`. A pesquisa confirmou que ela já existe e funciona em `Index.tsx`. A localização em `Index.tsx` é correta porque `router.reload` opera em nível de página. Manter em `Index.tsx`, registrar no SUMMARY.
  </action>

  <verify>
    <automated>cd D:/projetos/briefy && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - resources/js/pages/Demands/Index.tsx contém `reloadTimeout`
    - resources/js/pages/Demands/Index.tsx contém `clearTimeout(reloadTimeout.current)`
    - resources/js/pages/Demands/Index.tsx contém `setTimeout(() => {` seguido de `router.reload`
    - resources/js/pages/Demands/Index.tsx ainda contém `window.Echo.leave` no cleanup (não removido)
    - resources/js/pages/Demands/Index.tsx ainda contém `channel.listen('.demand.board.updated'` com ponto inicial
    - `npx tsc --noEmit` não reporta erros em Index.tsx (zero erros TypeScript no arquivo)
  </acceptance_criteria>

  <done>Index.tsx tem debounce de 300ms no callback RT-01. Subscription existente intacta. Zero erros TypeScript.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Reverb→browser | Evento `demand.board.updated` entregue via WebSocket ao cliente autenticado |
| browser→API | `router.reload({ only: ['demands'] })` dispara GET com cookies de sessão — requere autenticação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information Disclosure | Subscription `private-organization.{orgId}` em Index.tsx | mitigate | Canal privado autorizado via `routes/channels.php` — apenas `current_organization_id === orgId` garante acesso; não-membros não recebem o broadcast |
| T-06-06 | Denial of Service | Rafada de broadcasts (drag rápido) | mitigate | Debounce de 300ms agrupa múltiplos eventos em um único router.reload — previne request stampede |
| T-06-07 | Information Disclosure | `router.reload({ only: ['demands'] })` retorna dados filtrados por org | accept | DemandController já filtra demands por `current_organization_id`; o reload não expõe dados de outras orgs |
</threat_model>

<verification>
Verificação manual (não automatizável sem browser real):

1. Abrir dois browsers com usuários diferentes da mesma organização na página `/demands`
2. User A arrasta um card para nova coluna
3. Verificar que User B vê o card na nova coluna sem precisar de refresh
4. DevTools → Network → WS confirma evento `demand.board.updated` chegando

Verificação automatizada:
```bash
npx tsc --noEmit
```
Zero erros TypeScript.

Verificação de conteúdo:
```bash
grep -n "reloadTimeout\|clearTimeout\|setTimeout" resources/js/pages/Demands/Index.tsx
```
Deve mostrar as 3 linhas do debounce.
</verification>

<success_criteria>
- `Index.tsx` contém debounce de 300ms com `useRef` + `clearTimeout` + `setTimeout`
- Subscription `.demand.board.updated` permanece intacta com ponto inicial no nome do evento
- `window.Echo.leave()` permanece no cleanup (não substituído por stopListening)
- Zero erros TypeScript em Index.tsx (`npx tsc --noEmit`)
- SUMMARY documenta divergência D-03 (subscription em Index.tsx vs KanbanBoard.tsx)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-real-time-infrastructure/06-02-SUMMARY.md` com:
- Mudança aplicada (debounce 300ms)
- Divergência D-03 documentada (subscription em Index.tsx, não KanbanBoard.tsx)
- Resultado da verificação TypeScript
</output>
