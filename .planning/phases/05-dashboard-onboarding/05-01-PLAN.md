---
phase: 05-dashboard-onboarding
plan: "01"
type: execute
wave: 1
depends_on:
  - "05-00"
files_modified:
  - app/Http/Controllers/DashboardController.php
  - app/Models/ActivityLog.php
  - routes/web.php
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - ONBRD-01
  - ONBRD-02

must_haves:
  truths:
    - "GET /dashboard retorna prop 'personal' com statusCounts, focusDemands, myDemands, blockers, weekProgress, completedByDay para qualquer usuário autenticado"
    - "GET /dashboard retorna prop 'overview' (não null) apenas para admin/owner; colaborador recebe overview: null"
    - "overview contém statusBreakdown, priorityBreakdown, demandsOverTime, latestDemands, teamWorkload, clientDistribution, teamPerformance"
    - "activityFeed scoped: colaborador vê apenas eventos de demandas atribuídas/criadas por ele; admin vê todos"
    - "PATCH /settings/preferences com {onboarding_dismissed: true} persiste o campo (BLOCKER-02 resolvido)"
  artifacts:
    - path: "app/Http/Controllers/DashboardController.php"
      provides: "Controller completo com queries personal + overview + activityFeed + onboarding"
      min_lines: 120
    - path: "app/Models/ActivityLog.php"
      provides: "Model ActivityLog com fillable, casts, belongsTo"
    - path: "routes/web.php"
      provides: "preferences route com 'onboarding_dismissed' no only()"
  key_links:
    - from: "app/Http/Controllers/DashboardController.php"
      to: "app/Models/ActivityLog.php"
      via: "ActivityLog::where(organization_id)"
      pattern: "ActivityLog::where"
    - from: "routes/web.php"
      to: "app/Models/User.php"
      via: "preferences JSON merge"
      pattern: "onboarding_dismissed"
    - from: "app/Http/Controllers/DashboardController.php"
      to: "app/Models/Demand.php"
      via: "Demand::where(organization_id)->whereNull(archived_at)"
      pattern: "whereNull.*archived_at"
---

<objective>
Wave 1 (paralela com 05-02) — Backend completo do dashboard.

Purpose: Expande o DashboardController stub em um controller real que agrega todos os dados necessários para ambas as views (personal + overview). Cria o modelo ActivityLog. Corrige o BLOCKER-02 (preferences route).

Output:
- DashboardController.php com método index() completo (~150 linhas)
- ActivityLog.php model
- routes/web.php corrigido para aceitar onboarding_dismissed
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-onboarding/05-CONTEXT.md
@.planning/phases/05-dashboard-onboarding/05-RESEARCH.md
@.planning/phases/05-dashboard-onboarding/05-PATTERNS.md

<interfaces>
<!-- DashboardController atual (stub a ser substituído) -->
```php
// app/Http/Controllers/DashboardController.php — estado atual
public function index(Request $request): Response
{
    $orgId = auth()->user()->current_organization_id;
    $planningReminderClients = Client::where('organization_id', $orgId)
        ->whereNotNull('planning_day')
        ->get(['id', 'name', 'planning_day']);
    return Inertia::render('Dashboard', [
        'stats'                   => [],
        'alerts'                  => [],
        'recentDemands'           => [],
        'clients'                 => [],
        'planningReminderClients' => $planningReminderClients,
    ]);
}
```

<!-- routes/web.php preferences route atual (BLOCKER-02) -->
```php
// linha 138-146 — $r->only(['locale', 'theme']) PRECISA incluir 'onboarding_dismissed'
Route::patch('/preferences', function (Request $r) {
    $r->user()->update([
        'preferences' => array_merge(
            $r->user()->preferences ?? [],
            $r->only(['locale', 'theme'])  // ← adicionar 'onboarding_dismissed'
        ),
    ]);
    return back()->with('success', 'Preferências salvas.');
})->name('preferences');
```

<!-- User::isAdminOrOwner() — fonte: app/Models/User.php -->
```php
public function isAdminOrOwner(): bool
{
    // verifica role na organização atual do usuário
}
// Usar: if ($user->isAdminOrOwner()) { ... }
```

<!-- Padrões de query verificados no codebase -->
```php
// Soft-delete é automático (SoftDeletes trait); archived_at requer guard manual
Demand::where('organization_id', $orgId)->whereNull('archived_at')

// Carbon para date range
$weekStart = Carbon::now()->startOfWeek();
$weekEnd   = Carbon::now()->endOfWeek();

// Request date range
$start = $request->filled('start')
    ? Carbon::parse($request->start)->startOfDay()
    : Carbon::now()->startOfWeek();
$end = $request->filled('end')
    ? Carbon::parse($request->end)->endOfDay()
    : Carbon::now()->endOfWeek();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActivityLog model + preferences route fix (BLOCKER-02)</name>
  <read_first>
    - app/Models/DemandComment.php (template de model simples com belongsTo)
    - routes/web.php linhas 138-146 (preferences route a corrigir)
    - app/Models/User.php (verificar como preferences é definido como cast)
  </read_first>
  <behavior>
    - ActivityLog::create(['organization_id' => $orgId, 'action_type' => 'demand.created', ...]) persiste sem erro
    - ActivityLog tem $timestamps = false (apenas created_at, sem updated_at)
    - ActivityLog->metadata é castado como array (não string JSON)
    - PATCH /settings/preferences com {onboarding_dismissed: true} → user->preferences['onboarding_dismissed'] === true após refresh
    - PATCH /settings/preferences com {locale: 'pt'} ainda funciona (não quebrar comportamento existente)
  </behavior>
  <action>
**1a. Criar `app/Models/ActivityLog.php`:**

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

class ActivityLog extends Model
{
    use HasFactory;

    public $timestamps = false; // Apenas created_at — log imutável

    protected $fillable = [
        'organization_id',
        'user_id',
        'action_type',
        'subject_type',
        'subject_id',
        'subject_name',
        'metadata',
    ];

    protected $casts = [
        'metadata'   => 'array',
        'created_at' => 'datetime',
    ];

    public function organization(): BelongsTo
    {
        return $this->belongsTo(Organization::class);
    }

    public function user(): BelongsTo
    {
        return $this->belongsTo(User::class);
    }
}
```

**1b. Corrigir `routes/web.php` — BLOCKER-02:**

Localizar a linha `$r->only(['locale', 'theme'])` (aprox. linha 142) e alterar para:
```php
$r->only(['locale', 'theme', 'onboarding_dismissed'])
```

O bloco completo deve ficar:
```php
Route::patch('/preferences', function (Request $r) {
    $r->user()->update([
        'preferences' => array_merge(
            $r->user()->preferences ?? [],
            $r->only(['locale', 'theme', 'onboarding_dismissed'])
        ),
    ]);
    return back()->with('success', 'Preferências salvas.');
})->name('preferences');
```

Atenção: não alterar mais nada em routes/web.php. Esta é uma modificação cirúrgica de uma linha.
  </action>
  <verify>
    <automated>php artisan test --filter PreferencesTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ActivityLog" app/Models/ActivityLog.php` retorna match (arquivo existe)
    - `grep "timestamps = false" app/Models/ActivityLog.php` retorna match
    - `grep "'metadata' => 'array'" app/Models/ActivityLog.php` retorna match
    - `grep "onboarding_dismissed" routes/web.php` retorna match
    - `php artisan test --filter PreferencesTest` PASSA (verde)
  </acceptance_criteria>
  <done>ActivityLog model criado com $timestamps=false e cast de metadata como array; preferences route aceita onboarding_dismissed; PreferencesTest passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DashboardController — index() completo com todas as queries</name>
  <read_first>
    - app/Http/Controllers/DashboardController.php (stub atual a ser substituído)
    - app/Models/Demand.php (fillable, casts, relacionamentos — priority já adicionado na Wave 0)
    - app/Models/User.php (isAdminOrOwner(), current_organization_id)
    - app/Http/Controllers/DemandController.php linhas 1-80 (padrões de query e filtros)
    - app/Http/Controllers/ArchiveController.php (padrão whereNull archived_at)
    - app/Models/ActivityLog.php (criado na Task 1 deste plano)
  </read_first>
  <behavior>
    - GET /dashboard com admin logado → prop 'overview' não null, contém 'statusBreakdown', 'priorityBreakdown', 'teamWorkload', 'clientDistribution'
    - GET /dashboard com colaborador logado → prop 'overview' é null
    - GET /dashboard → prop 'personal' contém 'statusCounts', 'focusDemands', 'myDemands', 'blockers', 'weekProgress', 'completedByDay'
    - GET /dashboard → props 'hasClients' e 'hasDemands' presentes (booleanos)
    - GET /dashboard → prop 'activityFeed' é array
    - GET /dashboard?start=2026-01-01&end=2026-01-07 com admin → partial reload de 'overview' com dados filtrados pelo range
    - Demandas archived_at != null NÃO aparecem nas queries de demandas ativas
    - DashboardControllerTest: todos os 6 testes PASSAM
  </behavior>
  <action>
Substituir completamente o método index() de `app/Http/Controllers/DashboardController.php`.

O arquivo completo deve ser:

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0

namespace App\Http\Controllers;

use App\Models\ActivityLog;
use App\Models\Client;
use App\Models\Demand;
use Carbon\Carbon;
use Illuminate\Http\Request;
use Inertia\Inertia;
use Inertia\Response;

class DashboardController extends Controller
{
    public function index(Request $request): Response
    {
        $user      = auth()->user();
        $orgId     = $user->current_organization_id;
        $today     = Carbon::today();
        $yesterday = Carbon::yesterday();
        $weekStart = Carbon::now()->startOfWeek();
        $weekEnd   = Carbon::now()->endOfWeek();

        // ---------------------------------------------------------------
        // Personal props — todos os usuários (scoped a assigned_to = $user)
        // ---------------------------------------------------------------
        $personalBase = Demand::where('organization_id', $orgId)
            ->where('assigned_to', $user->id)
            ->whereNull('archived_at');
        // Nota: SoftDeletes aplica whereNull('deleted_at') automaticamente

        $statusCounts = (clone $personalBase)
            ->selectRaw('status, COUNT(*) as count')
            ->groupBy('status')
            ->pluck('count', 'status')
            ->toArray();

        // Overdue: deadline passado, status != approved, ativo
        $overdueCount = (clone $personalBase)
            ->where('deadline', '<', $today)
            ->whereNotIn('status', ['approved'])
            ->count();
        $statusCounts['overdue'] = $overdueCount;

        // Delta vs ontem: contagens de demandas que tiveram updated_at = ontem por status
        $yesterdayCounts = (clone $personalBase)
            ->whereDate('updated_at', $yesterday)
            ->selectRaw('status, COUNT(*) as count')
            ->groupBy('status')
            ->pluck('count', 'status')
            ->toArray();

        // Foco agora: atrasadas ou deadline hoje/amanhã, excluindo aprovadas, max 5
        $focusDemands = (clone $personalBase)
            ->whereNotIn('status', ['approved'])
            ->where(function ($q) use ($today) {
                $q->where('deadline', '<', $today)
                  ->orWhereDate('deadline', $today)
                  ->orWhereDate('deadline', $today->copy()->addDay());
            })
            ->with('client:id,name')
            ->orderByRaw("CASE WHEN deadline < NOW() THEN 0 ELSE 1 END")
            ->orderBy('deadline')
            ->limit(5)
            ->get(['id', 'title', 'status', 'priority', 'deadline', 'client_id'])
            ->map(fn ($d) => [
                'id'            => $d->id,
                'title'         => $d->title,
                'status'        => $d->status,
                'priority'      => $d->priority,
                'deadline'      => $d->deadline?->toDateString(),
                'client_name'   => $d->client?->name,
            ]);

        // Minhas demandas: todas (para tabs no frontend), max 20
        $myDemands = (clone $personalBase)
            ->with('client:id,name')
            ->orderByDesc('updated_at')
            ->limit(20)
            ->get(['id', 'title', 'status', 'priority', 'deadline', 'client_id', 'updated_at'])
            ->map(fn ($d) => [
                'id'          => $d->id,
                'title'       => $d->title,
                'status'      => $d->status,
                'priority'    => $d->priority,
                'deadline'    => $d->deadline?->toDateString(),
                'client_name' => $d->client?->name,
                'updated_at'  => $d->updated_at?->toDateString(),
            ]);

        // Bloqueios: demandas awaiting_feedback
        $blockers = (clone $personalBase)
            ->where('status', 'awaiting_feedback')
            ->get(['id', 'title', 'updated_at'])
            ->map(fn ($d) => [
                'id'         => $d->id,
                'title'      => $d->title,
                'since'      => $d->updated_at?->format('d/m/Y'),
            ]);

        // Progresso da semana: concluídas nesta semana vs total atribuídas
        $weekCompleted = (clone $personalBase)
            ->where('status', 'approved')
            ->whereBetween('updated_at', [$weekStart, $weekEnd])
            ->count();
        $totalAssigned = (clone $personalBase)->count();
        $weekProgress = ['completed' => $weekCompleted, 'total' => $totalAssigned];

        // Concluídas por dia da semana (para bar chart pessoal) — últimos 7 dias
        $completedByDay = Demand::where('organization_id', $orgId)
            ->where('assigned_to', $user->id)
            ->whereNull('archived_at')
            ->where('status', 'approved')
            ->whereBetween('updated_at', [$weekStart, $weekEnd])
            ->selectRaw("TO_CHAR(updated_at, 'Dy') as day, COUNT(*) as count")
            ->groupByRaw("TO_CHAR(updated_at, 'Dy'), EXTRACT(DOW FROM updated_at)")
            ->orderByRaw("EXTRACT(DOW FROM updated_at)")
            ->pluck('count', 'day')
            ->toArray();

        $personal = [
            'statusCounts'     => $statusCounts,
            'deltaVsYesterday' => $yesterdayCounts,
            'focusDemands'     => $focusDemands,
            'myDemands'        => $myDemands,
            'blockers'         => $blockers,
            'weekProgress'     => $weekProgress,
            'completedByDay'   => $completedByDay,
        ];

        // ---------------------------------------------------------------
        // Overview props — apenas admin/owner (scoped à organização inteira)
        // ---------------------------------------------------------------
        $overview = null;

        if ($user->isAdminOrOwner()) {
            // Validar date range inputs (T-5-04: sanitização antes de Carbon::parse)
            $start = $request->filled('start') && $this->isValidDate($request->start)
                ? Carbon::parse($request->start)->startOfDay()
                : $weekStart;
            $end = $request->filled('end') && $this->isValidDate($request->end)
                ? Carbon::parse($request->end)->endOfDay()
                : $weekEnd;

            $orgBase = Demand::where('organization_id', $orgId)->whereNull('archived_at');

            // Status breakdown para donut (D-15)
            $statusBreakdown = (clone $orgBase)
                ->selectRaw('status, COUNT(*) as count')
                ->groupBy('status')
                ->pluck('count', 'status')
                ->toArray();

            // Status delta vs ontem (org-wide)
            $orgYesterdayCounts = (clone $orgBase)
                ->whereDate('updated_at', $yesterday)
                ->selectRaw('status, COUNT(*) as count')
                ->groupBy('status')
                ->pluck('count', 'status')
                ->toArray();

            // Overdue org-wide
            $orgOverdueCount = (clone $orgBase)
                ->where('deadline', '<', $today)
                ->whereNotIn('status', ['approved'])
                ->count();

            // Priority breakdown para bar chart (D-16) — scoped pelo date range
            $priorityBreakdown = (clone $orgBase)
                ->whereBetween('created_at', [$start, $end])
                ->selectRaw('priority, COUNT(*) as count')
                ->groupBy('priority')
                ->pluck('count', 'priority')
                ->toArray();

            // Demandas ao longo do tempo: criadas vs concluídas por dia (D-17)
            $demandsCreatedByDay = (clone $orgBase)
                ->whereBetween('created_at', [$start, $end])
                ->selectRaw("TO_CHAR(created_at, 'MM-DD') as date, COUNT(*) as count")
                ->groupByRaw("TO_CHAR(created_at, 'MM-DD')")
                ->orderByRaw("TO_CHAR(created_at, 'MM-DD')")
                ->pluck('count', 'date')
                ->toArray();

            $demandsCompletedByDay = (clone $orgBase)
                ->where('status', 'approved')
                ->whereBetween('updated_at', [$start, $end])
                ->selectRaw("TO_CHAR(updated_at, 'MM-DD') as date, COUNT(*) as count")
                ->groupByRaw("TO_CHAR(updated_at, 'MM-DD')")
                ->orderByRaw("TO_CHAR(updated_at, 'MM-DD')")
                ->pluck('count', 'date')
                ->toArray();

            // Mesclar em array [{date, created, completed}]
            $allDates = array_unique(array_merge(
                array_keys($demandsCreatedByDay),
                array_keys($demandsCompletedByDay)
            ));
            sort($allDates);
            $demandsOverTime = array_map(fn ($date) => [
                'date'      => $date,
                'created'   => $demandsCreatedByDay[$date] ?? 0,
                'completed' => $demandsCompletedByDay[$date] ?? 0,
            ], $allDates);

            // Últimas demandas (D-18) — 5 mais recentes
            $latestDemands = (clone $orgBase)
                ->with(['client:id,name', 'assignee:id,name,avatar'])
                ->orderByDesc('updated_at')
                ->limit(5)
                ->get(['id', 'title', 'status', 'priority', 'updated_at', 'client_id', 'assigned_to'])
                ->map(fn ($d) => [
                    'id'           => $d->id,
                    'title'        => $d->title,
                    'status'       => $d->status,
                    'priority'     => $d->priority,
                    'updated_at'   => $d->updated_at?->format('d/m/Y'),
                    'client_name'  => $d->client?->name,
                    'assignee_name'  => $d->assignee?->name,
                    'assignee_avatar' => $d->assignee?->avatar,
                ]);

            // Workload por membro da equipe (DASH-02)
            $teamWorkload = Demand::where('organization_id', $orgId)
                ->whereNull('archived_at')
                ->whereNotNull('assigned_to')
                ->join('users', 'users.id', '=', 'demands.assigned_to')
                ->selectRaw('users.id, users.name, users.avatar, COUNT(*) as total,
                    SUM(CASE WHEN demands.status = \'approved\' THEN 1 ELSE 0 END) as completed,
                    SUM(CASE WHEN demands.deadline < NOW() AND demands.status != \'approved\' THEN 1 ELSE 0 END) as overdue')
                ->groupBy('users.id', 'users.name', 'users.avatar')
                ->orderByDesc('total')
                ->get()
                ->map(fn ($row) => [
                    'user_id'   => $row->id,
                    'name'      => $row->name,
                    'avatar'    => $row->avatar,
                    'total'     => (int) $row->total,
                    'completed' => (int) $row->completed,
                    'overdue'   => (int) $row->overdue,
                    'active'    => (int) $row->total - (int) $row->completed,
                ]);

            // Distribuição por cliente (DASH-03)
            $clientDistribution = Demand::where('demands.organization_id', $orgId)
                ->whereNull('demands.archived_at')
                ->join('clients', 'clients.id', '=', 'demands.client_id')
                ->selectRaw('clients.id, clients.name, COUNT(*) as total,
                    SUM(CASE WHEN demands.status = \'approved\' THEN 1 ELSE 0 END) as completed_this_week')
                ->groupBy('clients.id', 'clients.name')
                ->orderByDesc('total')
                ->limit(10)
                ->get()
                ->map(fn ($row) => [
                    'client_id' => $row->id,
                    'name'      => $row->name,
                    'total'     => (int) $row->total,
                    'completed' => (int) $row->completed_this_week,
                ]);

            // Desempenho da equipe (D-19)
            $orgTotal     = (clone $orgBase)->count();
            $orgCompleted = (clone $orgBase)->where('status', 'approved')->count();
            $teamPerformance = [
                'total'       => $orgTotal,
                'completed'   => $orgCompleted,
                'in_progress' => $orgTotal - $orgCompleted,
                'rate'        => $orgTotal > 0 ? round(($orgCompleted / $orgTotal) * 100) : 0,
            ];

            $overview = [
                'statusBreakdown'    => $statusBreakdown,
                'deltaVsYesterday'   => $orgYesterdayCounts,
                'overdueCount'       => $orgOverdueCount,
                'priorityBreakdown'  => $priorityBreakdown,
                'demandsOverTime'    => $demandsOverTime,
                'latestDemands'      => $latestDemands,
                'teamWorkload'       => $teamWorkload,
                'clientDistribution' => $clientDistribution,
                'teamPerformance'    => $teamPerformance,
                'dateRange'          => [
                    'start' => $start->toDateString(),
                    'end'   => $end->toDateString(),
                ],
            ];
        }

        // ---------------------------------------------------------------
        // Activity Feed (D-23: scoped por role)
        // ---------------------------------------------------------------
        $activityQuery = ActivityLog::where('organization_id', $orgId)
            ->with('user:id,name,avatar')
            ->orderByDesc('created_at')
            ->limit(15);

        if (! $user->isAdminOrOwner()) {
            // T-5-03: colaborador vê apenas eventos das suas demandas (assigned_to ou created_by)
            $myDemandIds = Demand::where('organization_id', $orgId)
                ->where(fn ($q) => $q
                    ->where('assigned_to', $user->id)
                    ->orWhere('created_by', $user->id)
                )
                ->pluck('id');

            $activityQuery->where(function ($q) use ($user, $myDemandIds) {
                $q->where(fn ($sub) => $sub
                    ->whereIn('subject_id', $myDemandIds)
                    ->where('subject_type', 'demand')
                )->orWhere('user_id', $user->id);
            });
        }

        $activityFeed = $activityQuery->get()->map(fn ($log) => [
            'id'           => $log->id,
            'action_type'  => $log->action_type,
            'subject_name' => $log->subject_name,
            'user_name'    => $log->user?->name ?? 'Sistema',
            'user_avatar'  => $log->user?->avatar,
            'metadata'     => $log->metadata,
            'created_at'   => $log->created_at?->diffForHumans(),
        ]);

        // ---------------------------------------------------------------
        // Onboarding (ONBRD-01)
        // ---------------------------------------------------------------
        $hasClients = Client::where('organization_id', $orgId)->exists();
        $hasDemands = Demand::where('organization_id', $orgId)
            ->whereNull('archived_at')
            ->exists();

        // Planning reminder clients (widget existente — manter)
        $planningReminderClients = Client::where('organization_id', $orgId)
            ->whereNotNull('planning_day')
            ->get(['id', 'name', 'planning_day']);

        return Inertia::render('Dashboard', [
            'personal'               => $personal,
            'overview'               => $overview, // null para colaboradores
            'activityFeed'           => $activityFeed,
            'hasClients'             => $hasClients,
            'hasDemands'             => $hasDemands,
            'planningReminderClients' => $planningReminderClients,
        ]);
    }

    /**
     * Valida que uma string de data é válida antes de passar ao Carbon::parse (T-5-04).
     */
    private function isValidDate(string $value): bool
    {
        return (bool) strtotime($value) && preg_match('/^\d{4}-\d{2}-\d{2}$/', $value);
    }
}
```

Atenção: O `$planningReminderClients` deve ser mantido pois o DashboardPlanningWidget existente ainda precisa dele.
  </action>
  <verify>
    <automated>php artisan test --filter DashboardControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter DashboardControllerTest` passa TODOS os 6 testes (verde)
    - `grep "isAdminOrOwner" app/Http/Controllers/DashboardController.php` retorna match
    - `grep "whereNull.*archived_at" app/Http/Controllers/DashboardController.php` retorna match (guards contra archived)
    - `grep "isValidDate" app/Http/Controllers/DashboardController.php` retorna match (validação de date range T-5-04)
    - `grep "planningReminderClients" app/Http/Controllers/DashboardController.php` retorna match (widget existente preservado)
    - `php artisan test` retorna suite completa sem erros de regressão
  </acceptance_criteria>
  <done>DashboardController.index() implementado com todas as queries; admin recebe overview com 9 sub-props; colaborador recebe overview: null; activityFeed scoped por role; todos os testes DashboardControllerTest passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GET /dashboard | Usuário autenticado + rota auth,verified. Trust boundary: props overview vs null para non-admins. |
| PATCH /settings/preferences | Usuário autenticado altera só os próprios preferences via allowlist. |
| Query param start/end | Input externo não confiável para date range. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Elevation of Privilege | DashboardController — overview props | mitigate | `if ($user->isAdminOrOwner())` — overview é null para colaboradores; colaborador não recebe dados gerenciais da org |
| T-5-02 | Tampering | routes/web.php preferences allowlist | mitigate | `$r->only(['locale', 'theme', 'onboarding_dismissed'])` — somente esses campos aceitos; campos arbitrários descartados pelo `only()` |
| T-5-03 | Information Disclosure | ActivityLog — colaborador vê eventos alheios | mitigate | Query com `whereIn(subject_id, myDemandIds)->where(subject_type, demand)` — colaborador só vê eventos das suas próprias demandas |
| T-5-04 | Tampering | DashboardController — date range query params | mitigate | `isValidDate()` valida formato YYYY-MM-DD antes de `Carbon::parse()`; queries usam bindings parameterizados do Eloquent |
</threat_model>

<verification>
**Plano 01 completo quando:**
1. `php artisan test --filter DashboardControllerTest` — 6 testes PASS
2. `php artisan test --filter PreferencesTest` — 1 teste PASS
3. `grep "isAdminOrOwner" app/Http/Controllers/DashboardController.php` — match
4. `grep "onboarding_dismissed" routes/web.php` — match
5. `php artisan test` — suite completa sem regressões
</verification>

<success_criteria>
- DashboardController.index() retorna props completos para ambas as views
- Admin recebe overview com statusBreakdown, priorityBreakdown, teamWorkload, clientDistribution, teamPerformance
- Colaborador recebe overview: null (nunca vaza dados gerenciais)
- activityFeed filtrado corretamente por role
- PATCH preferences aceita e persiste onboarding_dismissed
- Todos os testes criados na Wave 0 que cobrem este plano passam verde
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-dashboard-onboarding/05-01-SUMMARY.md`
</output>
