---
phase: 05-dashboard-onboarding
plan: "02"
type: execute
wave: 1
depends_on:
  - "05-00"
files_modified:
  - app/Observers/DemandObserver.php
  - app/Observers/DemandCommentObserver.php
  - app/Observers/ClientObserver.php
  - app/Providers/AppServiceProvider.php
autonomous: true
requirements:
  - DASH-05

must_haves:
  truths:
    - "Criar uma Demand gera registro em activity_logs com action_type='demand.created'"
    - "Mudar status de uma Demand gera registro com action_type='demand.status_changed' e metadata={from, to}"
    - "Mudar assigned_to gera registro com action_type='demand.assigned'"
    - "Setar archived_at gera registro com action_type='demand.archived' (via updated() — NÃO via deleted())"
    - "Soft-delete (lixeira) gera registro com action_type='demand.archived' via deleted()"
    - "Restaurar demand (restore) gera registro com action_type='demand.restored'"
    - "Criar um Client gera registro com action_type='client.created'"
    - "Criar um DemandComment gera registro com action_type='demand.comment_added' (fix review Codex HIGH)"
    - "Observer funciona mesmo em contexto CLI sem auth (user_id nullable, fallback demand->created_by)"
  artifacts:
    - path: "app/Observers/DemandObserver.php"
      provides: "Observer Eloquent para Demand com 4 métodos: created, updated, deleted, restored"
    - path: "app/Observers/DemandCommentObserver.php"
      provides: "Observer Eloquent para DemandComment com método created — dispara demand.comment_added"
    - path: "app/Observers/ClientObserver.php"
      provides: "Observer Eloquent para Client com método created"
    - path: "app/Providers/AppServiceProvider.php"
      provides: "boot() com registro dos três observers"
  key_links:
    - from: "app/Observers/DemandObserver.php"
      to: "app/Models/ActivityLog.php"
      via: "ActivityLog::create(['action_type' => ...])"
      pattern: "ActivityLog::create"
    - from: "app/Observers/DemandCommentObserver.php"
      to: "app/Models/ActivityLog.php"
      via: "ActivityLog::create(['action_type' => 'demand.comment_added'])"
      pattern: "demand.comment_added"
    - from: "app/Providers/AppServiceProvider.php"
      to: "app/Observers/DemandObserver.php"
      via: "Demand::observe(DemandObserver::class)"
      pattern: "observe"
---

<objective>
Wave 1 (paralela com 05-01) — Observers do Laravel para alimentar activity_logs automaticamente.

Purpose: D-25 (locked) exige que os logs sejam criados via Observer/Event, nunca diretamente nos controllers. Este plano implementa os dois observers e os registra no AppServiceProvider.

Output:
- DemandObserver.php com hooks: created, updated (status_changed + assigned), deleted (lixeira), restored
- ClientObserver.php com hook: created
- AppServiceProvider.php com os dois observers registrados no boot()
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-onboarding/05-CONTEXT.md
@.planning/phases/05-dashboard-onboarding/05-RESEARCH.md
@.planning/phases/05-dashboard-onboarding/05-PATTERNS.md

<interfaces>
<!-- ActivityLog model (criado em 05-01 — paralelo; assumir que existe) -->
```php
// app/Models/ActivityLog.php
class ActivityLog extends Model
{
    public $timestamps = false;
    protected $fillable = [
        'organization_id', 'user_id', 'action_type',
        'subject_type', 'subject_id', 'subject_name', 'metadata',
    ];
    protected $casts = ['metadata' => 'array', 'created_at' => 'datetime'];
}
```

<!-- AppServiceProvider atual (boot a ser expandido) -->
```php
public function boot(): void
{
    Vite::prefetch(concurrency: 3);
    // Adicionar observers APÓS esta linha
}
```

<!-- Demand model fields relevantes -->
```php
// Demand usa SoftDeletes — deleted() = soft delete (lixeira)
// archived_at é campo separado — detectar via wasChanged('archived_at') em updated()
// created_by = fallback para user_id quando auth()->id() é null (contexto CLI)
```

<!-- PITFALL CRÍTICO: demand.archived vs soft-delete (Pitfall 3 do RESEARCH.md) -->
<!--
- demand.archived (D-22) = wasChanged('archived_at') && archived_at !== null → em updated()
- Soft delete (lixeira) também usa action_type 'demand.archived' → em deleted()
- São dois eventos distintos mas com mesmo action_type
- demand.restored → em restored() do SoftDeletes
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DemandObserver + ClientObserver</name>
  <read_first>
    - app/Models/Demand.php (verificar campos: organization_id, created_by, assigned_to, status, archived_at)
    - app/Models/Client.php (verificar campos: organization_id, id, name)
    - app/Models/ActivityLog.php (fillable e casts — criado em 05-01)
  </read_first>
  <behavior>
    - DemandObserver::created() cria ActivityLog com action_type='demand.created'
    - DemandObserver::updated() com wasChanged('status') cria ActivityLog action_type='demand.status_changed' com metadata={from, to}
    - DemandObserver::updated() com wasChanged('assigned_to') E assigned_to != null cria ActivityLog action_type='demand.assigned'
    - DemandObserver::updated() com wasChanged('archived_at') E archived_at != null cria ActivityLog action_type='demand.archived'
    - DemandObserver::deleted() cria ActivityLog action_type='demand.archived' (soft delete = lixeira)
    - DemandObserver::restored() cria ActivityLog action_type='demand.restored'
    - user_id é auth()->id() ?? $demand->created_by (nunca lança exceção em contexto CLI)
    - ClientObserver::created() cria ActivityLog action_type='client.created'
    - ActivityLogTest: test_demand_created_logs_activity PASSA
    - ActivityLogTest: test_demand_status_changed_logs_activity PASSA
  </behavior>
  <action>
**1a. Criar diretório `app/Observers/` (se não existir) e `app/Observers/DemandObserver.php`:**

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Observers;

use App\Models\ActivityLog;
use App\Models\Demand;

class DemandObserver
{
    public function created(Demand $demand): void
    {
        ActivityLog::create([
            'organization_id' => $demand->organization_id,
            'user_id'         => auth()->id() ?? $demand->created_by,
            'action_type'     => 'demand.created',
            'subject_type'    => 'demand',
            'subject_id'      => $demand->id,
            'subject_name'    => $demand->title,
            'metadata'        => [],
        ]);
    }

    public function updated(Demand $demand): void
    {
        // Detectar mudança de status (priority: demand.status_changed)
        if ($demand->wasChanged('status')) {
            ActivityLog::create([
                'organization_id' => $demand->organization_id,
                'user_id'         => auth()->id() ?? $demand->created_by,
                'action_type'     => 'demand.status_changed',
                'subject_type'    => 'demand',
                'subject_id'      => $demand->id,
                'subject_name'    => $demand->title,
                'metadata'        => [
                    'from' => $demand->getOriginal('status'),
                    'to'   => $demand->status,
                ],
            ]);
        }

        // Detectar mudança de responsável (assigned_to)
        if ($demand->wasChanged('assigned_to') && $demand->assigned_to !== null) {
            ActivityLog::create([
                'organization_id' => $demand->organization_id,
                'user_id'         => auth()->id() ?? $demand->created_by,
                'action_type'     => 'demand.assigned',
                'subject_type'    => 'demand',
                'subject_id'      => $demand->id,
                'subject_name'    => $demand->title,
                'metadata'        => [
                    'assigned_to_id' => $demand->assigned_to,
                ],
            ]);
        }

        // PITFALL 3: archived_at != soft delete
        // archived_at setado = arquivamento manual (ArchiveController)
        // Usar wasChanged('archived_at') aqui, NÃO o deleted() hook
        if ($demand->wasChanged('archived_at') && $demand->archived_at !== null) {
            ActivityLog::create([
                'organization_id' => $demand->organization_id,
                'user_id'         => auth()->id() ?? $demand->created_by,
                'action_type'     => 'demand.archived',
                'subject_type'    => 'demand',
                'subject_id'      => $demand->id,
                'subject_name'    => $demand->title,
                'metadata'        => [],
            ]);
        }
    }

    public function deleted(Demand $demand): void
    {
        // Soft delete = movido para lixeira → também registra como archived
        ActivityLog::create([
            'organization_id' => $demand->organization_id,
            'user_id'         => auth()->id() ?? $demand->created_by,
            'action_type'     => 'demand.archived',
            'subject_type'    => 'demand',
            'subject_id'      => $demand->id,
            'subject_name'    => $demand->title,
            'metadata'        => ['via' => 'trash'],
        ]);
    }

    public function restored(Demand $demand): void
    {
        ActivityLog::create([
            'organization_id' => $demand->organization_id,
            'user_id'         => auth()->id() ?? $demand->created_by,
            'action_type'     => 'demand.restored',
            'subject_type'    => 'demand',
            'subject_id'      => $demand->id,
            'subject_name'    => $demand->title,
            'metadata'        => [],
        ]);
    }
}
```

**1b. Criar `app/Observers/ClientObserver.php`:**

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Observers;

use App\Models\ActivityLog;
use App\Models\Client;

class ClientObserver
{
    public function created(Client $client): void
    {
        ActivityLog::create([
            'organization_id' => $client->organization_id,
            'user_id'         => auth()->id(),
            'action_type'     => 'client.created',
            'subject_type'    => 'client',
            'subject_id'      => $client->id,
            'subject_name'    => $client->name,
            'metadata'        => [],
        ]);
    }
}
```

Nota: ClientObserver não usa fallback para `created_by` porque Client model não tem esse campo. Se auth()->id() for null em contexto CLI, user_id ficará null (campo nullable na migration).

**1c. Criar `app/Observers/DemandCommentObserver.php` (fix review Codex HIGH — demand.comment_added):**

DemandComment é criado diretamente no DemandController (não via Demand model), então DemandObserver nunca vê essa ação. É preciso um observer no próprio modelo DemandComment.

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Observers;

use App\Models\ActivityLog;
use App\Models\DemandComment;

class DemandCommentObserver
{
    public function created(DemandComment $comment): void
    {
        // demand->organization_id via relacionamento
        $demand = $comment->demand;
        if (! $demand) {
            return; // guard: sem demand associada, não registrar
        }

        ActivityLog::create([
            'organization_id' => $demand->organization_id,
            'user_id'         => auth()->id() ?? $comment->user_id,
            'action_type'     => 'demand.comment_added',
            'subject_type'    => 'demand',
            'subject_id'      => $demand->id,
            'subject_name'    => $demand->title,
            'metadata'        => [],
        ]);
    }
}
```

Nota: Ler `app/Models/DemandComment.php` antes para confirmar o nome do relacionamento (`demand()` BelongsTo) e o campo `user_id`.
  </action>
  <verify>
    <automated>php artisan test --filter ActivityLogTest</automated>
  </verify>
  <acceptance_criteria>
    - `ls app/Observers/` lista DemandObserver.php, DemandCommentObserver.php e ClientObserver.php
    - `grep "demand.status_changed" app/Observers/DemandObserver.php` retorna match
    - `grep "wasChanged.*archived_at" app/Observers/DemandObserver.php` retorna match
    - `grep "demand.assigned" app/Observers/DemandObserver.php` retorna match
    - `grep "client.created" app/Observers/ClientObserver.php` retorna match
    - `grep "demand.comment_added" app/Observers/DemandCommentObserver.php` retorna match
    - Observers NÃO passam em test antes do registro no AppServiceProvider (passo 2) — esperado
  </acceptance_criteria>
  <done>DemandObserver (4 hooks), ClientObserver (1 hook) e DemandCommentObserver (1 hook) criados; todos os 8 action_types do D-22 cobertos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registrar observers no AppServiceProvider</name>
  <read_first>
    - app/Providers/AppServiceProvider.php (estrutura atual do boot() para não sobrescrever o singleton existente)
    - app/Observers/DemandObserver.php (criado na Task 1)
    - app/Observers/DemandCommentObserver.php (criado na Task 1)
    - app/Observers/ClientObserver.php (criado na Task 1)
    - app/Models/DemandComment.php (confirmar namespace correto para import)
  </read_first>
  <behavior>
    - Demand::observe(DemandObserver::class) está registrado em boot()
    - DemandComment::observe(DemandCommentObserver::class) está registrado em boot()
    - Client::observe(ClientObserver::class) está registrado em boot()
    - O singleton AnthropicClientFactory existente NÃO é removido do register()
    - Vite::prefetch existente NÃO é removido do boot()
    - Criar DemandComment::factory()->create() com actingAs() → assertDatabaseHas activity_logs com demand.comment_added
    - ActivityLogTest: todos os testes PASSAM após registro
  </behavior>
  <action>
Editar `app/Providers/AppServiceProvider.php` para adicionar os imports e registros dos três observers no método boot().

O arquivo completo deve ser:

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0

namespace App\Providers;

use App\Models\Client;
use App\Models\Demand;
use App\Models\DemandComment;
use App\Observers\ClientObserver;
use App\Observers\DemandCommentObserver;
use App\Observers\DemandObserver;
use Illuminate\Support\Facades\Vite;
use Illuminate\Support\ServiceProvider;

class AppServiceProvider extends ServiceProvider
{
    /**
     * Register any application services.
     */
    public function register(): void
    {
        // BYOK factory — singleton so the same instance is reused per request.
        // Do NOT bind Anthropic\Client as a global singleton (keys are per-org).
        $this->app->singleton(\App\Services\Ai\AnthropicClientFactory::class);
    }

    /**
     * Bootstrap any application services.
     */
    public function boot(): void
    {
        Vite::prefetch(concurrency: 3);

        // Activity logging observers (Phase 5 — D-25)
        Demand::observe(DemandObserver::class);
        DemandComment::observe(DemandCommentObserver::class);
        Client::observe(ClientObserver::class);
    }
}
```

Atenção:
- NÃO remover o `$this->app->singleton(\App\Services\Ai\AnthropicClientFactory::class)` do register()
- NÃO remover o `Vite::prefetch(concurrency: 3)` do boot()
- Adicionar os três `observe()` calls e os imports correspondentes
  </action>
  <verify>
    <automated>php artisan test --filter ActivityLogTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DemandObserver" app/Providers/AppServiceProvider.php` retorna match em import E em boot()
    - `grep "DemandCommentObserver" app/Providers/AppServiceProvider.php` retorna match em import E em boot()
    - `grep "ClientObserver" app/Providers/AppServiceProvider.php` retorna match em import E em boot()
    - `grep "AnthropicClientFactory" app/Providers/AppServiceProvider.php` retorna match (singleton preservado)
    - `php artisan test --filter ActivityLogTest` PASSA todos os testes (verde)
    - `php artisan test` retorna suite completa sem regressões
  </acceptance_criteria>
  <done>Três observers registrados no AppServiceProvider (Demand, DemandComment, Client); todos os 8 action_types do D-22 cobertos; ActivityLogTest verde; suite completa sem regressões.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Observer → ActivityLog | Dados vêm do model Eloquent (confiável) — sem input externo |
| auth()->id() no Observer | Pode ser null em contextos CLI/jobs — tratado com fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-03 | Information Disclosure | DemandObserver — user_id fallback | mitigate | `auth()->id() ?? $demand->created_by` — evita user_id nulo que causaria IntegrityConstraintViolation; user_id é nullable na migration |
| T-5-05 | Denial of Service | Observer em loop (Observer cria ActivityLog que dispara outro Observer) | accept | ActivityLog não tem Observer registrado — sem loop recursivo; risco inexistente com este design |
| T-5-06 | Tampering | Observer disparado em contexto sem organização (org_id null) | accept | Demand.organization_id é NOT NULL na migration — nunca null em produção; seeds/factories devem sempre fornecer org_id |
</threat_model>

<verification>
**Plano 02 completo quando:**
1. `php artisan test --filter ActivityLogTest` — todos os testes PASS
2. `grep "Demand::observe" app/Providers/AppServiceProvider.php` — match
3. `grep "DemandComment::observe" app/Providers/AppServiceProvider.php` — match
4. `grep "Client::observe" app/Providers/AppServiceProvider.php` — match
5. `grep "demand.comment_added" app/Observers/DemandCommentObserver.php` — match
6. `php artisan test` — suite completa sem regressões
</verification>

<success_criteria>
- DemandObserver captura: created, status_changed, assigned, archived (via archived_at), archived (via soft-delete), restored
- ClientObserver captura: created
- Ambos registrados no AppServiceProvider::boot()
- ActivityLogTest passa todos os casos
- Suite completa sem regressões
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-dashboard-onboarding/05-02-SUMMARY.md`
</output>
