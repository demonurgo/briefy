---
phase: 05-dashboard-onboarding
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - resources/js/Components/DashboardStatusCard.tsx
  - resources/js/Components/DashboardSectionCard.tsx
  - resources/js/Components/ActivityFeed.tsx
  - resources/js/Components/OnboardingChecklist.tsx
  - resources/css/app.css
autonomous: true
requirements:
  - DASH-04
  - DASH-05
  - ONBRD-01
  - ONBRD-02

must_haves:
  truths:
    - "DashboardStatusCard renderiza label, count, delta (↑/↓/—), ícone e cor corretamente"
    - "DashboardSectionCard renderiza title, children e action (link 'Ver todas →') com design tokens corretos"
    - "ActivityFeed renderiza lista de eventos com ícone colorido por action_type, texto formatado e timestamp relativo; estado vazio exibe 'Nenhuma atividade recente.'"
    - "OnboardingChecklist aparece quando !hasClients || !hasDemands; some automaticamente quando ambos completos; dismiss chama PATCH /settings/preferences com {onboarding_dismissed: true}"
    - "Animação fadeInUp definida em app.css com @keyframes e classe .animate-fadeInUp"
  artifacts:
    - path: "resources/js/Components/DashboardStatusCard.tsx"
      provides: "Componente de card de métrica com delta indicator"
    - path: "resources/js/Components/DashboardSectionCard.tsx"
      provides: "Wrapper genérico de seção do dashboard"
    - path: "resources/js/Components/ActivityFeed.tsx"
      provides: "Feed de atividade com ícones por action_type"
    - path: "resources/js/Components/OnboardingChecklist.tsx"
      provides: "Card dismissível de onboarding com 2 passos"
    - path: "resources/css/app.css"
      provides: "Keyframe @keyframes fadeInUp e .animate-fadeInUp"
  key_links:
    - from: "resources/js/Components/OnboardingChecklist.tsx"
      to: "routes/web.php settings.preferences"
      via: "router.patch(route('settings.preferences'), {onboarding_dismissed: true})"
      pattern: "onboarding_dismissed"
    - from: "resources/js/Components/ActivityFeed.tsx"
      to: "DashboardController activityFeed prop"
      via: "ActivityEvent interface — action_type, subject_name, user_name, created_at"
      pattern: "action_type"
---

<objective>
Wave 2 (paralela com 05-04) — Componentes React do dashboard.

Purpose: Cria os 4 componentes novos que Dashboard.tsx (05-04) vai compor. Cada componente é independente e testável isoladamente. A animação CSS também é adicionada aqui.

Output:
- DashboardStatusCard.tsx — card de métrica com delta vs ontem
- DashboardSectionCard.tsx — wrapper genérico de seção
- ActivityFeed.tsx — feed de atividade com ícones coloridos
- OnboardingChecklist.tsx — card dismissível com 2 passos
- app.css — @keyframes fadeInUp + .animate-fadeInUp
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-onboarding/05-CONTEXT.md
@.planning/phases/05-dashboard-onboarding/05-UI-SPEC.md
@.planning/phases/05-dashboard-onboarding/05-PATTERNS.md

<interfaces>
<!-- DashboardPlanningWidget.tsx — template visual mestre (D-26: "mesmo padrão") -->
<!-- Tokens de design chave extraídos do arquivo: -->
```tsx
// Card roxo (onboarding — usar este):
"rounded-xl border border-[#a78bfa]/30 bg-[#7c3aed]/5 dark:bg-[#7c3aed]/10 p-4 space-y-3"

// Item card interno:
"flex items-start justify-between gap-3 rounded-lg border border-[#a78bfa]/20 bg-white dark:bg-[#1e1b2e] px-4 py-3 shadow-sm"

// Botão dismiss X:
"p-1 rounded text-[#9ca3af] hover:text-[#374151] dark:hover:text-[#d1d5db] transition-colors"

// Link roxo:
"text-xs font-medium text-[#7c3aed] hover:underline whitespace-nowrap"
```

<!-- Padrão router.patch para preferences (extraído de useTheme.ts) -->
```tsx
router.patch(
    route('settings.preferences'),
    { theme: next },
    { preserveState: true, preserveScroll: true }
);
// Adaptar para: { onboarding_dismissed: true }
```

<!-- Action type colors (05-UI-SPEC.md) -->
```tsx
const ACTION_CONFIG = {
  'demand.status_changed': { icon: TrendingUp, color: '#3b82f6' },
  'demand.created':        { icon: Plus,        color: '#10b981' },
  'demand.comment_added':  { icon: MessageCircle, color: '#6b7280' },
  'demand.assigned':       { icon: Users,        color: '#8b5cf6' },
  'demand.archived':       { icon: Archive,      color: '#9ca3af' },
  'demand.restored':       { icon: RotateCcw,    color: '#f59e0b' },
  'client.created':        { icon: Building2,    color: '#7c3aed' },
  'member.invited':        { icon: UserPlus,     color: '#7c3aed' },
} as const;
```

<!-- Delta colors (05-UI-SPEC.md D-07) -->
```tsx
// Up = verde, Down = vermelho, EXCETO "Atrasadas" onde Up = vermelho (deltaInverted)
// deltaInverted?: boolean prop para inverter cores
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashboardStatusCard, DashboardSectionCard e animação CSS</name>
  <read_first>
    - resources/js/Components/DashboardPlanningWidget.tsx (tokens de design a reusar)
    - resources/css/app.css (verificar estrutura atual para adicionar @keyframes no lugar correto)
    - resources/js/Components/StatusBadge.tsx (verificar se já importa lucide-react — confirmar ArrowUp/ArrowDown disponíveis)
  </read_first>
  <action>
**1a. Criar `resources/js/Components/DashboardStatusCard.tsx`:**

```tsx
// (c) 2026 Briefy contributors — AGPL-3.0
import { ArrowUp, ArrowDown, Minus, type LucideIcon } from 'lucide-react';

interface Props {
  label: string;
  count: number;
  delta: number | null;
  deltaInverted?: boolean; // true para "Atrasadas": ↑ é ruim (vermelho), ↓ é bom (verde)
  icon: LucideIcon;
  iconColor: string;
  animationDelay?: number; // ms para stagger
}

export function DashboardStatusCard({
  label,
  count,
  delta,
  deltaInverted = false,
  icon: Icon,
  iconColor,
  animationDelay = 0,
}: Props) {
  const isUp   = delta !== null && delta > 0;
  const isDown = delta !== null && delta < 0;

  // Cor do delta — invertida para cards onde "mais" é ruim (Atrasadas)
  const deltaColor = delta === null || delta === 0
    ? 'text-[#9ca3af]'
    : isUp
      ? (deltaInverted ? 'text-[#ef4444]' : 'text-[#10b981]')
      : (deltaInverted ? 'text-[#10b981]' : 'text-[#ef4444]');

  const deltaText = delta === null || delta === 0
    ? '— mesmo que ontem'
    : isUp
      ? `↑ ${Math.abs(delta)} vs ontem`
      : `↓ ${Math.abs(delta)} vs ontem`;

  const DeltaIcon = delta === null || delta === 0
    ? Minus
    : isUp ? ArrowUp : ArrowDown;

  return (
    <div
      className="rounded-[12px] border border-[#e5e7eb] dark:border-[#1f2937] bg-white dark:bg-[#111827] p-4 flex flex-col gap-2 animate-fadeInUp"
      style={{ animationDelay: `${animationDelay}ms` }}
      role="region"
      aria-label={`${label} count`}
    >
      {/* Top row: label + icon */}
      <div className="flex items-center justify-between">
        <span className="text-xs font-medium text-[#6b7280]">{label}</span>
        <Icon size={20} style={{ color: iconColor }} aria-hidden="true" />
      </div>

      {/* Count */}
      <span className="text-2xl font-bold text-[#111827] dark:text-[#f9fafb]">
        {count}
      </span>

      {/* Delta indicator */}
      <div className={`flex items-center gap-1 text-xs font-medium ${deltaColor}`}>
        <DeltaIcon size={12} aria-hidden="true" />
        <span>{deltaText}</span>
      </div>
    </div>
  );
}
```

**1b. Criar `resources/js/Components/DashboardSectionCard.tsx`:**

```tsx
// (c) 2026 Briefy contributors — AGPL-3.0
import { type ReactNode } from 'react';

interface Props {
  title: string;
  children: ReactNode;
  action?: ReactNode;
  className?: string;
}

export function DashboardSectionCard({ title, children, action, className = '' }: Props) {
  return (
    <div
      className={`rounded-[12px] border border-[#e5e7eb] dark:border-[#1f2937] bg-white dark:bg-[#111827] p-6 ${className}`}
    >
      <div className="flex items-center justify-between mb-4">
        <h3 className="text-base font-semibold text-[#111827] dark:text-[#f9fafb]">
          {title}
        </h3>
        {action && <div>{action}</div>}
      </div>
      {children}
    </div>
  );
}
```

**1c. Adicionar animação CSS em `resources/css/app.css`:**

Localizar o final do arquivo (ou a seção de utilitários CSS existente) e adicionar:

```css
/* Dashboard status card entrance animation (Phase 5 — UI-SPEC) */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
.animate-fadeInUp {
  animation: fadeInUp 200ms ease-out both;
}
```

Atenção: adicionar APÓS todo o conteúdo existente, não sobrescrever.
  </action>
  <verify>
    <automated>ls resources/js/Components/DashboardStatusCard.tsx resources/js/Components/DashboardSectionCard.tsx && grep "animate-fadeInUp" resources/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep "deltaInverted" resources/js/Components/DashboardStatusCard.tsx` retorna match
    - `grep "role=\"region\"" resources/js/Components/DashboardStatusCard.tsx` retorna match (acessibilidade)
    - `grep "animate-fadeInUp" resources/js/Components/DashboardStatusCard.tsx` retorna match
    - `grep "animationDelay" resources/js/Components/DashboardStatusCard.tsx` retorna match (stagger support)
    - `grep "fadeInUp" resources/css/app.css` retorna match
    - `grep "@keyframes fadeInUp" resources/css/app.css` retorna match
    - `grep "DashboardSectionCard" resources/js/Components/DashboardSectionCard.tsx` retorna match
    - `npx tsc --noEmit 2>&1 | grep -E "(DashboardStatusCard|DashboardSectionCard)"` — sem erros de tipo
  </acceptance_criteria>
  <done>DashboardStatusCard com delta indicator + acessibilidade; DashboardSectionCard genérico; @keyframes fadeInUp em app.css.</done>
</task>

<task type="auto">
  <name>Task 2: ActivityFeed e OnboardingChecklist</name>
  <read_first>
    - resources/js/Components/DashboardPlanningWidget.tsx (padrão exato de dismiss + visual para OnboardingChecklist)
    - resources/js/hooks/useTheme.ts (padrão de router.patch para preferences — extrair pattern de dismiss)
    - resources/js/Components/DashboardSectionCard.tsx (criado em Task 1 — wrapper para ActivityFeed)
  </read_first>
  <action>
**2a. Criar `resources/js/Components/ActivityFeed.tsx`:**

```tsx
// (c) 2026 Briefy contributors — AGPL-3.0
import {
  TrendingUp,
  Plus,
  MessageCircle,
  Users,
  Archive,
  RotateCcw,
  Building2,
  UserPlus,
  Activity,
  type LucideIcon,
} from 'lucide-react';
import { DashboardSectionCard } from '@/Components/DashboardSectionCard';

export interface ActivityEvent {
  id: number;
  action_type: string;
  subject_name: string;
  user_name: string;
  user_avatar?: string | null;
  metadata?: Record<string, unknown>;
  created_at: string; // já formatado como diffForHumans() no backend
}

interface ActionConfig {
  icon: LucideIcon;
  color: string;
  verb: (event: ActivityEvent) => string;
}

const ACTION_CONFIG: Record<string, ActionConfig> = {
  'demand.status_changed': {
    icon: TrendingUp,
    color: '#3b82f6',
    verb: (e) => {
      const meta = e.metadata as { to?: string } | undefined;
      return `moveu "${e.subject_name}" para ${meta?.to ?? 'novo status'}`;
    },
  },
  'demand.created': {
    icon: Plus,
    color: '#10b981',
    verb: (e) => `criou "${e.subject_name}"`,
  },
  'demand.comment_added': {
    icon: MessageCircle,
    color: '#6b7280',
    verb: (e) => `comentou em "${e.subject_name}"`,
  },
  'demand.assigned': {
    icon: Users,
    color: '#8b5cf6',
    verb: (e) => `atribuiu "${e.subject_name}"`,
  },
  'demand.archived': {
    icon: Archive,
    color: '#9ca3af',
    verb: (e) => {
      const meta = e.metadata as { via?: string } | undefined;
      return meta?.via === 'trash'
        ? `moveu "${e.subject_name}" para a lixeira`
        : `arquivou "${e.subject_name}"`;
    },
  },
  'demand.restored': {
    icon: RotateCcw,
    color: '#f59e0b',
    verb: (e) => `restaurou "${e.subject_name}"`,
  },
  'client.created': {
    icon: Building2,
    color: '#7c3aed',
    verb: (e) => `adicionou o cliente "${e.subject_name}"`,
  },
  'member.invited': {
    icon: UserPlus,
    color: '#7c3aed',
    verb: (e) => `convidou ${e.subject_name}`,
  },
};

const FALLBACK_CONFIG: ActionConfig = {
  icon: Activity,
  color: '#9ca3af',
  verb: (e) => `agiu em "${e.subject_name}"`,
};

interface Props {
  events: ActivityEvent[];
}

export function ActivityFeed({ events }: Props) {
  return (
    <DashboardSectionCard title="Atividade recente">
      <div
        className="max-h-80 overflow-y-auto"
        role="feed"
        aria-label="Feed de atividade recente"
      >
        {events.length === 0 ? (
          <p className="text-sm text-[#9ca3af] text-center py-8">
            Nenhuma atividade recente.
          </p>
        ) : (
          events.map((event) => {
            const config = ACTION_CONFIG[event.action_type] ?? FALLBACK_CONFIG;
            const Icon = config.icon;

            return (
              <div
                key={event.id}
                className="flex items-start gap-3 py-3 border-b border-[#e5e7eb] dark:border-[#1f2937] last:border-0"
                role="article"
              >
                {/* Icon circle */}
                <div
                  className="w-8 h-8 rounded-full flex items-center justify-center shrink-0"
                  style={{ backgroundColor: `${config.color}1a` }}
                  aria-hidden="true"
                >
                  <Icon size={16} style={{ color: config.color }} />
                </div>

                {/* Text */}
                <div className="flex-1 min-w-0">
                  <p className="text-sm text-[#111827] dark:text-[#f9fafb]">
                    <span className="font-medium">{event.user_name}</span>{' '}
                    {config.verb(event)}
                  </p>
                  <p className="text-xs text-[#9ca3af] mt-0.5">{event.created_at}</p>
                </div>
              </div>
            );
          })
        )}
      </div>
    </DashboardSectionCard>
  );
}
```

**2b. Criar `resources/js/Components/OnboardingChecklist.tsx`:**

```tsx
// (c) 2026 Briefy contributors — AGPL-3.0
import { useState } from 'react';
import { Link, router } from '@inertiajs/react';
import { CheckCircle, Circle, X } from 'lucide-react';

interface Props {
  hasClients: boolean;
  hasDemands: boolean;
  onboardingDismissed?: boolean;
}

export function OnboardingChecklist({ hasClients, hasDemands, onboardingDismissed }: Props) {
  const [dismissed, setDismissed] = useState(false);

  // Não renderizar se: dispensado (estado local ou preference persistida) OU ambos os passos completos
  if (dismissed || onboardingDismissed || (hasClients && hasDemands)) {
    return null;
  }

  const handleDismiss = () => {
    setDismissed(true); // Otimistic — esconde imediatamente
    router.patch(
      route('settings.preferences'),
      { onboarding_dismissed: true },
      { preserveState: true, preserveScroll: true }
    );
  };

  const steps = [
    {
      id: 'add-client',
      label: 'Adicionar um cliente',
      href: '/clients/create',
      complete: hasClients,
    },
    {
      id: 'create-demand',
      label: 'Criar a primeira demanda',
      href: '/demands?create=1',
      complete: hasDemands,
    },
  ];

  return (
    <div className="rounded-xl border border-[#a78bfa]/30 bg-[#7c3aed]/5 dark:bg-[#7c3aed]/10 p-4 space-y-3">
      {/* Header */}
      <div className="flex items-center justify-between">
        <div className="flex items-center gap-2">
          <CheckCircle size={20} className="text-[#7c3aed] dark:text-[#a78bfa]" aria-hidden="true" />
          <span className="text-sm font-semibold text-[#7c3aed] dark:text-[#a78bfa]">
            Primeiros passos
          </span>
        </div>
        <button
          onClick={handleDismiss}
          className="p-1 rounded text-[#9ca3af] hover:text-[#374151] dark:hover:text-[#d1d5db] transition-colors"
          aria-label="Dispensar guia de início"
          type="button"
        >
          <X size={14} />
        </button>
      </div>

      {/* Steps */}
      {steps.map((step) => (
        <div
          key={step.id}
          className="flex items-center gap-3 rounded-lg border border-[#a78bfa]/20 bg-white dark:bg-[#1e1b2e] px-4 py-3 shadow-sm"
          aria-checked={step.complete}
        >
          {step.complete ? (
            <CheckCircle size={16} className="text-[#10b981] shrink-0" aria-hidden="true" />
          ) : (
            <Circle size={16} className="text-[#9ca3af] shrink-0" aria-hidden="true" />
          )}

          <div className="flex-1 min-w-0">
            <span className="text-sm font-medium text-[#111827] dark:text-[#f3f4f6]">
              {step.label}
            </span>
            {!step.complete && (
              <div className="mt-0.5">
                <Link
                  href={step.href}
                  className="text-xs font-medium text-[#7c3aed] hover:underline"
                  aria-label={`${step.label} — abrir`}
                >
                  Começar →
                </Link>
              </div>
            )}
          </div>
        </div>
      ))}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>ls resources/js/Components/ActivityFeed.tsx resources/js/Components/OnboardingChecklist.tsx && npx tsc --noEmit 2>&1 | grep -c "error TS" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "role=\"feed\"" resources/js/Components/ActivityFeed.tsx` retorna match (acessibilidade)
    - `grep "role=\"article\"" resources/js/Components/ActivityFeed.tsx` retorna match
    - `grep "ACTION_CONFIG" resources/js/Components/ActivityFeed.tsx` retorna match com todos os 8 action_types
    - `grep "onboarding_dismissed" resources/js/Components/OnboardingChecklist.tsx` retorna match (PATCH)
    - `grep "dismissed.*true.*setDismissed" resources/js/Components/OnboardingChecklist.tsx` retorna match (otimistic hide)
    - `grep "aria-label=\"Dispensar guia de início\"" resources/js/Components/OnboardingChecklist.tsx` retorna match
    - `grep "hasClients.*hasDemands" resources/js/Components/OnboardingChecklist.tsx` retorna match (auto-hide quando completo)
    - `npx tsc --noEmit 2>&1 | grep -E "(ActivityFeed|OnboardingChecklist)" | grep "error"` — sem erros TypeScript
  </acceptance_criteria>
  <done>ActivityFeed com 8 action_types mapeados, ícones coloridos, roles ARIA, estado vazio; OnboardingChecklist com dismiss otimístico, PATCH preferences, auto-hide quando passos completos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ActivityFeed — dados do server | action_type e metadata vêm do backend (confiáveis); subject_name pode conter texto do usuário — renderizar como text, não dangerouslySetInnerHTML |
| OnboardingChecklist — PATCH | Envia apenas {onboarding_dismissed: true} — payload mínimo, sem dados sensíveis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-07 | Tampering | ActivityFeed — XSS via subject_name | mitigate | subject_name renderizado como texto React (não innerHTML) — JSX escapa automaticamente; nunca usar dangerouslySetInnerHTML |
| T-5-08 | Tampering | OnboardingChecklist — PATCH payload | accept | Payload é `{onboarding_dismissed: true}` fixo; backend usa `$r->only()` allowlist — sem risco de injection via payload |
| T-5-02 | Tampering | OnboardingChecklist — PATCH /settings/preferences | mitigate | Backend allowlist `$r->only(['locale', 'theme', 'onboarding_dismissed'])` corrigida em 05-01 — campos arbitrários descartados |
</threat_model>

<verification>
**Plano 03 completo quando:**
1. Todos os 4 arquivos de componente existem
2. `grep "animate-fadeInUp" resources/css/app.css` — match
3. `npx tsc --noEmit` — sem erros TypeScript nos novos arquivos
4. `grep "onboarding_dismissed" resources/js/Components/OnboardingChecklist.tsx` — match
</verification>

<success_criteria>
- 4 novos componentes criados com design tokens corretos (06-UI-SPEC)
- Animação fadeInUp em app.css com stagger support no DashboardStatusCard
- ActivityFeed mapeia todos os 8 action_types com ícones e cores do UI-SPEC
- OnboardingChecklist: dismiss otimístico + PATCH + auto-hide quando completo
- Zero erros TypeScript nos novos arquivos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-dashboard-onboarding/05-03-SUMMARY.md`
</output>
