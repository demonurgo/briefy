---
phase: 03-ai-integration
plan: 05
type: execute
wave: 1
depends_on: [01, 02, 03]
files_modified:
  - app/Services/Ai/ChatPromptBuilder.php
  - app/Services/Ai/ChatStreamer.php
  - app/Http/Controllers/AiChatController.php
  - app/Http/Requests/ChatMessageRequest.php
  - resources/prompts/chat_system.md
  - routes/web.php
  - tests/Feature/AiChatControllerTest.php
  - lang/pt-BR/app.php
  - lang/en/app.php
  - lang/es/app.php
autonomous: true
requirements: [AI-04, AI-05, AI-07]
must_haves:
  truths:
    - "POST /demands/{demand}/chat/conversations creates a new ai_conversation scoped to (organization_id, user_id, demand_id) and returns JSON {id}"
    - "POST /demands/{demand}/chat/{conversation}/stream accepts {message: string}, persists user message to ai_conversation_messages BEFORE opening the stream, then emits SSE delta events for the assistant response"
    - "On message_stop: assistant message persisted to ai_conversation_messages AND ExtractClientMemoryJob is dispatched (Plan 07 defines the job)"
    - "System prompt includes client memory + demand metadata + last 20 comments + file list (AI-05)"
    - "When messages.count() on a conversation exceeds 30, CompactConversationJob is dispatched and a compacted_at flag is surfaced via Inertia prop"
    - "BYOK degradation: no key → back()->with('error', ...)"
  artifacts:
    - path: "app/Services/Ai/ChatPromptBuilder.php"
      provides: "chatSystem(Demand $demand): array"
      contains: "cache_control"
    - path: "app/Services/Ai/ChatStreamer.php"
      provides: "stream(AiConversation $conversation, string $userMessage, Client $anthropic): Generator"
      contains: "message_stop"
    - path: "app/Http/Controllers/AiChatController.php"
      provides: "startConversation(), stream()"
      exports: ["startConversation", "stream"]
    - path: "app/Http/Requests/ChatMessageRequest.php"
      provides: "message validation (required, 1-4000 chars)"
      contains: "max:4000"
    - path: "resources/prompts/chat_system.md"
      provides: "Versioned chat system prompt"
      min_lines: 25
  key_links:
    - from: "app/Http/Controllers/AiChatController.php"
      to: "app/Services/Ai/AnthropicClientFactory.php"
      via: "forOrganization gate + Anthropic\\Client resolution"
      pattern: "forOrganization"
    - from: "app/Http/Controllers/AiChatController.php"
      to: "app/Jobs/ExtractClientMemoryJob (from Plan 07)"
      via: "dispatch after message_stop"
      pattern: "ExtractClientMemoryJob::dispatch"
    - from: "ChatStreamer"
      to: "CompactConversationJob (from Plan 07)"
      via: "dispatch when messages > 30"
      pattern: "CompactConversationJob::dispatch"
---

<objective>
Implementar **chat com IA por demanda** (AI-04, AI-05, AI-07). Painel da aba "Chat IA" abrirá uma conversa (`ai_conversations` scopo `context_type='demand'`), usuário envia mensagem, backend abre SSE com Claude Sonnet 4.6 e streamig delta → frontend renderiza typewriter. System prompt inclui tudo necessário por D-12. Após `message_stop`: dispara jobs de extração de memória e compactação.

Purpose: segunda capability core. Reutiliza quase literalmente o padrão do Plan 04 (mesmo SDK, mesmo SSE, mesma persistência) mas adiciona histórico multi-turn, cache_control para ~80% dos turnos 2..N, e gancho para memória de cliente.

Output: chat endpoint SSE funcional + histórico persistido em `ai_conversation_messages` + disparo de jobs de memória.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai-integration/03-CONTEXT.md
@.planning/phases/03-ai-integration/03-AI-SPEC.md
@.planning/phases/03-ai-integration/03-PATTERNS.md

<interfaces>
SSE contract:

```
POST /demands/{demand}/chat/conversations       → 200 JSON {id, created_at}
POST /demands/{demand}/chat/{conversation}/stream
  body: {message: string (1..4000)}
  response: 200 text/event-stream
  events: event:delta data:{text}, event:done data:{ok,assistant_message_id}
```

```php
// app/Services/Ai/ChatPromptBuilder.php
public function chatSystem(\App\Models\Demand $demand): array;
// returns Anthropic system-block array:
//   [0] => ['type'=>'text','text'=>"...client memory + demand metadata + files...",'cache_control'=>['type'=>'ephemeral']]
//   [1] => ['type'=>'text','text'=>"## Last 20 comments\n..."]  // outside cache — volatile tail

// app/Services/Ai/ChatStreamer.php
public function stream(
  \App\Models\AiConversation $conversation,
  string $userMessage,
  \Anthropic\Client $anthropic
): \Generator;
```

Jobs dispatched (defined in Plan 07 — signatures used here):
```php
\App\Jobs\ExtractClientMemoryJob::dispatch($conversation)->onQueue('ai');
\App\Jobs\CompactConversationJob::dispatch($conversation)->onQueue('ai');
```

Existing tables:
```
ai_conversations:   id, organization_id, user_id, context_type(enum), context_id, title, compacted_at, timestamps
ai_conversation_messages: id, conversation_id, role(user|assistant|system), content, timestamps
```
</interfaces>

Source references:
- `app/Models/AiConversation.php` (after Plan 02 mods — `compacted_at` cast)
- `app/Models/AiConversationMessage.php`
- AI-SPEC §3 lines 310-346 (chat streaming entry point pattern (b))
- AI-SPEC §4 lines 534-589 (ChatPromptBuilder code — **copy system prompt structure verbatim**)
- AI-SPEC §4 "Context Window Management" (compaction trigger at 30 msgs)
- PATTERNS.md line 722-723 (routes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatPromptBuilder + versioned prompt</name>
  <read_first>
    - D:\projetos\briefy\app\Models\Demand.php
    - D:\projetos\briefy\app\Models\Client.php (aiMemory relation)
    - D:\projetos\briefy\app\Models\ClientAiMemory.php
    - D:\projetos\briefy\app\Models\DemandFile.php
    - D:\projetos\briefy\app\Models\DemandComment.php
    - .planning/phases/03-ai-integration/03-AI-SPEC.md lines 534-589 (full ChatPromptBuilder code)
    - .planning/phases/03-ai-integration/03-AI-SPEC.md §5 Dimension 6 (context grounding rubric) — the prompt must explicitly constrain hallucinations
  </read_first>
  <files>
    - resources/prompts/chat_system.md (NEW)
    - app/Services/Ai/ChatPromptBuilder.php (NEW)
  </files>
  <action>
### 1. Create `resources/prompts/chat_system.md`

```markdown
# Briefy — Chat System Prompt v1

You are Briefy's AI assistant for a marketing agency. You help the team work on one specific creative demand for one specific client. You have persistent memory about this client (captured from past work) and full metadata about the current demand.

## Rules

- You MAY only reference files, comments, memory entries, or demand fields that appear in the context below. Do NOT invent past interactions, past campaigns, or client preferences that are not explicitly recorded.
- When asked about something that's not in your context, say so plainly: "Não tenho essa informação registrada — você pode me contar?".
- Stay inside the client's tone (see `client_ai_memory` entries). Avoid generic marketing-speak ("alavancar", "desbloquear", "potencializar") unless the memory entries explicitly endorse those patterns for THIS client.
- Output in Portuguese-BR unless the user writes in English.
- Keep answers concise. If the user wants a long draft, ask whether they'd prefer a draft (longer output) or a direction (tighter output).
- Do NOT generate actual legal/medical/financial claims about the client's products unless those claims are already in the demand description.

## Context (injected below by ChatPromptBuilder)

(Client, demand, files, comments are inserted by the backend.)
```

### 2. Create `app/Services/Ai/ChatPromptBuilder.php`

Mirror the AI-SPEC §4 structure exactly (lines 534-589). Two system blocks: one cached with stable prefix, one volatile tail for comments.

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Services\Ai;

use App\Models\Demand;

final class ChatPromptBuilder
{
    /**
     * Build system blocks for the chat. First block is cached (stable per demand/client);
     * second block carries the volatile comments tail (last 20) outside the cache.
     *
     * @return array<int, array{type: string, text: string, cache_control?: array}>
     */
    public function chatSystem(Demand $demand): array
    {
        $client   = $demand->client;
        $files    = $demand->files->map(fn ($f) => "- {$f->name} ({$f->mime_type})")->implode("\n") ?: '_(nenhum)_';
        $memory   = $client->aiMemory()->orderBy('category')->get()
                        ->map(fn ($m) => "- [{$m->category}] (confidence={$m->confidence}) {$m->insight}")
                        ->implode("\n") ?: '_(sem memória durável ainda)_';
        $template = file_get_contents(resource_path('prompts/chat_system.md'));

        $stablePrefix = <<<PROMPT
        {$template}

        ## Client: {$client->name}

        Tone, patterns, preferences (`client_ai_memory`):
        {$memory}

        ## Demand

        - Title: {$demand->title}
        - Objective: {$demand->objective}
        - Channel: {$demand->channel}
        - Tone declared: {$demand->tone}
        - Deadline: {$demand->deadline}

        Description:
        {$demand->description}

        ## Files attached

        {$files}
        PROMPT;

        // Volatile tail — comments tail. Loaded separately, outside cache_control.
        $comments = $demand->comments()
            ->with('user')
            ->latest()
            ->take(20)
            ->get()
            ->reverse()
            ->map(fn ($c) => "[{$c->created_at}] {$c->user?->name}: {$c->body}")
            ->implode("\n") ?: '_(sem comentários)_';

        return [
            [
                'type' => 'text',
                'text' => $stablePrefix,
                'cache_control' => ['type' => 'ephemeral'],
            ],
            [
                'type' => 'text',
                'text' => "## Last 20 comments on this demand\n\n{$comments}",
            ],
        ];
    }
}
```
  </action>
  <verify>
    <automated>php -l app/Services/Ai/ChatPromptBuilder.php && test -f resources/prompts/chat_system.md</automated>
  </verify>
  <acceptance_criteria>
    - File `resources/prompts/chat_system.md` exists (≥ 25 lines)
    - `grep -q "Do NOT invent" resources/prompts/chat_system.md` → exit 0
    - File `app/Services/Ai/ChatPromptBuilder.php` exists
    - `grep -q "public function chatSystem" app/Services/Ai/ChatPromptBuilder.php` → exit 0
    - `grep -q "cache_control.*ephemeral" app/Services/Ai/ChatPromptBuilder.php` → exit 0
    - `grep -q "aiMemory" app/Services/Ai/ChatPromptBuilder.php` → exit 0
    - `grep -q "take(20)" app/Services/Ai/ChatPromptBuilder.php` → exit 0 (D-12: last 20 comments)
    - `grep -q "Briefy contributors" app/Services/Ai/ChatPromptBuilder.php` → exit 0
    - `php -l` passes
  </acceptance_criteria>
  <done>Versioned prompt + builder ready; system blocks cover all D-12 items (memory, demand, files, last 20 comments).</done>
</task>

<task type="auto">
  <name>Task 2: ChatStreamer + AiChatController + ChatMessageRequest + routes</name>
  <read_first>
    - D:\projetos\briefy\app\Services\Ai\BriefStreamer.php (Plan 04 — mirror error handling + set_time_limit)
    - D:\projetos\briefy\app\Http\Controllers\AiBriefController.php (Plan 04 — mirror auth + BYOK gate + SSE wrapper)
    - D:\projetos\briefy\app\Models\AiConversation.php (post Plan 02)
    - D:\projetos\briefy\app\Models\AiConversationMessage.php
    - .planning/phases/03-ai-integration/03-AI-SPEC.md lines 310-346 (chat streaming pattern (b)) + lines 750-760 (memory extraction dispatch)
    - .planning/phases/03-ai-integration/03-PATTERNS.md line 722-723 (routes)
  </read_first>
  <files>
    - app/Services/Ai/ChatStreamer.php (NEW)
    - app/Http/Controllers/AiChatController.php (NEW)
    - app/Http/Requests/ChatMessageRequest.php (NEW)
    - routes/web.php (MODIFY — append chat routes)
    - lang/pt-BR/app.php + en + es (MODIFY)
  </files>
  <action>
### 1. Create `app/Http/Requests/ChatMessageRequest.php`

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

class ChatMessageRequest extends FormRequest
{
    public function authorize(): bool { return true; }

    public function rules(): array
    {
        return [
            'message' => ['required', 'string', 'min:1', 'max:4000'],
        ];
    }
}
```

### 2. Create `app/Services/Ai/ChatStreamer.php`

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Services\Ai;

use Anthropic\Client;
use App\Jobs\CompactConversationJob;
use App\Jobs\ExtractClientMemoryJob;
use App\Models\AiConversation;
use Generator;
use Illuminate\Http\StreamedEvent;
use Illuminate\Support\Facades\Log;

final class ChatStreamer
{
    public const COMPACTION_THRESHOLD = 30;

    public function __construct(private ChatPromptBuilder $prompts) {}

    /**
     * Stream the assistant response. Caller is responsible for persisting the USER
     * message BEFORE calling this method (so the turn survives a client disconnect).
     * This method persists only the ASSISTANT message on message_stop.
     */
    public function stream(AiConversation $conversation, string $userMessage, Client $anthropic): Generator
    {
        @set_time_limit(0);

        $demand = $conversation->context_type === 'demand'
            ? \App\Models\Demand::with(['client.aiMemory', 'files', 'comments.user'])->find($conversation->context_id)
            : null;

        abort_unless($demand, 422, 'Chat conversation has no associated demand');

        $system = $this->prompts->chatSystem($demand);

        // Build the turns array from existing history + the just-appended user message.
        $history = $conversation->messages()
            ->orderBy('id')
            ->get()
            ->map(fn ($m) => ['role' => $m->role, 'content' => $m->content])
            ->all();

        $assistant = '';

        try {
            $stream = $anthropic->messages->createStream(
                maxTokens: 2048,
                messages: $history,
                model: (string) config('services.anthropic.model_chat'),
                system: $system,
                temperature: 0.7,
            );

            foreach ($stream as $event) {
                if ($event->type === 'content_block_delta'
                    && ($event->delta->type ?? null) === 'text_delta') {
                    $chunk = $event->delta->text ?? '';
                    $assistant .= $chunk;
                    yield new StreamedEvent(
                        event: 'delta',
                        data: json_encode(['text' => $chunk], JSON_UNESCAPED_UNICODE),
                    );
                }

                if ($event->type === 'message_stop') {
                    $msg = $conversation->messages()->create([
                        'role'    => 'assistant',
                        'content' => $assistant,
                    ]);

                    // Dispatch post-turn jobs — fire-and-forget, non-blocking.
                    ExtractClientMemoryJob::dispatch($conversation->fresh())->onQueue('ai');

                    if ($conversation->messages()->count() > self::COMPACTION_THRESHOLD) {
                        CompactConversationJob::dispatch($conversation->fresh())->onQueue('ai');
                    }

                    yield new StreamedEvent(
                        event: 'done',
                        data: json_encode([
                            'ok' => true,
                            'assistant_message_id' => $msg->id,
                            'compaction_triggered' => $conversation->messages()->count() > self::COMPACTION_THRESHOLD,
                        ], JSON_UNESCAPED_UNICODE),
                    );
                }
            }
        } catch (\Throwable $e) {
            Log::warning('chat.stream_failed', [
                'conversation_id' => $conversation->id,
                'error_class' => class_basename($e),
                'partial_chars' => strlen($assistant),
            ]);
            yield new StreamedEvent(
                event: 'error',
                data: json_encode(['message' => __('app.ai_chat_stream_failed')], JSON_UNESCAPED_UNICODE),
            );
        }
    }
}
```

### 3. Create `app/Http/Controllers/AiChatController.php`

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Http\Controllers;

use App\Http\Requests\ChatMessageRequest;
use App\Models\AiConversation;
use App\Models\Demand;
use App\Services\Ai\AnthropicClientFactory;
use App\Services\Ai\ChatStreamer;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\RedirectResponse;
use Symfony\Component\HttpFoundation\StreamedResponse;

class AiChatController extends Controller
{
    public function __construct(
        private AnthropicClientFactory $clientFactory,
        private ChatStreamer $streamer,
    ) {}

    /**
     * POST /demands/{demand}/chat/conversations — create a new empty conversation.
     */
    public function startConversation(Demand $demand): JsonResponse
    {
        $this->authorizeDemand($demand);

        $conversation = AiConversation::create([
            'organization_id' => auth()->user()->organization_id,
            'user_id'         => auth()->id(),
            'context_type'    => 'demand',
            'context_id'      => $demand->id,
            'title'           => "Chat — {$demand->title}",
        ]);

        return response()->json([
            'id'         => $conversation->id,
            'created_at' => $conversation->created_at->toIso8601String(),
        ]);
    }

    /**
     * POST /demands/{demand}/chat/{conversation}/stream — SSE reply.
     */
    public function stream(
        ChatMessageRequest $request,
        Demand $demand,
        AiConversation $conversation,
    ): StreamedResponse|RedirectResponse {
        $this->authorizeDemand($demand);
        abort_if(
            $conversation->organization_id !== auth()->user()->organization_id
            || $conversation->context_type !== 'demand'
            || $conversation->context_id !== $demand->id,
            403,
        );

        $org = auth()->user()->organization;
        if (! $org->hasAnthropicKey()) {
            return back()->with('error', __('app.ai_key_missing'));
        }

        // Persist user message BEFORE opening the stream — survives client disconnect.
        $conversation->messages()->create([
            'role'    => 'user',
            'content' => $request->validated('message'),
        ]);

        $anthropic = $this->clientFactory->forOrganization($org);

        return response()->eventStream(
            fn () => yield from $this->streamer->stream($conversation->fresh(), $request->validated('message'), $anthropic),
        );
    }

    private function authorizeDemand(Demand $demand): void
    {
        abort_if($demand->organization_id !== auth()->user()->organization_id, 403);
    }
}
```

### 4. Modify `routes/web.php` — append inside the existing auth+verified group

```php
// AI — Chat per demand (streaming)
Route::post('/demands/{demand}/chat/conversations', [\App\Http\Controllers\AiChatController::class, 'startConversation'])
    ->name('demands.chat.start');
Route::post('/demands/{demand}/chat/{conversation}/stream', [\App\Http\Controllers\AiChatController::class, 'stream'])
    ->name('demands.chat.stream');
```

### 5. Append to `lang/pt-BR/app.php` + en + es

pt-BR:
```php
'ai_chat_stream_failed' => 'A resposta foi interrompida. Envie sua mensagem novamente.',
```
EN: `'ai_chat_stream_failed' => 'The response was interrupted. Send your message again.'`
ES: `'ai_chat_stream_failed' => 'La respuesta se interrumpió. Envíe su mensaje nuevamente.'`
  </action>
  <verify>
    <automated>php -l app/Services/Ai/ChatStreamer.php && php -l app/Http/Controllers/AiChatController.php && php -l app/Http/Requests/ChatMessageRequest.php && grep -q "demands.chat.start" routes/web.php && grep -q "demands.chat.stream" routes/web.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "public function stream.*AiConversation" app/Services/Ai/ChatStreamer.php` → exit 0
    - `grep -q "ExtractClientMemoryJob::dispatch" app/Services/Ai/ChatStreamer.php` → exit 0
    - `grep -q "CompactConversationJob::dispatch" app/Services/Ai/ChatStreamer.php` → exit 0
    - `grep -q "COMPACTION_THRESHOLD = 30" app/Services/Ai/ChatStreamer.php` → exit 0
    - `grep -q "public function startConversation" app/Http/Controllers/AiChatController.php` → exit 0
    - `grep -q "public function stream" app/Http/Controllers/AiChatController.php` → exit 0
    - `grep -q "hasAnthropicKey" app/Http/Controllers/AiChatController.php` → exit 0 (D-33 gate)
    - `grep -q "Persist user message BEFORE" app/Http/Controllers/AiChatController.php` → exit 0 (comment marker indicating AI-SPEC pattern honored)
    - `grep -q "messages()->create" app/Http/Controllers/AiChatController.php` → exit 0 (user turn persisted before stream)
    - `grep -q "demands.chat.start" routes/web.php` → exit 0
    - `grep -q "demands.chat.stream" routes/web.php` → exit 0
    - `grep -q "max:4000" app/Http/Requests/ChatMessageRequest.php` → exit 0
    - `grep -q "ai_chat_stream_failed" lang/pt-BR/app.php` → exit 0
    - All PHP files have `Briefy contributors` header
    - `php -l` passes on all new files
  </acceptance_criteria>
  <done>Chat streaming endpoint functional end-to-end. startConversation creates empty conversation; stream persists user turn, calls SDK, persists assistant turn, dispatches jobs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Feature test for AiChatController</name>
  <read_first>
    - D:\projetos\briefy\tests\Feature\AiBriefControllerTest.php (Plan 04 — mirror structure)
    - D:\projetos\briefy\tests\TestCase.php
  </read_first>
  <files>
    - tests/Feature/AiChatControllerTest.php (NEW)
  </files>
  <behavior>
    - Test 1: startConversation creates AiConversation scoped to org+user+demand, returns JSON with id
    - Test 2: stream without Anthropic key → redirects with error
    - Test 3: stream with valid key + fake ChatStreamer → persists user message before stream, emits delta events, persists assistant message on done
    - Test 4: stream with message > 4000 chars → 422
    - Test 5: Cross-org conversation access → 403
    - Test 6: Compaction trigger when msgs > 30 → CompactConversationJob dispatched (use Queue::fake())
  </behavior>
  <action>
```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace Tests\Feature;

use App\Jobs\CompactConversationJob;
use App\Jobs\ExtractClientMemoryJob;
use App\Models\AiConversation;
use App\Models\Client;
use App\Models\Demand;
use App\Models\Organization;
use App\Models\User;
use App\Services\Ai\ChatStreamer;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Http\StreamedEvent;
use Illuminate\Support\Facades\Queue;
use Tests\TestCase;

class AiChatControllerTest extends TestCase
{
    use RefreshDatabase;

    private Organization $org;
    private User $user;
    private Client $client;
    private Demand $demand;

    protected function setUp(): void
    {
        parent::setUp();
        $this->org = Organization::factory()->create();
        $this->user = User::factory()->create(['organization_id' => $this->org->id]);
        $this->client = Client::factory()->create(['organization_id' => $this->org->id]);
        $this->demand = Demand::factory()->create([
            'organization_id' => $this->org->id,
            'client_id' => $this->client->id,
        ]);
    }

    public function test_start_conversation_creates_row(): void
    {
        $res = $this->actingAs($this->user)
            ->postJson("/demands/{$this->demand->id}/chat/conversations");

        $res->assertStatus(200);
        $res->assertJsonStructure(['id', 'created_at']);
        $this->assertDatabaseHas('ai_conversations', [
            'organization_id' => $this->org->id,
            'user_id' => $this->user->id,
            'context_type' => 'demand',
            'context_id' => $this->demand->id,
        ]);
    }

    public function test_stream_without_key_redirects_with_error(): void
    {
        $conv = AiConversation::create([
            'organization_id' => $this->org->id, 'user_id' => $this->user->id,
            'context_type' => 'demand', 'context_id' => $this->demand->id,
        ]);

        $res = $this->actingAs($this->user)
            ->post("/demands/{$this->demand->id}/chat/{$conv->id}/stream", ['message' => 'oi']);

        $res->assertRedirect();
        $res->assertSessionHas('error');
    }

    public function test_stream_persists_user_and_assistant_messages(): void
    {
        Queue::fake();
        $this->org->update(['anthropic_api_key_encrypted' => 'sk-ant-' . str_repeat('a', 50)]);
        $conv = AiConversation::create([
            'organization_id' => $this->org->id, 'user_id' => $this->user->id,
            'context_type' => 'demand', 'context_id' => $this->demand->id,
        ]);

        $this->app->instance(ChatStreamer::class, new class extends ChatStreamer {
            public function __construct() {}
            public function stream($conversation, string $userMessage, \Anthropic\Client $anthropic): \Generator
            {
                yield new StreamedEvent(event: 'delta', data: json_encode(['text' => 'Resposta ']));
                yield new StreamedEvent(event: 'delta', data: json_encode(['text' => 'da IA.']));
                $conversation->messages()->create(['role' => 'assistant', 'content' => 'Resposta da IA.']);
                \App\Jobs\ExtractClientMemoryJob::dispatch($conversation);
                yield new StreamedEvent(event: 'done', data: json_encode(['ok' => true, 'assistant_message_id' => 1]));
            }
        });

        $res = $this->actingAs($this->user)
            ->post("/demands/{$this->demand->id}/chat/{$conv->id}/stream", ['message' => 'Olá IA']);

        $res->assertStatus(200);
        ob_start();
        $res->sendContent();
        $body = ob_get_clean();
        $this->assertStringContainsString('event: delta', $body);
        $this->assertStringContainsString('event: done', $body);

        // User message persisted before stream
        $this->assertDatabaseHas('ai_conversation_messages', ['conversation_id' => $conv->id, 'role' => 'user', 'content' => 'Olá IA']);
        // Assistant message persisted by fake streamer
        $this->assertDatabaseHas('ai_conversation_messages', ['conversation_id' => $conv->id, 'role' => 'assistant']);

        Queue::assertPushed(ExtractClientMemoryJob::class);
    }

    public function test_stream_rejects_oversize_message(): void
    {
        $conv = AiConversation::create([
            'organization_id' => $this->org->id, 'user_id' => $this->user->id,
            'context_type' => 'demand', 'context_id' => $this->demand->id,
        ]);

        $res = $this->actingAs($this->user)
            ->post("/demands/{$this->demand->id}/chat/{$conv->id}/stream", ['message' => str_repeat('a', 4001)]);

        $res->assertStatus(302);
        $res->assertSessionHasErrors('message');
    }

    public function test_cross_org_conversation_returns_403(): void
    {
        $otherOrg = Organization::factory()->create();
        $otherUser = User::factory()->create(['organization_id' => $otherOrg->id]);

        $conv = AiConversation::create([
            'organization_id' => $this->org->id, 'user_id' => $this->user->id,
            'context_type' => 'demand', 'context_id' => $this->demand->id,
        ]);

        $this->actingAs($otherUser)
            ->post("/demands/{$this->demand->id}/chat/{$conv->id}/stream", ['message' => 'hi'])
            ->assertStatus(403);
    }
}
```
  </action>
  <verify>
    <automated>php -l tests/Feature/AiChatControllerTest.php && grep -c "public function test_" tests/Feature/AiChatControllerTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public function test_" tests/Feature/AiChatControllerTest.php` ≥ 5
    - `grep -q "Queue::fake" tests/Feature/AiChatControllerTest.php` → exit 0
    - `grep -q "ExtractClientMemoryJob" tests/Feature/AiChatControllerTest.php` → exit 0
    - `grep -q "RefreshDatabase" tests/Feature/AiChatControllerTest.php` → exit 0
    - `php -l` passes
  </acceptance_criteria>
  <done>Test file with 5+ methods covering SSE contract, BYOK degradation, persistence, validation, cross-org isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API chat/stream | User message + conversation context, must be scoped to their org/user/demand |
| chat→client_ai_memory (via job) | Post-turn extraction writes to memory — cross-client scoping is failure mode #5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-40 | Spoofing | AiChatController::stream | mitigate | Triple-check: demand.org + conversation.org + conversation.context_id == demand.id |
| T-03-41 | Tampering | ChatMessageRequest | mitigate | `required|string|min:1|max:4000` — bounds payload |
| T-03-42 | Information disclosure | System prompt exposure | accept | Assistant response MAY quote system prompt content; this is client-side data the user is already authorized to see |
| T-03-43 | Denial of service | Unbounded chat cost | accept | BYOK shifts cost to user; Plan 13 adds usage meter |
| T-03-44 | Elevation of privilege | cross-conversation write | mitigate | conversation.organization_id + context scoping checked before any write |
| T-03-45 | Memory poisoning (domain-specific) | post-turn insight extraction | defer-to-Plan-07 | Plan 07's ExtractClientMemoryJob is responsible for client_id scoping on writes |
</threat_model>

<verification>
- All must_haves.truths pass
- Manual: configure key, open demand (after Plan 09 wires UI), send chat message, observe streaming + persistence
- `php artisan route:list | grep demands.chat` shows both routes
</verification>

<success_criteria>
- SSE endpoint returns text/event-stream
- ai_conversation_messages table grows by 2 rows per turn (user + assistant)
- ExtractClientMemoryJob dispatched on every completed turn
- Compaction triggered at msg 31
</success_criteria>

<output>
Create `.planning/phases/03-ai-integration/03-05-SUMMARY.md`. Note test-run status, any issues with @headlessui, any TODO for Plan 07 alignment.
</output>
