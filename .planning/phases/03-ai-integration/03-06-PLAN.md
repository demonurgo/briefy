---
phase: 03-ai-integration
plan: 06
type: execute
wave: 1
depends_on: [01, 02, 03]
files_modified:
  - app/Services/Ai/Schemas/MonthlyPlanSchema.php
  - app/Services/Ai/MonthlyPlanGenerator.php
  - app/Services/Ai/ItemRedesigner.php
  - app/Services/Ai/CostEstimator.php
  - app/Http/Controllers/MonthlyPlanningController.php
  - app/Http/Requests/RedesignItemRequest.php
  - resources/js/Components/CostConfirmModal.tsx
  - resources/prompts/plan_system.md
  - resources/prompts/redesign_system.md
  - routes/web.php
  - tests/Feature/MonthlyPlanningControllerTest.php
  - lang/pt-BR/app.php
  - lang/en/app.php
  - lang/es/app.php
autonomous: true
requirements: [MPLAN-03, MPLAN-04, MPLAN-05]
must_haves:
  truths:
    - "GET /planejamento renders Inertia page with demands(type='planning') grouped by month + clients + filter prop"
    - "POST /planejamento/generate {client_id, year, month} creates a Demand(type='planning') + exactly monthly_posts PlanningSuggestion rows, returns 302 back"
    - "Generator uses Opus 4.7 (model_complex) + tool-use 'submit_plan' with JSON schema enforcing size=monthly_posts"
    - "Retry logic: 2 attempts on ValidationException; if all fail, back()->with('error', ...)"
    - "POST /planning-suggestions/{s}/convert creates a Demand(type='demand') with title/date from suggestion, sets suggestion.status='accepted', converted_demand_id"
    - "POST /planning-suggestions/convert-bulk {ids: [int]} creates N demands atomically"
    - "POST /planning-suggestions/{s}/redesign {feedback} calls Claude Haiku to rewrite title/description, PATCHes suggestion in-place"
    - "POST /planning-suggestions/{s}/reject sets status='rejected'"
    - "GET /planejamento/estimate-cost?client_id=X returns {cost_usd: float, model: 'opus', monthly_posts: int} for frontend pre-generation confirm modal (D-34)"
    - "CostConfirmModal shared component renders 'Essa operação custará ~$X. Continuar?' when estimated cost > 0.05 USD; blocks submission until user confirms"
  artifacts:
    - path: "app/Services/Ai/Schemas/MonthlyPlanSchema.php"
      provides: "toolSchema(int $count) + rules(int $count)"
      contains: "channel"
    - path: "app/Services/Ai/MonthlyPlanGenerator.php"
      provides: "generate(Client $client, int $year, int $month, Anthropic\\Client $anthropic): array"
      contains: "submit_plan"
    - path: "app/Services/Ai/ItemRedesigner.php"
      provides: "redesign(PlanningSuggestion $s, string $feedback, Anthropic\\Client $anthropic): array"
      contains: "model_cheap"
    - path: "app/Http/Controllers/MonthlyPlanningController.php"
      provides: "index, generate, convert, convertBulk, redesign, reject, update"
      exports: ["index", "generate", "convert", "convertBulk", "redesign", "reject", "update"]
  key_links:
    - from: "MonthlyPlanGenerator"
      to: "MonthlyPlanSchema::toolSchema"
      via: "tools input_schema"
      pattern: "MonthlyPlanSchema::toolSchema"
    - from: "MonthlyPlanningController::convert"
      to: "planning_suggestions + demands tables"
      via: "DB transaction creating Demand + updating PlanningSuggestion"
      pattern: "DB::transaction"
---

<objective>
Implementar a capability de **planejamento mensal** (MPLAN-03, MPLAN-04, MPLAN-05). Opus 4.7 gera exatamente `client.monthly_posts` sugestões via **tool-use com schema estrito** (D-22), itens são persistidos em `planning_suggestions` atrelados a uma `Demand(type='planning')`, usuário pode converter individual/bulk, rejeitar, ou redesenhar itens individuais via Haiku.

Purpose: capability mais sensível da Phase 3 em termos de falhas silenciosas (quota violation = monocultura de pillars = plan inútil). Por isso Opus 4.7 (AI-SPEC §4 model routing rationale) + retry com schema validation.

Output: generator service + redesign service + 7-method controller + schema class + prompts + rotas + testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai-integration/03-CONTEXT.md
@.planning/phases/03-ai-integration/03-AI-SPEC.md
@.planning/phases/03-ai-integration/03-PATTERNS.md

<interfaces>
```php
// app/Services/Ai/Schemas/MonthlyPlanSchema.php
final class MonthlyPlanSchema {
    public static function toolSchema(int $expectedCount): array;  // Anthropic input_schema
    public static function rules(int $expectedCount): array;        // Laravel validator rules
}

// app/Services/Ai/MonthlyPlanGenerator.php
public function generate(
    \App\Models\Client $client,
    int $year,
    int $month,
    \Anthropic\Client $anthropic,
): array;  // returns ['items' => [{date,title,description,channel}, ...]]

// app/Services/Ai/ItemRedesigner.php
public function redesign(
    \App\Models\PlanningSuggestion $suggestion,
    string $feedback,
    \Anthropic\Client $anthropic,
): array;  // returns ['title'=>..., 'description'=>..., 'channel'=>...]

// Routes (controller actions)
// GET  /planejamento                          -> index (Inertia render)
// POST /planejamento/generate                 -> generate (blocking; creates Demand + PlanningSuggestion[])
// POST /planning-suggestions/{s}/convert      -> convert single
// POST /planning-suggestions/convert-bulk     -> convertBulk
// POST /planning-suggestions/{s}/redesign     -> redesign (calls Claude Haiku, returns JSON)
// POST /planning-suggestions/{s}/reject       -> reject
// PATCH /planning-suggestions/{s}             -> update (manual edit)
```

Existing tables:
```
demands.type enum: 'demand' | 'planning'   (per D-20)
demands.ai_analysis: JSON (can be used for plan metadata)
planning_suggestions: id, demand_id, date, title, description, status enum(pending|accepted|rejected), converted_demand_id
```

Existing:
- `App\Models\PlanningSuggestion` — already created in Phase 2
- `App\Models\Demand` — already supports `type='planning'`
</interfaces>

Source references:
- AI-SPEC §3 lines 349-368 (MonthlyPlanGenerator entry point)
- AI-SPEC §4b lines 670-770 (tool-use + retry + validation)
- AI-SPEC §4 lines 458-465 (Opus 4.7 rationale)
- PATTERNS.md lines 726-733 (planejamento routes)
- PATTERNS.md line 64 (demands.type enum values)
</context>

<tasks>

<task type="auto">
  <name>Task 1: MonthlyPlanSchema + ItemRedesigner + prompt files</name>
  <read_first>
    - D:\projetos\briefy\app\Models\PlanningSuggestion.php
    - D:\projetos\briefy\app\Models\Demand.php (confirm type='planning' enum exists)
    - .planning/phases/03-ai-integration/03-AI-SPEC.md lines 672-712 (MonthlyPlanSchema) + lines 596-634 (tool-use pattern)
  </read_first>
  <files>
    - app/Services/Ai/Schemas/MonthlyPlanSchema.php (NEW)
    - app/Services/Ai/ItemRedesigner.php (NEW)
    - resources/prompts/plan_system.md (NEW)
    - resources/prompts/redesign_system.md (NEW)
  </files>
  <action>
### 1. Create `app/Services/Ai/Schemas/MonthlyPlanSchema.php`

Per AI-SPEC §4b lines 672-712:

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Services\Ai\Schemas;

use Illuminate\Validation\Rule;

final class MonthlyPlanSchema
{
    public const CHANNELS = ['instagram', 'linkedin', 'facebook', 'tiktok', 'blog', 'email', 'other'];

    /** JSON schema passed as Anthropic tool input_schema. */
    public static function toolSchema(int $expectedCount): array
    {
        return [
            'type' => 'object',
            'properties' => [
                'items' => [
                    'type' => 'array',
                    'minItems' => $expectedCount,
                    'maxItems' => $expectedCount,
                    'items' => [
                        'type' => 'object',
                        'properties' => [
                            'date'        => ['type' => 'string', 'format' => 'date'],
                            'title'       => ['type' => 'string', 'minLength' => 3, 'maxLength' => 140],
                            'description' => ['type' => 'string', 'minLength' => 10, 'maxLength' => 600],
                            'channel'     => ['type' => 'string', 'enum' => self::CHANNELS],
                        ],
                        'required' => ['date', 'title', 'description', 'channel'],
                    ],
                ],
            ],
            'required' => ['items'],
        ];
    }

    /** Laravel validator rules — second defensive gate. */
    public static function rules(int $expectedCount): array
    {
        return [
            'items'               => ['required', 'array', "size:{$expectedCount}"],
            'items.*.date'        => ['required', 'date'],
            'items.*.title'       => ['required', 'string', 'min:3', 'max:140'],
            'items.*.description' => ['required', 'string', 'min:10', 'max:600'],
            'items.*.channel'     => ['required', Rule::in(self::CHANNELS)],
        ];
    }
}
```

### 2. Create `resources/prompts/plan_system.md`

```markdown
# Briefy — Monthly Plan Generation System Prompt v1

You are Briefy's senior content strategist. You build a month of content for a marketing agency's brand-client.

## Rules (HARD)

1. Return EXACTLY `{{monthly_posts}}` items in your tool call — not one less, not one more.
2. Every `date` falls within the requested month `{{year}}-{{month:02d}}`.
3. Spread items across at least 3 distinct calendar weeks of the month; do NOT stack the whole month in a single week.
4. When the user provides a channel breakdown (e.g., "8 Instagram, 5 LinkedIn, 7 Facebook"), the `channel` counts in your output MUST match exactly.
5. Channel mix must reflect at least 3 distinct pillars (educate / entertain / inspire / promote / authority). No pillar owns more than 40% of the plan.
6. Use the client's tone from `client_ai_memory`. Avoid generic "alavancar/desbloquear/potencializar" unless the memory explicitly endorses that vocabulary.
7. Each item's `description` is a one-paragraph content brief (10-600 chars) — specific enough that a copywriter can start writing without asking questions.
8. Surface at least one relevant Brazilian commemorative date for the month (Dia das Mães in May, Festa Junina in June, Black Friday in November, etc.) when canonical for the client's segment.
9. Do NOT schedule every item on a Sunday. Respect channel conventions (Instagram/LinkedIn feed items weekdays; long-form blog pieces mid-week).

## Client Context

**Name:** {{client_name}}
**Segment:** {{client_segment}}
**Monthly posts quota:** {{monthly_posts}}
**Channel breakdown (optional):** {{monthly_plan_notes}}
**Client memory (tone, patterns, preferences):**
{{client_memory}}

## Output Protocol

Call the `submit_plan` tool exactly once with the full `items` array. Do NOT produce any text outside the tool call.
```

### 3. Create `resources/prompts/redesign_system.md`

```markdown
# Briefy — Planning Item Redesign System Prompt v1

You rewrite ONE planning item based on user feedback. Keep the `date` unchanged unless feedback explicitly requests a new date.

## Rules

- Output ONLY a JSON object with keys: `title` (3-140 chars), `description` (10-600 chars), `channel` (enum: instagram/linkedin/facebook/tiktok/blog/email/other), `date` (YYYY-MM-DD string).
- Do NOT wrap in markdown fences. Do NOT prepend commentary.
- Honor the user's feedback literally. If they say "more playful", lean into playful. If they say "change to LinkedIn", change `channel`.
- Stay inside the client's tone per memory.

## Current item

Title: {{current_title}}
Description: {{current_description}}
Channel: {{current_channel}}
Date: {{current_date}}

## User feedback

{{feedback}}

## Client context

{{client_memory_short}}

Output the JSON now. No other output.
```

### 4. Create `app/Services/Ai/ItemRedesigner.php`

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Services\Ai;

use Anthropic\Client;
use App\Models\PlanningSuggestion;

final class ItemRedesigner
{
    public function redesign(PlanningSuggestion $suggestion, string $feedback, Client $anthropic): array
    {
        $demand = $suggestion->demand; // planning-type demand
        $client = $demand?->client;

        $memory = $client
            ? $client->aiMemory()->take(10)->get()->map(fn ($m) => "- [{$m->category}] {$m->insight}")->implode("\n")
            : '_(sem memória)_';

        $template = file_get_contents(resource_path('prompts/redesign_system.md'));
        $system = strtr($template, [
            '{{current_title}}'       => (string) $suggestion->title,
            '{{current_description}}' => (string) $suggestion->description,
            '{{current_channel}}'     => (string) ($suggestion->channel ?? 'other'),
            '{{current_date}}'        => $suggestion->date?->toDateString() ?? '',
            '{{feedback}}'            => $feedback,
            '{{client_memory_short}}' => $memory,
        ]);

        $response = $anthropic->messages->create(
            maxTokens: 512,
            messages: [['role' => 'user', 'content' => 'Rewrite the item per feedback. Output JSON only.']],
            model: (string) config('services.anthropic.model_cheap'),  // Haiku 4.5
            system: $system,
            temperature: 0.7,
        );

        $text = $response->content[0]->text ?? '';
        // Strip code fences if the model ignored the "no fences" rule.
        $text = preg_replace('/^```(?:json)?\s*|\s*```$/m', '', $text);
        $decoded = json_decode(trim($text), true);

        if (! is_array($decoded) || ! isset($decoded['title'], $decoded['description'])) {
            throw new \RuntimeException('Redesign model returned malformed JSON.');
        }

        return [
            'title'       => (string) $decoded['title'],
            'description' => (string) $decoded['description'],
            'channel'     => (string) ($decoded['channel'] ?? $suggestion->channel ?? 'other'),
            'date'        => (string) ($decoded['date'] ?? $suggestion->date?->toDateString()),
        ];
    }
}
```
  </action>
  <verify>
    <automated>php -l app/Services/Ai/Schemas/MonthlyPlanSchema.php && php -l app/Services/Ai/ItemRedesigner.php && test -f resources/prompts/plan_system.md && test -f resources/prompts/redesign_system.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "public static function toolSchema" app/Services/Ai/Schemas/MonthlyPlanSchema.php` → exit 0
    - `grep -q "minItems.*expectedCount" app/Services/Ai/Schemas/MonthlyPlanSchema.php` → exit 0
    - `grep -q "maxItems.*expectedCount" app/Services/Ai/Schemas/MonthlyPlanSchema.php` → exit 0
    - `grep -q "'instagram', 'linkedin', 'facebook', 'tiktok', 'blog', 'email', 'other'" app/Services/Ai/Schemas/MonthlyPlanSchema.php` → exit 0
    - `grep -q "public function redesign" app/Services/Ai/ItemRedesigner.php` → exit 0
    - `grep -q "model_cheap" app/Services/Ai/ItemRedesigner.php` → exit 0
    - `grep -q "{{monthly_posts}}" resources/prompts/plan_system.md` → exit 0
    - `grep -q "Dia das Mães" resources/prompts/plan_system.md` → exit 0
    - Both PHP files have Briefy contributors header
    - `php -l` passes
  </acceptance_criteria>
  <done>Schema + prompts + redesigner compile.</done>
</task>

<task type="auto">
  <name>Task 2: MonthlyPlanGenerator (Opus 4.7 + tool-use + retry)</name>
  <read_first>
    - app/Services/Ai/Schemas/MonthlyPlanSchema.php (from Task 1)
    - resources/prompts/plan_system.md (from Task 1)
    - .planning/phases/03-ai-integration/03-AI-SPEC.md lines 716-767 (full generate() implementation with retry) + lines 458-465 (Opus rationale)
  </read_first>
  <files>
    - app/Services/Ai/MonthlyPlanGenerator.php (NEW)
  </files>
  <action>
```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Services\Ai;

use Anthropic\Client as AnthropicClient;
use App\Models\Client as ClientModel;
use App\Services\Ai\Schemas\MonthlyPlanSchema;
use Illuminate\Support\Facades\Log;
use Illuminate\Validation\ValidationException;

final class MonthlyPlanGenerator
{
    public function generate(ClientModel $client, int $year, int $month, AnthropicClient $anthropic): array
    {
        $expectedCount = (int) $client->monthly_posts;
        abort_if($expectedCount <= 0, 422, 'Cliente sem quota mensal configurada.');

        $template = file_get_contents(resource_path('prompts/plan_system.md'));
        $memory = $client->aiMemory()->orderBy('category')->get()
            ->map(fn ($m) => "- [{$m->category}] (confidence={$m->confidence}) {$m->insight}")
            ->implode("\n") ?: '_(sem memória)_';

        $system = strtr($template, [
            '{{client_name}}'        => (string) $client->name,
            '{{client_segment}}'     => (string) ($client->segment ?? 'não informado'),
            '{{monthly_posts}}'      => (string) $expectedCount,
            '{{monthly_plan_notes}}' => (string) ($client->monthly_plan_notes ?? '(sem breakdown específico — use 3-5 pillars e distribuição natural)'),
            '{{client_memory}}'      => $memory,
            '{{year}}'               => (string) $year,
            '{{month:02d}}'          => sprintf('%02d', $month),
        ]);

        $attempts = 0;
        $lastError = null;

        while ($attempts < 3) {
            $attempts++;
            try {
                $userMsg = "Gere {$expectedCount} itens para {$year}-" . sprintf('%02d', $month) . ".";
                if ($lastError) {
                    $userMsg .= "\n\nTENTATIVA ANTERIOR FALHOU a validação do schema: " . json_encode($lastError);
                }

                $response = $anthropic->messages->create(
                    maxTokens: 4096,
                    messages: [['role' => 'user', 'content' => $userMsg]],
                    model: (string) config('services.anthropic.model_complex'),  // Opus 4.7
                    system: $system,
                    tools: [[
                        'name' => 'submit_plan',
                        'description' => "Submit exactly {$expectedCount} items for the month.",
                        'input_schema' => MonthlyPlanSchema::toolSchema($expectedCount),
                    ]],
                    toolChoice: ['type' => 'tool', 'name' => 'submit_plan'],
                    temperature: 0.4,
                );

                $toolUse = collect($response->content ?? [])->firstWhere('type', 'tool_use');
                if (! $toolUse) {
                    $lastError = ['model' => 'did not call submit_plan'];
                    continue;
                }

                $input = is_array($toolUse->input) ? $toolUse->input : (array) $toolUse->input;

                $validated = validator($input, MonthlyPlanSchema::rules($expectedCount))->validate();

                Log::info('monthly_plan.generated', [
                    'client_id' => $client->id,
                    'year' => $year,
                    'month' => $month,
                    'attempts' => $attempts,
                    'items' => count($validated['items']),
                ]);

                return $validated;

            } catch (ValidationException $e) {
                $lastError = $e->errors();
                Log::warning('monthly_plan.validation_failed', [
                    'client_id' => $client->id, 'attempt' => $attempts, 'errors' => $lastError,
                ]);
            }
        }

        Log::error('monthly_plan.max_retries_exceeded', [
            'client_id' => $client->id, 'last_error' => $lastError,
        ]);
        throw new \RuntimeException('Monthly plan generation failed schema validation 3×.');
    }
}
```
  </action>
  <verify>
    <automated>php -l app/Services/Ai/MonthlyPlanGenerator.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "model_complex" app/Services/Ai/MonthlyPlanGenerator.php` → exit 0
    - `grep -q "MonthlyPlanSchema::toolSchema" app/Services/Ai/MonthlyPlanGenerator.php` → exit 0
    - `grep -q "toolChoice.*submit_plan" app/Services/Ai/MonthlyPlanGenerator.php` → exit 0
    - `grep -q "while (\\\$attempts < 3)" app/Services/Ai/MonthlyPlanGenerator.php` → exit 0
    - `grep -q "ValidationException" app/Services/Ai/MonthlyPlanGenerator.php` → exit 0
    - `grep -q "Briefy contributors" app/Services/Ai/MonthlyPlanGenerator.php` → exit 0
    - `php -l` passes
  </acceptance_criteria>
  <done>Generator with Opus 4.7 + tool-use + 2-retry loop + schema validation.</done>
</task>

<task type="auto">
  <name>Task 3: MonthlyPlanningController (7 actions) + FormRequest + routes + i18n</name>
  <read_first>
    - D:\projetos\briefy\app\Http\Controllers\DemandController.php (authorize + index + store patterns)
    - D:\projetos\briefy\app\Models\PlanningSuggestion.php
    - D:\projetos\briefy\app\Models\Demand.php
    - D:\projetos\briefy\app\Http\Controllers\PlanningController.php (current stub — MAY delete or leave)
    - .planning/phases/03-ai-integration/03-PATTERNS.md lines 726-733 (routes)
    - .planning/phases/03-ai-integration/03-UI-SPEC.md §8 /planejamento page
  </read_first>
  <files>
    - app/Http/Requests/RedesignItemRequest.php (NEW)
    - app/Http/Controllers/MonthlyPlanningController.php (NEW)
    - routes/web.php (MODIFY)
    - lang/pt-BR/app.php (MODIFY)
    - lang/en/app.php (MODIFY)
    - lang/es/app.php (MODIFY)
  </files>
  <action>
### 1. Create `app/Http/Requests/RedesignItemRequest.php`

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

class RedesignItemRequest extends FormRequest
{
    public function authorize(): bool { return true; }

    public function rules(): array
    {
        return [
            'feedback' => ['required', 'string', 'min:5', 'max:1000'],
        ];
    }
}
```

### 2. Create `app/Http/Controllers/MonthlyPlanningController.php`

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Http\Controllers;

use App\Http\Requests\RedesignItemRequest;
use App\Models\Client;
use App\Models\Demand;
use App\Models\PlanningSuggestion;
use App\Services\Ai\AnthropicClientFactory;
use App\Services\Ai\ItemRedesigner;
use App\Services\Ai\MonthlyPlanGenerator;
use Illuminate\Http\JsonResponse;
use Illuminate\Http\RedirectResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\DB;
use Inertia\Inertia;
use Inertia\Response;

class MonthlyPlanningController extends Controller
{
    public function __construct(
        private AnthropicClientFactory $clientFactory,
        private MonthlyPlanGenerator $generator,
        private ItemRedesigner $redesigner,
    ) {}

    /** GET /planejamento */
    public function index(Request $request): Response
    {
        $orgId = auth()->user()->organization_id;

        $planningDemands = Demand::where('organization_id', $orgId)
            ->where('type', 'planning')
            ->when($request->client_id, fn ($q, $id) => $q->where('client_id', $id))
            ->with(['client', 'planningSuggestions' => fn ($q) => $q->orderBy('date')])
            ->orderByDesc('created_at')
            ->get();

        $clients = Client::where('organization_id', $orgId)->orderBy('name')
            ->get(['id', 'name', 'monthly_posts', 'monthly_plan_notes']);

        return Inertia::render('Planejamento/Index', [
            'plannings' => $planningDemands,
            'clients'   => $clients,
            'filters'   => $request->only('client_id'),
        ]);
    }

    /** POST /planejamento/generate */
    public function generate(Request $request): RedirectResponse
    {
        $request->validate([
            'client_id' => ['required', 'exists:clients,id'],
            'year'      => ['required', 'integer', 'min:2024', 'max:2030'],
            'month'     => ['required', 'integer', 'min:1', 'max:12'],
        ]);

        $client = Client::findOrFail($request->client_id);
        abort_if($client->organization_id !== auth()->user()->organization_id, 403);

        $org = auth()->user()->organization;
        if (! $org->hasAnthropicKey()) {
            return back()->with('error', __('app.ai_key_missing'));
        }
        if (empty($client->monthly_posts)) {
            return back()->with('error', __('app.planning_quota_missing'));
        }

        try {
            $anthropic = $this->clientFactory->forOrganization($org);
            $plan = $this->generator->generate($client, (int) $request->year, (int) $request->month, $anthropic);

            DB::transaction(function () use ($client, $request, $plan) {
                $monthLabel = \Carbon\Carbon::create((int) $request->year, (int) $request->month, 1)
                    ->locale('pt-BR')->isoFormat('MMMM YYYY');

                $planningDemand = Demand::create([
                    'organization_id' => auth()->user()->organization_id,
                    'client_id'       => $client->id,
                    'created_by'      => auth()->id(),
                    'title'           => "Planejamento {$monthLabel}",
                    'description'     => $client->monthly_plan_notes ?: null,
                    'type'            => 'planning',
                    'status'          => 'todo',
                ]);

                foreach ($plan['items'] as $item) {
                    PlanningSuggestion::create([
                        'demand_id'   => $planningDemand->id,
                        'date'        => $item['date'],
                        'title'       => $item['title'],
                        'description' => $item['description'],
                        'channel'     => $item['channel'],
                        'status'      => 'pending',
                    ]);
                }
            });

            return back()->with('success', __('app.planning_generated'));
        } catch (\Throwable $e) {
            report($e);
            return back()->with('error', __('app.planning_generation_failed'));
        }
    }

    /** POST /planning-suggestions/{suggestion}/convert */
    public function convert(PlanningSuggestion $suggestion): RedirectResponse
    {
        $this->authorizeSuggestion($suggestion);
        abort_if($suggestion->status !== 'pending', 422, 'Item já foi convertido ou rejeitado.');

        DB::transaction(function () use ($suggestion) {
            $planningDemand = $suggestion->demand;
            $newDemand = Demand::create([
                'organization_id' => $planningDemand->organization_id,
                'client_id'       => $planningDemand->client_id,
                'created_by'      => auth()->id(),
                'title'           => $suggestion->title,
                'description'     => $suggestion->description,
                'channel'         => $suggestion->channel,
                'deadline'        => $suggestion->date,
                'type'            => 'demand',
                'status'          => 'todo',
            ]);
            $suggestion->update([
                'status' => 'accepted',
                'converted_demand_id' => $newDemand->id,
            ]);
        });

        return back()->with('success', __('app.planning_converted'));
    }

    /** POST /planning-suggestions/convert-bulk */
    public function convertBulk(Request $request): RedirectResponse
    {
        $request->validate([
            'ids'   => ['required', 'array', 'min:1', 'max:100'],
            'ids.*' => ['integer', 'exists:planning_suggestions,id'],
        ]);

        $count = 0;
        DB::transaction(function () use ($request, &$count) {
            $suggestions = PlanningSuggestion::whereIn('id', $request->ids)
                ->with('demand')->get();
            foreach ($suggestions as $s) {
                if ($s->demand->organization_id !== auth()->user()->organization_id) continue;
                if ($s->status !== 'pending') continue;
                $new = Demand::create([
                    'organization_id' => $s->demand->organization_id,
                    'client_id' => $s->demand->client_id,
                    'created_by' => auth()->id(),
                    'title' => $s->title, 'description' => $s->description,
                    'channel' => $s->channel, 'deadline' => $s->date,
                    'type' => 'demand', 'status' => 'todo',
                ]);
                $s->update(['status' => 'accepted', 'converted_demand_id' => $new->id]);
                $count++;
            }
        });

        return back()->with('success', __('app.planning_bulk_converted', ['count' => $count]));
    }

    /** POST /planning-suggestions/{suggestion}/redesign */
    public function redesign(RedesignItemRequest $request, PlanningSuggestion $suggestion): JsonResponse|RedirectResponse
    {
        $this->authorizeSuggestion($suggestion);
        $org = auth()->user()->organization;
        if (! $org->hasAnthropicKey()) {
            return response()->json(['ok' => false, 'message' => __('app.ai_key_missing')], 422);
        }

        $anthropic = $this->clientFactory->forOrganization($org);
        try {
            $new = $this->redesigner->redesign($suggestion, $request->validated('feedback'), $anthropic);
            $suggestion->update([
                'title' => $new['title'],
                'description' => $new['description'],
                'channel' => $new['channel'],
                'date' => $new['date'],
            ]);
            return response()->json(['ok' => true, 'suggestion' => $suggestion->fresh()]);
        } catch (\Throwable $e) {
            report($e);
            return response()->json(['ok' => false, 'message' => __('app.planning_redesign_failed')], 422);
        }
    }

    /** POST /planning-suggestions/{suggestion}/reject */
    public function reject(PlanningSuggestion $suggestion): RedirectResponse
    {
        $this->authorizeSuggestion($suggestion);
        $suggestion->update(['status' => 'rejected']);
        return back()->with('success', __('app.planning_rejected'));
    }

    /** PATCH /planning-suggestions/{suggestion} */
    public function update(Request $request, PlanningSuggestion $suggestion): RedirectResponse
    {
        $this->authorizeSuggestion($suggestion);
        $data = $request->validate([
            'title' => ['sometimes', 'string', 'min:3', 'max:140'],
            'description' => ['sometimes', 'string', 'min:10', 'max:600'],
            'channel' => ['sometimes', 'string'],
            'date' => ['sometimes', 'date'],
            'status' => ['sometimes', 'in:pending,accepted,rejected'],
        ]);
        $suggestion->update($data);
        return back()->with('success', __('app.planning_updated'));
    }

    private function authorizeSuggestion(PlanningSuggestion $suggestion): void
    {
        abort_if(
            $suggestion->demand->organization_id !== auth()->user()->organization_id,
            403,
        );
    }
}
```

### 3. Modify `routes/web.php` — append inside auth+verified group

```php
Route::prefix('planejamento')->name('planejamento.')->group(function () {
    Route::get('/', [\App\Http\Controllers\MonthlyPlanningController::class, 'index'])->name('index');
    Route::post('/generate', [\App\Http\Controllers\MonthlyPlanningController::class, 'generate'])->name('generate');
});
Route::post('/planning-suggestions/{suggestion}/convert', [\App\Http\Controllers\MonthlyPlanningController::class, 'convert'])->name('planning-suggestions.convert');
Route::post('/planning-suggestions/convert-bulk', [\App\Http\Controllers\MonthlyPlanningController::class, 'convertBulk'])->name('planning-suggestions.convertBulk');
Route::post('/planning-suggestions/{suggestion}/redesign', [\App\Http\Controllers\MonthlyPlanningController::class, 'redesign'])->name('planning-suggestions.redesign');
Route::post('/planning-suggestions/{suggestion}/reject', [\App\Http\Controllers\MonthlyPlanningController::class, 'reject'])->name('planning-suggestions.reject');
Route::patch('/planning-suggestions/{suggestion}', [\App\Http\Controllers\MonthlyPlanningController::class, 'update'])->name('planning-suggestions.update');
```

Optionally redirect `/planning` → `/planejamento`:
```php
Route::redirect('/planning', '/planejamento');
```

### 4. Append to `lang/pt-BR/app.php` (+mirror en/es):

```php
'planning_generated'          => 'Planejamento gerado com sucesso.',
'planning_generation_failed'  => 'Não foi possível gerar o planejamento. Tente novamente.',
'planning_converted'          => 'Item convertido em demanda.',
'planning_bulk_converted'     => ':count demandas criadas.',
'planning_redesign_failed'    => 'Não foi possível redesenhar o item.',
'planning_rejected'           => 'Item rejeitado.',
'planning_updated'            => 'Item atualizado.',
'planning_quota_missing'      => 'Configure o plano mensal do cliente para gerar o planejamento.',
```

### 5. Relation on Demand model

If `Demand::planningSuggestions()` does not exist, add to `app/Models/Demand.php`:
```php
public function planningSuggestions(): \Illuminate\Database\Eloquent\Relations\HasMany
{
    return $this->hasMany(PlanningSuggestion::class);
}
```
Check first — may already exist.
  </action>
  <verify>
    <automated>php -l app/Http/Controllers/MonthlyPlanningController.php && php -l app/Http/Requests/RedesignItemRequest.php && grep -q "planejamento.index" routes/web.php && grep -q "planning-suggestions.convert" routes/web.php && grep -q "planning_generated" lang/pt-BR/app.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public function (index|generate|convert|convertBulk|redesign|reject|update)" app/Http/Controllers/MonthlyPlanningController.php` = 7
    - `grep -q "type.*=>.*'planning'" app/Http/Controllers/MonthlyPlanningController.php` → exit 0 (creates planning-type demand per D-20)
    - `grep -q "converted_demand_id" app/Http/Controllers/MonthlyPlanningController.php` → exit 0 (convert sets FK per D-22)
    - `grep -q "hasAnthropicKey" app/Http/Controllers/MonthlyPlanningController.php` → exit 0
    - `grep -q "DB::transaction" app/Http/Controllers/MonthlyPlanningController.php` → exit 0 (atomicity)
    - `grep -q "planejamento.index" routes/web.php` → exit 0
    - `grep -q "planejamento.generate" routes/web.php` → exit 0
    - `grep -q "planning-suggestions.convertBulk" routes/web.php` → exit 0
    - `grep -q "planning_generated" lang/pt-BR/app.php` → exit 0
    - `grep -q "planning_quota_missing" lang/pt-BR/app.php` → exit 0
    - `php -l` passes on all new PHP files
    - `grep -q "Briefy contributors" app/Http/Controllers/MonthlyPlanningController.php` → exit 0
  </acceptance_criteria>
  <done>Controller with 7 methods, all routes registered, translations in place.</done>
</task>

<task type="auto">
  <name>Task 4: Cost estimate endpoint + shared CostConfirmModal component (D-34)</name>
  <read_first>
    - app/Http/Controllers/MonthlyPlanningController.php (from Task 3 — extend with estimateCost action)
    - .planning/phases/03-ai-integration/03-CONTEXT.md D-34 (cost confirmation policy)
    - .planning/phases/03-ai-integration/03-AI-SPEC.md §4 Cost table (opus input $15 / output $75 per MTok)
    - D:\projetos\briefy\resources\js\Components\Sidebar.tsx (design-token reference)
  </read_first>
  <files>
    - app/Services/Ai/CostEstimator.php (NEW)
    - app/Http/Controllers/MonthlyPlanningController.php (MODIFY — add estimateCost)
    - routes/web.php (MODIFY — add estimate-cost route)
    - resources/js/Components/CostConfirmModal.tsx (NEW — shared; Plan 12 also uses it)
  </files>
  <action>
Per D-34 (cost confirmation prompts for expensive operations), add a lightweight server-side estimator + a **shared** frontend modal component. Plan 12 (Managed Agents launch) reuses the same component.

### 1. Create `app/Services/Ai/CostEstimator.php`

Deterministic, fast (no API call). Uses AI-SPEC §4 pricing table + a rough 400-tokens-per-item heuristic for monthly plan generation. NOT intended to match actual billing — the purpose is an "is this ~$0.01 or ~$5?" signal so users can cancel before triggering Opus on 200-item plans.

```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace App\Services\Ai;

final class CostEstimator
{
    // Opus 4.7 cost per 1M tokens (AI-SPEC §4).
    private const OPUS_INPUT_PER_MTOK  = 15.00;
    private const OPUS_OUTPUT_PER_MTOK = 75.00;

    // Managed Agents session (Opus-backed) — rough average per research pass.
    private const MA_SESSION_AVG_USD = 0.80;

    /**
     * Rough cost estimate (USD) for generating a monthly plan of N items.
     * Input budget: ~800 tokens (prompt + client context). Output: ~400 tokens per item.
     */
    public function monthlyPlan(int $monthlyPosts): float
    {
        $inputTok  = 800 + ($monthlyPosts * 40);
        $outputTok = max(400, $monthlyPosts * 400);
        return ($inputTok / 1_000_000) * self::OPUS_INPUT_PER_MTOK
             + ($outputTok / 1_000_000) * self::OPUS_OUTPUT_PER_MTOK;
    }

    /**
     * Managed Agents research session — averaged over 10-15 insights + 20-30 page crawl.
     * BYOK + highly variable; treat as "ballpark" for the confirmation modal.
     */
    public function clientResearchSession(): float
    {
        return self::MA_SESSION_AVG_USD;
    }
}
```

### 2. Modify `app/Http/Controllers/MonthlyPlanningController.php` — add `estimateCost` method

```php
public function estimateCost(Request $request, \App\Services\Ai\CostEstimator $estimator): \Illuminate\Http\JsonResponse
{
    $request->validate(['client_id' => ['required', 'integer', 'exists:clients,id']]);
    $client = \App\Models\Client::findOrFail($request->client_id);
    abort_if($client->organization_id !== auth()->user()->organization_id, 403);

    $posts = (int) $client->monthly_posts;
    $cost = $estimator->monthlyPlan($posts);
    return response()->json([
        'cost_usd'         => round($cost, 4),
        'model'            => 'opus',
        'monthly_posts'    => $posts,
        'confirm_required' => $cost > 0.05, // threshold per D-34
    ]);
}
```

Constructor-inject CostEstimator OR resolve per-call via method signature (shown above).

### 3. Modify `routes/web.php` — append inside the planejamento group

```php
Route::get('/estimate-cost', [\App\Http\Controllers\MonthlyPlanningController::class, 'estimateCost'])
    ->name('estimate-cost');
```

(Full route name becomes `planejamento.estimate-cost`.)

### 4. Create `resources/js/Components/CostConfirmModal.tsx` (SHARED — Plan 12 also uses it)

AGPL header at top. Target ≥ 60 lines.

```tsx
// (c) 2026 Briefy contributors — AGPL-3.0
import { AiIcon } from '@/Components/AiIcon';
import { X } from 'lucide-react';
import { useTranslation } from 'react-i18next';

interface Props {
  open: boolean;
  costUsd: number;
  title: string;          // e.g., 'Gerar planejamento mensal' or 'Iniciar pesquisa com IA'
  body?: string;          // additional operation-specific context (e.g., '20-40 min execution')
  confirmLabel?: string;  // default: 'Continuar'
  busy?: boolean;
  onConfirm: () => void;
  onCancel: () => void;
}

export function CostConfirmModal({ open, costUsd, title, body, confirmLabel, busy, onConfirm, onCancel }: Props) {
  const { t } = useTranslation();
  if (!open) return null;

  const costFmt = costUsd < 0.01 ? '<$0.01' : '~$' + costUsd.toFixed(costUsd < 1 ? 3 : 2);

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4" onClick={busy ? undefined : onCancel}>
      <div className="max-w-sm w-full rounded-[16px] bg-white p-6 shadow-lg dark:bg-[#111827] dark:text-[#f9fafb]" onClick={e => e.stopPropagation()}>
        <div className="mb-3 flex items-center justify-between">
          <div className="flex items-center gap-2">
            <AiIcon size={20} />
            <h2 className="text-base font-semibold">{title}</h2>
          </div>
          {!busy && (
            <button aria-label={t('common.dismiss')} onClick={onCancel} className="text-[#9ca3af] hover:text-[#6b7280]">
              <X size={16} />
            </button>
          )}
        </div>
        <p className="text-sm text-[#6b7280] dark:text-[#9ca3af] mb-2">
          {t('common.costConfirm.prefix')} <strong className="text-[#111827] dark:text-[#f9fafb]">{costFmt}</strong>. {t('common.costConfirm.suffix')}
        </p>
        {body && <p className="text-xs text-[#9ca3af] mb-4">{body}</p>}
        <div className="flex justify-end gap-2">
          <button onClick={onCancel} disabled={busy}
            className="rounded-[8px] border border-[#e5e7eb] dark:border-[#1f2937] px-3 py-1.5 text-sm text-[#6b7280] hover:border-[#7c3aed] hover:text-[#7c3aed] disabled:opacity-50">
            {t('common.cancel')}
          </button>
          <button onClick={onConfirm} disabled={busy}
            className="rounded-[8px] bg-[#7c3aed] hover:bg-[#6d28d9] px-4 py-1.5 text-sm font-medium text-white disabled:opacity-60">
            {busy ? t('common.saving') : (confirmLabel ?? t('common.continue'))}
          </button>
        </div>
      </div>
    </div>
  );
}
```

i18n additions (pt-BR.json / en.json / es.json) under `common.costConfirm`:

```json
"common": {
  "costConfirm": {
    "prefix": "Essa operação custará",
    "suffix": "Continuar?"
  },
  "continue": "Continuar",
  "cancel": "Cancelar"
}
```

(EN/ES: translate naturally. Merge with existing keys — do NOT overwrite.)

### 5. Consumer wiring note (for Plan 11 — PlanejamentoIndex)

The actual wiring into the Planejamento generation modal lives in Plan 11 Task 3 (which Plan 06 depends on via the shared component). Plan 11's submit flow becomes:

1. User fills `client_id` + `month` + clicks "Gerar Planejamento"
2. Frontend calls `fetch(route('planejamento.estimate-cost'), {client_id})` → receives `{cost_usd, confirm_required}`
3. If `confirm_required === true`, render `<CostConfirmModal>` with `costUsd` and `onConfirm = () => router.post(route('planejamento.generate'), ...)`
4. If `confirm_required === false`, skip modal and submit directly

Plan 11 frontmatter will be updated (in the matching revision task) to consume `CostConfirmModal` from `@/Components/CostConfirmModal`.
  </action>
  <verify>
    <automated>php -l app/Services/Ai/CostEstimator.php && grep -q "estimateCost" app/Http/Controllers/MonthlyPlanningController.php && grep -q "estimate-cost" routes/web.php && test -f resources/js/Components/CostConfirmModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `app/Services/Ai/CostEstimator.php` exists
    - `grep -q "monthlyPlan" app/Services/Ai/CostEstimator.php` → exit 0
    - `grep -q "clientResearchSession" app/Services/Ai/CostEstimator.php` → exit 0
    - `grep -q "public function estimateCost" app/Http/Controllers/MonthlyPlanningController.php` → exit 0
    - `grep -q "estimate-cost" routes/web.php` → exit 0
    - File `resources/js/Components/CostConfirmModal.tsx` exists (`wc -l` ≥ 50)
    - `grep -q "CostConfirmModal" resources/js/Components/CostConfirmModal.tsx` → exit 0
    - `grep -q "Briefy contributors" resources/js/Components/CostConfirmModal.tsx` → exit 0
    - `grep -q "costConfirm" resources/js/locales/pt-BR.json` → exit 0
    - `php -l` passes
  </acceptance_criteria>
  <done>CostEstimator service + estimateCost endpoint + shared CostConfirmModal component ready for Plan 11 (monthly plan generation modal) and Plan 12 (MA launch).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Feature test for MonthlyPlanningController</name>
  <read_first>
    - tests/Feature/AiBriefControllerTest.php (Plan 04 — mirror service-swap pattern)
  </read_first>
  <files>
    - tests/Feature/MonthlyPlanningControllerTest.php (NEW)
  </files>
  <behavior>
    - index renders Inertia with plannings grouped
    - generate creates Demand(type='planning') + N PlanningSuggestion rows when generator is stubbed
    - generate without quota → 302 back with error
    - generate without key → 302 back with error
    - convert creates Demand(type='demand') + updates suggestion to accepted with FK
    - convertBulk creates N demands atomically
    - redesign returns JSON ok=true when redesigner stubbed
    - reject sets status=rejected
    - cross-org suggestion → 403
  </behavior>
  <action>
```php
<?php
// (c) 2026 Briefy contributors — AGPL-3.0
namespace Tests\Feature;

use App\Models\Client as ClientModel;
use App\Models\Demand;
use App\Models\Organization;
use App\Models\PlanningSuggestion;
use App\Models\User;
use App\Services\Ai\MonthlyPlanGenerator;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class MonthlyPlanningControllerTest extends TestCase
{
    use RefreshDatabase;

    private Organization $org;
    private User $user;
    private ClientModel $client;

    protected function setUp(): void
    {
        parent::setUp();
        $this->org = Organization::factory()->create();
        $this->user = User::factory()->create(['organization_id' => $this->org->id]);
        $this->client = ClientModel::factory()->create([
            'organization_id' => $this->org->id,
            'monthly_posts' => 5,
        ]);
    }

    public function test_generate_without_key_redirects_with_error(): void
    {
        $res = $this->actingAs($this->user)->post('/planejamento/generate', [
            'client_id' => $this->client->id, 'year' => 2026, 'month' => 5,
        ]);
        $res->assertRedirect();
        $res->assertSessionHas('error');
    }

    public function test_generate_without_quota_redirects_with_error(): void
    {
        $this->org->update(['anthropic_api_key_encrypted' => 'sk-ant-' . str_repeat('a', 50)]);
        $this->client->update(['monthly_posts' => null]);

        $res = $this->actingAs($this->user)->post('/planejamento/generate', [
            'client_id' => $this->client->id, 'year' => 2026, 'month' => 5,
        ]);
        $res->assertRedirect();
        $res->assertSessionHas('error');
    }

    public function test_generate_creates_planning_demand_and_suggestions(): void
    {
        $this->org->update(['anthropic_api_key_encrypted' => 'sk-ant-' . str_repeat('a', 50)]);

        $this->app->instance(MonthlyPlanGenerator::class, new class extends MonthlyPlanGenerator {
            public function __construct() {}
            public function generate($client, int $year, int $month, \Anthropic\Client $anthropic): array
            {
                return [
                    'items' => array_map(fn ($i) => [
                        'date' => sprintf('%04d-%02d-%02d', $year, $month, 5 + $i * 5),
                        'title' => "Item {$i}",
                        'description' => "Descrição do item {$i} com mais de dez caracteres.",
                        'channel' => 'instagram',
                    ], range(1, $client->monthly_posts)),
                ];
            }
        });

        $res = $this->actingAs($this->user)->post('/planejamento/generate', [
            'client_id' => $this->client->id, 'year' => 2026, 'month' => 5,
        ]);

        $res->assertRedirect();
        $res->assertSessionHas('success');
        $this->assertDatabaseHas('demands', [
            'organization_id' => $this->org->id, 'client_id' => $this->client->id, 'type' => 'planning',
        ]);
        $this->assertSame(5, PlanningSuggestion::count());
    }

    public function test_convert_creates_demand_and_updates_suggestion(): void
    {
        $planningDemand = Demand::factory()->create([
            'organization_id' => $this->org->id, 'client_id' => $this->client->id, 'type' => 'planning',
        ]);
        $sugg = PlanningSuggestion::create([
            'demand_id' => $planningDemand->id, 'date' => '2026-05-10',
            'title' => 'Foo', 'description' => 'Descrição do item com mais de dez caracteres.',
            'channel' => 'instagram', 'status' => 'pending',
        ]);

        $res = $this->actingAs($this->user)->post("/planning-suggestions/{$sugg->id}/convert");

        $res->assertRedirect();
        $sugg->refresh();
        $this->assertSame('accepted', $sugg->status);
        $this->assertNotNull($sugg->converted_demand_id);
        $this->assertDatabaseHas('demands', ['id' => $sugg->converted_demand_id, 'type' => 'demand']);
    }

    public function test_reject_sets_status(): void
    {
        $pd = Demand::factory()->create(['organization_id' => $this->org->id, 'client_id' => $this->client->id, 'type' => 'planning']);
        $s = PlanningSuggestion::create(['demand_id' => $pd->id, 'date' => '2026-05-10', 'title' => 'x', 'description' => 'descrição longa', 'status' => 'pending']);

        $this->actingAs($this->user)->post("/planning-suggestions/{$s->id}/reject")->assertRedirect();
        $this->assertSame('rejected', $s->fresh()->status);
    }

    public function test_cross_org_suggestion_returns_403(): void
    {
        $otherOrg = Organization::factory()->create();
        $other = User::factory()->create(['organization_id' => $otherOrg->id]);
        $pd = Demand::factory()->create(['organization_id' => $this->org->id, 'client_id' => $this->client->id, 'type' => 'planning']);
        $s = PlanningSuggestion::create(['demand_id' => $pd->id, 'date' => '2026-05-10', 'title' => 'x', 'description' => 'descrição longa', 'status' => 'pending']);

        $this->actingAs($other)->post("/planning-suggestions/{$s->id}/convert")->assertStatus(403);
    }
}
```
  </action>
  <verify>
    <automated>php -l tests/Feature/MonthlyPlanningControllerTest.php && grep -c "public function test_" tests/Feature/MonthlyPlanningControllerTest.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public function test_" tests/Feature/MonthlyPlanningControllerTest.php` ≥ 6
    - `grep -q "MonthlyPlanGenerator" tests/Feature/MonthlyPlanningControllerTest.php` → exit 0
    - `grep -q "assertDatabaseHas.*demands.*type.*planning" tests/Feature/MonthlyPlanningControllerTest.php` → exit 0
    - `php -l` passes
  </acceptance_criteria>
  <done>6+ test methods covering all critical behaviors including BYOK gate, quota gate, convert-single, reject, cross-org.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→generate | User triggers Opus 4.7 call (most expensive operation in Phase 3) |
| browser→convert-bulk | Bulk demand creation — max:100 bounds |
| browser→redesign | User-authored feedback goes into system prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-50 | Tampering | generator output | mitigate | Tool-use schema + Laravel validator 2nd-gate |
| T-03-51 | Denial of service | convert-bulk | mitigate | `ids: max:100` hard cap |
| T-03-52 | Denial of service | generate (Opus cost) | accept | BYOK; user pays their own API bill |
| T-03-53 | Spoofing | cross-org suggestion | mitigate | authorizeSuggestion() checks demand→org chain |
| T-03-54 | Tampering | redesign feedback | mitigate | `required|string|min:5|max:1000` + Haiku temperature 0.7 |
| T-03-55 | Information disclosure | plan prompt | accept | Same as brief — part of BYOK contract |
</threat_model>

<verification>
- All routes registered (`php artisan route:list | grep planejamento` shows 2, `grep planning-suggestions` shows 5)
- Schema validation enforces exact item count
- DB transaction ensures atomicity on generate + convert + bulk
</verification>

<success_criteria>
- must_haves.truths all observable
- Feature tests run; factory-related skips noted in SUMMARY
</success_criteria>

<output>
Create `.planning/phases/03-ai-integration/03-06-SUMMARY.md` with exact route counts + test-suite status.
</output>
