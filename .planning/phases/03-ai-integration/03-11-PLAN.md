---
phase: 03-ai-integration
plan: 11
type: execute
wave: 2
depends_on: [06, 08, 10]
files_modified:
  - resources/js/pages/Planejamento/Index.tsx
  - resources/js/Components/PlanningCard.tsx
  - resources/js/Components/DashboardPlanningWidget.tsx
  - resources/js/Components/Sidebar.tsx
  - resources/js/pages/Dashboard.tsx
  - app/Http/Controllers/DashboardController.php
  - routes/web.php
# Note: resources/js/Components/CostConfirmModal.tsx is created in Plan 06 (shared component).
# Plan 11 CONSUMES it in the generation modal submit flow per D-34.
autonomous: false
requirements: [MPLAN-03, MPLAN-04, MPLAN-05]
must_haves:
  truths:
    - "Sidebar has 'Planejamento' nav item routing to /planejamento (existing /planning route, if any, redirects)"
    - "Page /planejamento renders AppLayout with header filter (client select) + month-grouped list + primary Gerar CTA"
    - "Generation modal captures client + month, validates (warn if client has no monthly_posts), POSTs to /planejamento/generate, closes on success"
    - "PlanningCard renders a single PlanningSuggestion: date chip, title, channel chip, description, 3 actions (Converter / Rejeitar / Redesenhar)"
    - "Bulk-convert floating bar appears when at least one card selected; POSTs selected IDs to convert-bulk"
    - "Redesign action expands inline textarea, POSTs to redesign endpoint, updates card in-place"
    - "Dashboard shows DashboardPlanningWidget per D-27 (visibility: planning_day-1 through planning_day+2, dismissible via localStorage)"
  artifacts:
    - path: "resources/js/pages/Planejamento/Index.tsx"
      provides: "Main planejamento page"
      min_lines: 250
    - path: "resources/js/Components/PlanningCard.tsx"
      provides: "Single-suggestion card"
      min_lines: 150
    - path: "resources/js/Components/DashboardPlanningWidget.tsx"
      provides: "Reminder widget"
      min_lines: 80
  key_links:
    - from: "resources/js/Components/Sidebar.tsx"
      to: "/planejamento route"
      via: "navItems href"
      pattern: "/planejamento"
    - from: "Planejamento/Index.tsx"
      to: "MonthlyPlanningController::generate"
      via: "router.post(route planejamento.generate)"
      pattern: "planejamento.generate"
    - from: "DashboardPlanningWidget"
      to: "/planejamento?client_id=X"
      via: "Link href"
      pattern: "planejamento\\?client_id"
---

<objective>
Entregar a **página `/planejamento`** (D-24, D-25, D-26, D-28), o **widget de lembrete no dashboard** (D-27), e ajustar a Sidebar. Consome os endpoints do plan 06.

Purpose: completa a capability MPLAN-05 (converter itens em demandas). Sem esta UI, o usuário não consegue usar o planejamento gerado pelo backend.

Output: página completa + card reutilizável + widget + sidebar + dashboard wire-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ai-integration/03-UI-SPEC.md
@.planning/phases/03-ai-integration/03-CONTEXT.md
@.planning/phases/03-ai-integration/03-PATTERNS.md

<interfaces>
Inertia props to `Planejamento/Index.tsx`:

```ts
interface PlanningDemand {
  id: number; title: string; client: {id: number; name: string}; created_at: string;
  planning_suggestions: PlanningSuggestion[];
}
interface PlanningSuggestion {
  id: number; date: string; title: string; description: string; channel?: string;
  status: 'pending' | 'accepted' | 'rejected';
  converted_demand_id?: number | null;
}
interface Props {
  plannings: PlanningDemand[];
  clients: Array<{id, name, monthly_posts: number|null, monthly_plan_notes: string|null}>;
  filters: { client_id?: string };
}
```

Routes consumed:
- GET /planejamento
- POST /planejamento/generate
- POST /planning-suggestions/{s}/convert
- POST /planning-suggestions/convert-bulk
- POST /planning-suggestions/{s}/redesign (JSON response)
- POST /planning-suggestions/{s}/reject

DashboardPlanningWidget:
```ts
interface WidgetProps { clients: Array<{id, name, planning_day: number|null}>; }
```
Visibility logic (D-27): show when today.getDate() is in [planning_day - 1, planning_day + 2]; dismissal persisted as `localStorage[planning_reminder_dismissed:{client_id}:{YYYY-MM}]`.
</interfaces>

Source references:
- UI-SPEC §7 (Sidebar nav), §8 (page), §9 (widget), §10 (AI icon audit)
- PATTERNS.md line 78 (Sidebar update) + lines 69-71 (page analog)
- D:\projetos\briefy\resources\js\Components\Sidebar.tsx (existing)
- D:\projetos\briefy\resources\js\pages\Dashboard.tsx (existing)
- D:\projetos\briefy\app\Http\Controllers\DashboardController.php (existing or inline closure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar nav update + /planning redirect</name>
  <read_first>
    - D:\projetos\briefy\resources\js\Components\Sidebar.tsx (entire file)
    - D:\projetos\briefy\routes\web.php
    - .planning/phases/03-ai-integration/03-UI-SPEC.md §7
  </read_first>
  <files>
    - resources/js/Components/Sidebar.tsx (MODIFY)
    - routes/web.php (MODIFY — add redirect if /planning still exists)
  </files>
  <action>
In `Sidebar.tsx`, find the navItems array (around line 14). Change the planning entry:

Before:
```tsx
{ key: 'nav.planning', icon: Calendar, href: '/planning' }
```

After:
```tsx
{ key: 'nav.planning', icon: Calendar, href: '/planejamento' }
```

Keep lucide `Calendar` icon (per UI-SPEC §7: sidebar nav is not an AI surface, so chatbot icon NOT used).

If any `isActive` matching uses a strict equality to `/planning`, update to use `url.startsWith('/planejamento')` OR keep a legacy `|| url.startsWith('/planning')` fallback.

In `routes/web.php`, if `/planning` is defined as a first-class route, replace with:
```php
Route::redirect('/planning', '/planejamento');
```

The old page at `resources/js/pages/Planning/Index.tsx` can be deleted once no longer referenced; leaving it in place is acceptable since the route is gone.
  </action>
  <verify>
    <automated>grep -q "/planejamento" resources/js/Components/Sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "/planejamento" resources/js/Components/Sidebar.tsx` → exit 0
    - `grep -q "href: '/planning'" resources/js/Components/Sidebar.tsx` → no match (old href removed)
    - `grep -q "planejamento" routes/web.php` → exit 0 (either via Plan 06 route group or a Route::redirect)
  </acceptance_criteria>
  <done>Sidebar points to /planejamento; old path redirected.</done>
</task>

<task type="auto">
  <name>Task 2: PlanningCard component (3 actions + redesign inline + selection)</name>
  <read_first>
    - D:\projetos\briefy\resources\js\Components\KanbanBoard.tsx (DemandCard pattern lines 40-72)
    - .planning/phases/03-ai-integration/03-UI-SPEC.md §8 "Planning item card" + "Redesign inline state" (lines 405-427)
  </read_first>
  <files>
    - resources/js/Components/PlanningCard.tsx (NEW)
  </files>
  <action>
Create `resources/js/Components/PlanningCard.tsx`. Target ≥ 150 lines.

Exported type:
```ts
export interface Suggestion {
  id: number; date: string; title: string; description: string; channel?: string;
  status: 'pending' | 'accepted' | 'rejected';
  converted_demand_id?: number | null;
}
```

Props:
```ts
interface PlanningCardProps {
  suggestion: Suggestion;
  selected: boolean;
  onToggleSelect: (id: number) => void;
  onLocalUpdate?: (next: Suggestion) => void;
}
```

Visual states:
- pending default: `rounded-[12px] border border-[#e5e7eb] dark:border-[#1f2937] bg-white dark:bg-[#111827] p-4 hover:border-[#7c3aed]/40`
- selected: add `ring-2 ring-[#7c3aed] border-[#7c3aed]`
- rejected: `opacity-50` + title gets `line-through`
- accepted: `border-[#10b981]/40` + small green Check icon top-right (14px)

Card layout top-to-bottom:
1. Top row: `<input type="checkbox" className="h-4 w-4 accent-[#7c3aed]" checked={selected} onChange={() => onToggleSelect(id)} disabled={status !== 'pending'}>` on left; date chip on right (`text-[11px] text-[#9ca3af]`, format `new Date(date).toLocaleDateString('pt-BR', {day: '2-digit', month: 'short'})`).
2. Title: `text-sm font-semibold line-clamp-2 mt-2 text-[#111827] dark:text-[#f9fafb]`.
3. Channel chip: `mt-2 inline-flex rounded-full bg-[#f3f4f6] dark:bg-[#1f2937] px-2 py-0.5 text-[11px] font-medium capitalize`.
4. Description: `mt-2 line-clamp-3 text-xs text-[#6b7280] dark:text-[#9ca3af]`.
5. Action row (only when status='pending' AND not redesigning): border-top divider + 3 buttons.
   - **Converter:** primary-ghost (`border-[#7c3aed] text-[#7c3aed] hover:bg-[#7c3aed]/10`), lucide Check 13px. onClick → `router.post(route('planning-suggestions.convert', suggestion.id), {}, {preserveScroll: true})`.
   - **Rejeitar:** ghost-destructive (`border-[#e5e7eb] text-[#9ca3af] hover:border-red-400 hover:text-red-500`), lucide X 13px. onClick → `router.post(route('planning-suggestions.reject', suggestion.id), {}, {preserveScroll: true})`.
   - **Redesenhar:** ghost (`border-[#e5e7eb] text-[#6b7280] hover:border-[#7c3aed] hover:text-[#7c3aed]`), lucide RefreshCw 13px. onClick → toggles `redesigning=true` state.
6. Redesign inline state (when `redesigning=true`):
   - Textarea (`w-full resize-none rounded-[8px] border border-[#7c3aed] bg-white dark:bg-[#0b0f14] px-3 py-2 text-sm min-h-[80px]`), placeholder from i18n key `planning.redesignPlaceholder`.
   - Inline error banner for empty feedback: `planning.redesignEmpty`.
   - Cancel + Aplicar buttons. Aplicar issues a `fetch` POST to `route('planning-suggestions.redesign', suggestion.id)` with `{feedback}`; on success, destructure `{ok, suggestion: updated}` from JSON and call `onLocalUpdate?.({...suggestion, ...updated})`; close the textarea; on error, show message from response.

CSRF for the fetch: read from `<meta name="csrf-token">`. Use `credentials: 'same-origin'` + `X-Requested-With: XMLHttpRequest`.

Loading state during Aplicar: button label changes to `planning.applyingRedesign` ("Aplicando…") with leading `<AiIcon size={12} spinning />`.

AGPL header at top. Imports: `router` from `@inertiajs/react`, `useTranslation` from `react-i18next`, lucide icons (Check, X, RefreshCw), AiIcon from `@/Components/AiIcon`.
  </action>
  <verify>
    <automated>test -f resources/js/Components/PlanningCard.tsx && test $(wc -l < resources/js/Components/PlanningCard.tsx) -ge 120</automated>
  </verify>
  <acceptance_criteria>
    - File exists, `wc -l` ≥ 120
    - `grep -q "planning-suggestions.convert" resources/js/Components/PlanningCard.tsx` → exit 0
    - `grep -q "planning-suggestions.reject" resources/js/Components/PlanningCard.tsx` → exit 0
    - `grep -q "planning-suggestions.redesign" resources/js/Components/PlanningCard.tsx` → exit 0
    - `grep -q "AiIcon" resources/js/Components/PlanningCard.tsx` → exit 0
    - `grep -q "planning.redesignPlaceholder" resources/js/Components/PlanningCard.tsx` → exit 0
    - `grep -q "Briefy contributors" resources/js/Components/PlanningCard.tsx` → exit 0
  </acceptance_criteria>
  <done>Card handles 3 actions + redesign inline + selection + all 4 status-visual states.</done>
</task>

<task type="auto">
  <name>Task 3: /planejamento page (filter + month groups + generation modal + bulk bar)</name>
  <read_first>
    - D:\projetos\briefy\resources\js\pages\Demands\Index.tsx (canonical page pattern)
    - resources/js/Components/PlanningCard.tsx (from Task 2)
    - .planning/phases/03-ai-integration/03-UI-SPEC.md §8 (full spec, lines 379-435)
  </read_first>
  <files>
    - resources/js/pages/Planejamento/Index.tsx (NEW — create the directory)
  </files>
  <action>
Create the full Inertia page. Target ≥ 250 lines. Structure:

**1. Sticky page header** (`sticky top-0 z-10 bg-white dark:bg-[#111827] border-b`):
- Left: title `text-lg font-semibold` → "Planejamento mensal".
- Center-left: client filter `<select>` with `max-w-[280px]`. First option `Todos os clientes` (i18n `planning.allClients`). On change: `router.get(route('planejamento.index'), {client_id}, {preserveState: true, replace: true})`.
- Right: primary CTA "Gerar Planejamento" (chatbot icon 16px + label). Opens the generation modal. Disabled + tooltip when `auth.user.organization.has_anthropic_key === false` (Inertia shared prop shape per Plan 01; H3 fix — repo uses `auth.user.organization`, NOT `auth.organization`).

**2. Page body** — month-grouped list:
- Iterate `plannings`, group by month label (`new Date(created_at).toLocaleDateString('pt-BR', {month: 'long', year: 'numeric'})`).
- Per month: `<h2 className="mb-3 text-base font-semibold capitalize">{label}</h2>`.
- Per planning within the month: small header (`client.name` + item count) + grid of cards.
- Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`, each card is `<PlanningCard>`.

**3. Generation modal** (custom overlay — NOT Headless UI Dialog for simplicity):
- Backdrop: `fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4`, click outside closes (unless generating).
- Card: `max-w-md rounded-[16px] bg-white dark:bg-[#111827] p-6 shadow-lg`.
- Title row: `<AiIcon size={20} />` + "Gerar planejamento mensal" (i18n `planning.generateModalTitle`).
- Form fields: client `<select>` (pre-filled from current filter; each option shows `{name}` and optionally ` ({monthly_posts}/mês)` when set) + `<input type="month">` defaulted to next month.
- Info banner when selected client has no `monthly_posts`: `<div className="rounded-[8px] border border-[#f59e0b]/30 bg-[#f59e0b]/10 p-3 text-xs text-[#f59e0b]">{t('planning.noQuotaWarning')}</div>`. Disable submit in this case.
- Submit flow (D-34 cost confirmation):
  1. On "Gerar" click: `fetch(route('planejamento.estimate-cost'), {method: 'GET', query: {client_id}})` — receives `{cost_usd, confirm_required}`.
  2. If `confirm_required === true`: set `<CostConfirmModal open=true costUsd={cost_usd} title={t('planning.generateModalTitle')} body={t('planning.costConfirmBody', {count: monthly_posts})} busy={generating} onConfirm={actuallyGenerate} onCancel={() => setCostOpen(false)} />`.
  3. `actuallyGenerate` runs `router.post(route('planejamento.generate'), {client_id, year, month}, {preserveScroll: true, onFinish: () => { setGenerating(false); setGenerateOpen(false); setCostOpen(false); }})`.
  4. If `confirm_required === false` (e.g., <$0.05 estimated), call `actuallyGenerate` directly — skip the extra modal.
- While generating, button shows `<AiIcon size={12} spinning />` + `planning.generating`.
- Import the shared modal: `import { CostConfirmModal } from '@/Components/CostConfirmModal';` (component owned by Plan 06).

**4. Bulk-convert floating bar** (`fixed bottom-6 right-6 z-40`):
- Render condition: `selected.size >= 1`.
- Content: `{count} selecionados` label + "Converter {count} selecionados" button + dismiss X.
- On click: `router.post(route('planning-suggestions.convertBulk'), {ids: Array.from(selected)}, {preserveScroll: true, onSuccess: () => setSelected(new Set())})`.

**5. Empty states** per UI-SPEC:
- No plannings at all: centered `<AiIcon size={64} />` + `planning.empty.noPlans.heading` (interpolates `{{clientName}}`) + body.
- (Optional) Client filter set but client has no monthly_posts: info banner.

**State:**
- `selected: Set<number>` with `toggleSelect` that clones the set.
- `generateOpen: boolean` for the modal.
- `genClientId`, `genMonth` (default: next month in `YYYY-MM` format).
- `generating: boolean`.

**Props interface:**
```ts
interface PlanningDemand { id: number; title: string; client: {id: number; name: string}; created_at: string; planning_suggestions: Suggestion[]; }
interface ClientLite { id: number; name: string; monthly_posts: number|null; monthly_plan_notes: string|null; }
interface Props { plannings: PlanningDemand[]; clients: ClientLite[]; filters: { client_id?: string }; }
```

Wrap everything in `<AppLayout>`. AGPL header at top of file.
  </action>
  <verify>
    <automated>test -f resources/js/pages/Planejamento/Index.tsx && test $(wc -l < resources/js/pages/Planejamento/Index.tsx) -ge 200</automated>
  </verify>
  <acceptance_criteria>
    - File exists, `wc -l` ≥ 200
    - `grep -q "planejamento.generate" resources/js/pages/Planejamento/Index.tsx` → exit 0
    - `grep -q "planning-suggestions.convertBulk" resources/js/pages/Planejamento/Index.tsx` → exit 0
    - `grep -q "PlanningCard" resources/js/pages/Planejamento/Index.tsx` → exit 0
    - `grep -q "noQuotaWarning" resources/js/pages/Planejamento/Index.tsx` → exit 0
    - `grep -q "has_anthropic_key" resources/js/pages/Planejamento/Index.tsx` → exit 0
    - `grep -q "AiIcon" resources/js/pages/Planejamento/Index.tsx` → exit 0
    - `grep -q "Briefy contributors" resources/js/pages/Planejamento/Index.tsx` → exit 0
  </acceptance_criteria>
  <done>Full page with filter, month groups, generation modal, bulk bar, and empty states.</done>
</task>

<task type="auto">
  <name>Task 4: DashboardPlanningWidget + Dashboard wire + DashboardController prop</name>
  <read_first>
    - D:\projetos\briefy\resources\js\pages\Dashboard.tsx
    - D:\projetos\briefy\app\Http\Controllers\DashboardController.php (if exists; otherwise check routes/web.php for the dashboard handler)
    - .planning/phases/03-ai-integration/03-UI-SPEC.md §9 (widget spec)
  </read_first>
  <files>
    - resources/js/Components/DashboardPlanningWidget.tsx (NEW)
    - resources/js/pages/Dashboard.tsx (MODIFY)
    - app/Http/Controllers/DashboardController.php (MODIFY or create via artisan if currently a closure)
  </files>
  <action>
### 1. Create `resources/js/Components/DashboardPlanningWidget.tsx`

Target ≥ 80 lines. AGPL header at top.

Visibility logic: compute `state` for each client whose `planning_day` is set.
- day == planning_day - 1 → state = `tomorrow`, copy `planning.reminder.dueTomorrow`
- day == planning_day → state = `today`, copy `planning.reminder.dueToday`
- planning_day < day ≤ planning_day + 2 → state = `overdue`, copy `planning.reminder.overdue` with `{{days}} = day - planning_day`
- otherwise → not shown

**Dismissal:** `localStorage.setItem("planning_reminder_dismissed:" + clientId + ":" + YYYY + "-" + MM, "1")`. On render, filter out clients whose dismissal key for the current year-month is set. Use a local `useState(0)` counter and `force(x => x+1)` to re-render after dismiss.

**Card style** (per UI-SPEC §9): `flex items-start gap-3 rounded-[12px] border border-[#f59e0b]/30 bg-[#f59e0b]/5 dark:bg-[#f59e0b]/10 p-4`.
- Left: `<AiIcon size={32} />` (D-15).
- Middle (`flex-1`): title `text-sm font-semibold` interpolating `planning.reminder.title` + body dynamic + `<Link href={"/planejamento?client_id=" + client.id}>` "Ver planejamento" (`text-xs text-[#6d28d9] dark:text-[#a78bfa] underline mt-2 inline-block`).
- Right: dismiss X (14px, `text-[#9ca3af] hover:text-[#6b7280]`, `aria-label={t('common.dismiss')}`).

Max 3 cards visible; if more, show `<Link href="/planejamento">+N mais</Link>`.

Props:
```ts
interface ClientWithDay { id: number; name: string; planning_day: number; }
interface Props { clients: ClientWithDay[]; }
```

### 2. Modify `app/Http/Controllers/DashboardController.php`

If the file exists, extend the method that renders `Inertia::render('Dashboard', ...)`. If `/dashboard` is a closure in routes/web.php, convert it to DashboardController::index (create the controller with `php artisan make:controller DashboardController`) and update the route binding.

Extend the prop payload:
```php
$planningReminderClients = \App\Models\Client::where('organization_id', auth()->user()->organization_id)
    ->whereNotNull('planning_day')
    ->get(['id', 'name', 'planning_day']);

return Inertia::render('Dashboard', [
    // ...existing props...
    'planningReminderClients' => $planningReminderClients,
]);
```

### 3. Modify `resources/js/pages/Dashboard.tsx`

Add imports:
```tsx
import { DashboardPlanningWidget } from '@/Components/DashboardPlanningWidget';
```

Extend the Props interface of the page component:
```ts
planningReminderClients?: Array<{id: number; name: string; planning_day: number}>;
```

Render near the top of the page body:
```tsx
{planningReminderClients && planningReminderClients.length > 0 && (
  <div className="mb-6">
    <DashboardPlanningWidget clients={planningReminderClients} />
  </div>
)}
```

Preserve all existing content — Phase 5 will add charts later; this plan's widget is additive.
  </action>
  <verify>
    <automated>test -f resources/js/Components/DashboardPlanningWidget.tsx && grep -q "DashboardPlanningWidget" resources/js/pages/Dashboard.tsx && test -f app/Http/Controllers/DashboardController.php && grep -q "planningReminderClients" app/Http/Controllers/DashboardController.php</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `resources/js/Components/DashboardPlanningWidget.tsx` exists, `wc -l` ≥ 60
    - `grep -q "planning_reminder_dismissed" resources/js/Components/DashboardPlanningWidget.tsx` → exit 0
    - `grep -q "planning.reminder.dueTomorrow" resources/js/Components/DashboardPlanningWidget.tsx` → exit 0
    - `grep -q "AiIcon size={32}" resources/js/Components/DashboardPlanningWidget.tsx` → exit 0
    - `grep -q "planningReminderClients" app/Http/Controllers/DashboardController.php` → exit 0 (hard requirement — controller MUST exist; closure-only routes are not acceptable per WARNING 6)
    - `test -f app/Http/Controllers/DashboardController.php` → exit 0 (file must exist; create via `php artisan make:controller DashboardController` if the /dashboard route was previously a closure)
    - `grep -q "DashboardPlanningWidget" resources/js/pages/Dashboard.tsx` → exit 0
    - `grep -q "Briefy contributors" resources/js/Components/DashboardPlanningWidget.tsx` → exit 0
    - `npx tsc --noEmit` (or `npm run typecheck` if configured) reports zero errors for the modified Dashboard.tsx and the new DashboardPlanningWidget.tsx
  </acceptance_criteria>
  <done>Widget renders when condition met, dismissible, wired into dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 [HUMAN]: Visual verification of /planejamento + dashboard widget</name>
  <what-built>Complete /planejamento page + PlanningCard + DashboardPlanningWidget + Sidebar nav</what-built>
  <how-to-verify>
    1. Create/edit a client: set monthly_posts=5, planning_day=(today's day-of-month)
    2. Visit /dashboard — widget should render with "dueToday" state copy
    3. Click Dispensar X on the widget — disappears; reload page — still hidden for this month
    4. Click "Planejamento" in the sidebar — navigates to /planejamento
    5. Click "Gerar Planejamento" — modal opens — select the client — submit (use current month)
    6. Expect ~20-60s while Opus 4.7 generates; page should reload with a new month group containing 5 cards
    7. Check a card's checkbox — floating bar appears bottom-right with count
    8. Click "Rejeitar" on a different card — opacity-50 + strikethrough
    9. Click "Redesenhar" on a third card — textarea appears — type feedback — Aplicar — ~3s — title/description update in-place
    10. Select 2-3 cards — bulk convert — page reloads; visit /demands — see the new demands (type=demand) populated
    11. No console errors; no `ai_key_missing` toasts when key configured
  </how-to-verify>
  <resume-signal>Type "approved" or describe any failures.</resume-signal>
  <acceptance_criteria>
    - Plan generation completes within 60s for 5 items
    - All 3 card actions work correctly
    - Bulk convert creates the expected number of demands
    - Dashboard widget shows and dismisses persistently
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-100 | Tampering | bulk convert IDs | mitigate | Backend (Plan 06) filters to suggestions whose demand.organization_id matches the auth org |
| T-03-101 | Information disclosure | widget shows planning_day | accept | Same trust level as the rest of /clients UI |
| T-03-102 | XSS | redesign response | mitigate | JSON parsed and bound via React text nodes; no dangerouslySetInnerHTML |
</threat_model>

<verification>
- All pages render without console errors
- /planejamento without client filter shows all plannings; with filter only matching client's
- Widget respects dismissal
- Bulk convert creates N demands correctly
</verification>

<success_criteria>
- All must_haves.truths observable
- Task 5 human-verify approved
</success_criteria>

<output>
Create `.planning/phases/03-ai-integration/03-11-SUMMARY.md`.
</output>
