---
phase: 04-team-management
plan: "06"
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - resources/js/pages/Invite/Accept.tsx
autonomous: true
requirements:
  - TEAM-01

must_haves:
  truths:
    - "GET /invite/{token} shows a centered card with org name and role badge"
    - "New user branch shows name + password + confirm fields with 'Criar conta e entrar' button"
    - "Existing user branch shows a confirmation message with 'Entrar na organização' button"
    - "Expired/invalid token shows error message 'Este convite expirou ou já foi utilizado.'"
    - "Page has no AppLayout/sidebar — standalone centered layout"
  artifacts:
    - path: "resources/js/pages/Invite/Accept.tsx"
      provides: "Invitation acceptance page with new/existing user branches and expired state"
  key_links:
    - from: "Accept.tsx form submit"
      to: "POST /invite/{token}/accept"
      via: "router.post(route('invitations.store', token), formData)"
      pattern: "invitations.store"
---

<objective>
Create the invitation acceptance page — a standalone centered card (no AppLayout) that handles three states: new user registration, existing user confirmation, and expired/invalid token. This is the only guest-accessible page in Phase 4.

Purpose: Invited users land on this page from the email link. It must clearly communicate the org name, role, and what action to take.
Output: resources/js/pages/Invite/Accept.tsx fully functional.
</objective>

<execution_context>
@/home/Usuario/.claude/get-shit-done/workflows/execute-plan.md
@/home/Usuario/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-team-management/04-UI-SPEC.md
@.planning/phases/04-team-management/04-CONTEXT.md
@.planning/phases/04-team-management/04-04-SUMMARY.md

<interfaces>
<!-- Inertia props from InvitationController::show() (Plan 04) -->
```typescript
interface Props {
  expired: boolean;
  invitation: { email: string; role: 'owner' | 'admin' | 'collaborator' } | null;
  organization: { name: string } | null;
  has_account: boolean;
}
```

<!-- Role labels (from UI-SPEC copywriting) -->
Role display: owner → "Proprietário", admin → "Admin", collaborator → "Colaborador"

<!-- Route to submit to -->
route('invitations.store', token) → POST /invite/{token}/accept
Payload for new user: { name: string, password: string, password_confirmation: string }
Payload for existing user: {} (no body needed, token is sufficient)

<!-- Existing component patterns to reuse -->
TextInput: resources/js/Components/TextInput.tsx
InputLabel: resources/js/Components/InputLabel.tsx
InputError: resources/js/Components/InputError.tsx
useForm: from @inertiajs/react

<!-- Design tokens from UI-SPEC -->
Page: min-h-screen bg-[#f9fafb] dark:bg-[#0b0f14] flex items-center justify-center p-4
Card: max-w-md w-full rounded-[16px] bg-white dark:bg-[#111827] p-8 shadow-lg
Submit button: w-full bg-[#7c3aed] hover:bg-[#6d28d9] rounded-[8px] py-3 text-sm font-semibold text-white
Role badge: inline-flex items-center rounded-full px-2 py-0.5 text-xs font-semibold
  collaborator: bg-[#9ca3af]/10 text-[#9ca3af] border border-[#9ca3af]/20
  admin: bg-[#3b82f6]/10 text-[#3b82f6] border border-[#3b82f6]/20
  owner: bg-[#7c3aed]/10 text-[#7c3aed] border border-[#7c3aed]/20
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Invite/Accept.tsx — guest invitation acceptance page</name>
  <files>resources/js/pages/Invite/Accept.tsx</files>
  <read_first>
    - resources/js/Components/TextInput.tsx (props interface)
    - resources/js/Components/InputLabel.tsx (props interface)
    - resources/js/Components/InputError.tsx (props interface)
    - resources/js/pages/Auth/Login.tsx (useForm pattern for guest page without AppLayout)
    - .planning/phases/04-team-management/04-UI-SPEC.md (section: /invite/{token} Invitation Accept Page)
    - .planning/phases/04-team-management/04-CONTEXT.md (D-02: flow; copywriting contract)
  </read_first>
  <action>
Create `resources/js/pages/Invite/Accept.tsx`. Add AGPL header. This page has NO AppLayout — it is a standalone centered layout.

The directory `resources/js/pages/Invite/` must be created. Create the file directly — `tsx` file creation also creates the directory.

```tsx
// (c) 2026 Briefy contributors — AGPL-3.0
import { Head, useForm } from '@inertiajs/react';
import InputError from '@/Components/InputError';
import InputLabel from '@/Components/InputLabel';
import TextInput from '@/Components/TextInput';

interface Props {
  expired: boolean;
  invitation: { email: string; role: 'owner' | 'admin' | 'collaborator' } | null;
  organization: { name: string } | null;
  has_account: boolean;
}

const roleLabels: Record<string, string> = {
  owner: 'Proprietário',
  admin: 'Admin',
  collaborator: 'Colaborador',
};

const roleBadgeClasses: Record<string, string> = {
  owner: 'bg-[#7c3aed]/10 text-[#7c3aed] border-[#7c3aed]/20',
  admin: 'bg-[#3b82f6]/10 text-[#3b82f6] border-[#3b82f6]/20',
  collaborator: 'bg-[#9ca3af]/10 text-[#9ca3af] border-[#9ca3af]/20',
};

export default function InviteAccept({ expired, invitation, organization, has_account }: Props) {
  const token = window.location.pathname.split('/invite/')[1]?.split('/accept')[0] ?? '';

  const form = useForm({
    name: '',
    password: '',
    password_confirmation: '',
  });

  const submit = (e: React.FormEvent) => {
    e.preventDefault();
    if (has_account) {
      form.post(route('invitations.store', token));
    } else {
      form.post(route('invitations.store', token));
    }
  };

  const role = invitation?.role ?? 'collaborator';
  const badgeClass = roleBadgeClasses[role] ?? roleBadgeClasses.collaborator;

  return (
    <div className="min-h-screen bg-[#f9fafb] dark:bg-[#0b0f14] flex items-center justify-center p-4">
      <Head title="Aceitar convite — Briefy" />

      <div className="max-w-md w-full rounded-[16px] bg-white dark:bg-[#111827] p-8 shadow-lg">

        {/* Briefy wordmark */}
        <div className="text-center mb-6">
          <span className="text-xl font-bold tracking-tight text-[#111827] dark:text-[#f9fafb]">briefy</span>
        </div>

        {expired || !invitation ? (
          /* ── Expired/invalid state ── */
          <div className="text-center">
            <p className="text-base font-semibold text-[#111827] dark:text-[#f9fafb] mb-2">
              Este convite expirou ou já foi utilizado.
            </p>
            <p className="text-sm text-[#6b7280]">
              Solicite um novo convite ao administrador da organização.
            </p>
          </div>
        ) : (
          <>
            {/* ── Header ── */}
            <div className="text-center mb-6">
              <h1 className="text-xl font-semibold text-[#111827] dark:text-[#f9fafb] mb-2">
                Você foi convidado para <strong>{organization?.name}</strong>
              </h1>
              <p className="text-sm text-[#6b7280] flex items-center justify-center gap-2">
                Você foi convidado como{' '}
                <span className={`inline-flex items-center rounded-full border px-2 py-0.5 text-xs font-semibold ${badgeClass}`}>
                  {roleLabels[role]}
                </span>
              </p>
            </div>

            <form onSubmit={submit} className="space-y-4">

              {has_account ? (
                /* ── Branch B: existing user ── */
                <div className="rounded-[8px] bg-[#f3f4f6] dark:bg-[#1f2937] px-4 py-3 text-sm text-[#374151] dark:text-[#d1d5db]">
                  Esta conta já existe no Briefy. Clique abaixo para entrar na organização.
                </div>
              ) : (
                /* ── Branch A: new user ── */
                <>
                  <div>
                    <InputLabel htmlFor="name" value="Seu nome" />
                    <TextInput
                      id="name"
                      type="text"
                      className="mt-1 block w-full"
                      value={form.data.name}
                      onChange={e => form.setData('name', e.target.value)}
                      required
                      autoFocus
                    />
                    <InputError message={form.errors.name} className="mt-1" />
                  </div>

                  <div>
                    <InputLabel htmlFor="password" value="Senha" />
                    <TextInput
                      id="password"
                      type="password"
                      className="mt-1 block w-full"
                      value={form.data.password}
                      onChange={e => form.setData('password', e.target.value)}
                      required
                      autoComplete="new-password"
                    />
                    <InputError message={form.errors.password} className="mt-1" />
                  </div>

                  <div>
                    <InputLabel htmlFor="password_confirmation" value="Confirmar senha" />
                    <TextInput
                      id="password_confirmation"
                      type="password"
                      className="mt-1 block w-full"
                      value={form.data.password_confirmation}
                      onChange={e => form.setData('password_confirmation', e.target.value)}
                      required
                      autoComplete="new-password"
                    />
                    <InputError message={form.errors.password_confirmation} className="mt-1" />
                  </div>
                </>
              )}

              <button
                type="submit"
                disabled={form.processing}
                className="w-full bg-[#7c3aed] hover:bg-[#6d28d9] disabled:opacity-60 rounded-[8px] py-3 text-sm font-semibold text-white transition-colors mt-2"
              >
                {form.processing
                  ? 'Aguarde...'
                  : has_account
                    ? 'Entrar na organização'
                    : 'Criar conta e entrar'
                }
              </button>

            </form>
          </>
        )}
      </div>
    </div>
  );
}
```

Notes:
- Extract the token from `window.location.pathname` because Inertia does not pass route params directly as props from InvitationController (the token is part of the URL). Alternatively, the controller can pass `token` as an explicit prop — if doing so, read InvitationController and pass token prop if it's simpler.
- Apply AGPL header at line 1.
- The `has_account=true` branch does NOT show form fields — only the confirmation message and submit button (no name/password needed for existing user).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | grep "Invite/Accept"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f resources/js/pages/Invite/Accept.tsx` returns 0
    - `npx tsc --noEmit 2>&1 | grep -c "Invite/Accept"` outputs 0 (no TypeScript errors in this file)
    - Component exports a default function named `InviteAccept`
    - `grep -n "invitations.store" resources/js/pages/Invite/Accept.tsx` shows the route call
    - `grep -n "Criar conta e entrar\|Entrar na organização\|Este convite expirou" resources/js/pages/Invite/Accept.tsx` shows all 3 copy strings
    - `grep -n "AppLayout" resources/js/pages/Invite/Accept.tsx` outputs 0 lines (no AppLayout)
  </acceptance_criteria>
  <done>Invite/Accept.tsx created with three branches (new user, existing user, expired); no AppLayout; TypeScript clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → POST /invite/{token}/accept | Browser sends name+password for new user; no role input (role from server-side invitation record) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Information Disclosure | Accept.tsx invitation props | mitigate | Only email, role, org name received from server — no token, invited_by, or internal IDs |
| T-04-18 | Tampering | Token in URL | mitigate | Token extracted from URL client-side but validated server-side in InvitationController::store(); client cannot forge a valid token |
</threat_model>

<verification>
```bash
npx tsc --noEmit 2>&1 | grep -E "error|Invite"
# Should show 0 errors related to Accept.tsx

# Visual check: build
npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- Invite/Accept.tsx created with AGPL header
- Three-state rendering: expired, new user form, existing user confirmation
- No AppLayout — standalone centered card layout
- TypeScript compilation clean (0 errors on this file)
- Correct copy strings per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/04-team-management/04-06-SUMMARY.md`
</output>
